# v0.16 Floodplains lane (#308) — running log

Branch `issue/308-floodplains-price-kept`. One line per completed task:
task id, commit hash, gates run + verdicts.

## Stage 1 (MAP / INTERIOR / DOOR) — implementer

- **preflight** — `godot --headless --import` rc=0; smoke boot `--quit` rc=0, zero `SCRIPT ERROR|Parse Error|WARNING`.
- **Task 1** (quests.json: `the_price_kept` + `chieftains_price` region casing) — committed with Tasks 2-6 (see Task 7 Step 1). Gates: json parse OK, 3 rungs + `_resolution_order` present, insert anchored after `chieftains_price` (idx 14 -> 15 of 18).
- **Task 2** (rags_meeting.json: offer row appended LAST, post-quest hub variant, 4 winter nodes) — gates: `test_dialogue` rc=0 / ^PASS / 0 noise. Shadow check `grep -c rags_price_kept qa/fixtures/rags_gates_met_start.json` = 0.
- **Task 3** (krshia_crate.json 2 hub rows + `goblin_run`; street.json `krshia_thread_goblin_run` stage after `krshia_thread_door_neutral`) — two-grep discriminator re-run: A = {inn_guests_ext_start, inn_guests_gate_start}, B = 13 scripts, intersection EMPTY. Gates: `test_dialogue` rc=0/PASS/0, `test_copy_fit` rc=0/PASS/0.
- **Task 4** (rags_camp.json NEW 12x9; `rags_camp_mouth` at floodplains (17,22); moods.rags_camp after `floodplains`; LANDMARK_TOKENS +1 after `"floodplains"`; MAP_REQUIRES +1 after `ruin_surface`) — stem uniqueness proved (0 hits in data/src/tests, no arena id). Gates: `data_lint.py` OK 107 files / 23 maps; BFS from (5,7) reaches all open cells, zero unreachable; both door landing cells verified unblocked+unoccupied in both directions; `scene_dynamism` composite **65.60 (dynamic)**.
- **Task 5** (camp_meat_rack, `present_when.requires {camp_carry_jobs, corusdeer_culled}`) — `corusdeer_range` verified unchanged, still banks the frozen `corusdeer_culled`. Gates: `data_lint.py` OK; (9,6) has 4 open neighbours.
- **Task 6** (5 combatant rows after `rags`; `camp_ground_press` encounter; 2 ENCOUNTER_CELLS; VISUAL-LOG row) — tuned per RULING A. Gates: `test_combat_data` rc=0/PASS/0, `test_combat_visuals` rc=0/PASS/0, **full `sim_combat_batch` rc=0, PASS over 136 cells, zero FAIL, zero noise**.
- **Task 7 Step 1** — the single producers+gates commit (Tasks 1-6). Pre-commit gates: `test_content` / `test_reachability` / `test_quests` / `test_fixture_coherence` / `test_inn_guests` / `test_copy_fit` / `test_dialogue` / `test_shipped_ids` / `test_sprite_registry` all rc=0 + ^PASS + 0 noise; `comment_census.py --check` rc=0, DATA **14.9% <= 15.0%**.

### Measured balance numbers (for the PR body, RULING A)

| cell | win rate | median rounds | range | ally downed |
|---|---|---|---|---|
| `camp_ground_press_t1_rags_ally` | **0.71** | **4** | 2-5 | 0.02 |
| `camp_ground_press_t1_spear_ally` | **0.68** | **4** | 2-6 | 0.62 |

Ordered against the shipped Floodplains stop cell `rags_scouting_party_t1_solo`
(same 0.55-0.95 / 3-12 window). Both harness cells field ONE ally; the shipped
encounter fields TWO, so real play sits above these readings.

Tuning applied off the plan's draft stats (levers in the plan's stated order):
`plains_scavenger_lead` con 18->14, str 13->11, weapon_die 5->4;
`plains_scavenger_a`/`_b` con 12->10; `goblin_spear_ally` con 14->18.
No shipped combatant row was touched; no shipped cell moved.

### Census (measured at end of Task 6, per the plan)

`python3 scripts/comment_census.py --check` rc=0.
DATA 169,081 `_comment` chars / 1,132,065 total = **14.9%** (limit 15.0%).
Against the plan's stated baseline (166,676 / 1,113,728) this lane adds
**2,405 `_comment` chars** — exactly the plan's projection — over 15,932 new
non-comment DATA chars, i.e. budget 112 + 0.1765 x 15,932 = **2,924**. Under.
Stage 2's `rags_inn.json` variant adds no `_comment`; re-check anyway.

### Decisions logged (surface at close)

- **`wandering_inn_game/docs/design/scene-dynamism-report.md` REVERTED, not committed.**
  Running `tools/scene_dynamism.gd` rewrites the whole ranked table, and adding
  `rags_camp` as its own singleton region group shifted c2/c4 for ~10 unrelated
  scenes. Nothing gates the file (no reference in `scripts/`, `.github/workflows/`,
  or `ci_sweep.sh`), and four lanes each rewriting the same whole table is a
  designed-in four-way conflict (RULING C's spirit). The score is recorded above.
  **The wave-close PR should regenerate it once, after the merge train.**
- `LANDMARK_TOKENS` row inserted after `"floodplains"` and `MAP_REQUIRES` row
  after `"ruin_surface"` — neither is a line a sibling region lane touches, and
  neither is the const's last row (RULING C).

## Stage 2 (QUEST / DIALOGUE / COMBAT-DATA) — implementer

Stage entered with Tasks 1-6 + Task 7 Step 1 already committed (`562541c`).
The only unchecked work inside this stage's scope was Task 7 Steps 2-7; Task 8
(QA canonicals / fixtures / manifest / seed table) is the next stage's and was
NOT started.

- **Task 7 Steps 2-4** (`data/dialogue/rags_inn.json`: ONE `text_variants` arm on
  the `greet` node, keyed `rags_price_kept >= 1`, verbatim from the plan) —
  RULING 4 honoured: it is the node's FIRST AND ONLY variant and the base `text`
  stays unconditional, so the node is never variants-only (`data_lint.py:198-202`).
  Shadow check run as written: `grep -c rags_price_kept` on
  `qa/fixtures/inn_guests_ext_start.json` and `inn_guests_gate_start.json` = **0
  and 0**, so the base greet line that `inn_guests_ext_loop` /
  `inn_guests_gate_proof` pin is still the arm those runs see — no script
  re-derivation owed. Step 4 honoured by omission: `data/maps/inn/inn.json` is
  absent from `git diff --name-only` (the commit touches exactly one data file).
- **Task 7 Steps 5-6 gates**, each read on the full three-part bar
  (nonzero-exit + `^PASS` + zero `SCRIPT ERROR|Parse Error|WARNING`):
  `data_lint.py` OK 107 files / 23 maps; `test_inn_guests` rc=0/PASS/0;
  `test_dialogue` rc=0/PASS/0; `test_copy_fit` rc=0/PASS/0 (2,971 strings);
  `test_content` rc=0/PASS/0 + **0 `CONTENT_FAIL`**; `test_reachability`
  rc=0/PASS/0 (248 consumed counters, 459 producers, **0 orphans** — RULING 7's
  produce-and-gate-together contract holds now that Step 1 landed);
  `test_quests` rc=0/PASS/0.
- **Census re-checked** (the plan predicted the variant adds no `_comment`, and it
  does not): `comment_census.py --check` rc=0, DATA 169,081 `_comment` /
  **1,132,220** total = **14.9%** (limit 15.0%). Total DATA grew by exactly the
  **155** non-comment chars of the new variant; the `_comment` numerator is
  unmoved from the Task 6 measurement, so the lane's `_comment` spend is still
  **2,405** against a ~2,924 budget. The binding measurement is unchanged.
- **Task 7 Step 7** — the commit this file rides in:
  `feat(dialogue): Rags's inn register answers the kept price (#308)`. (This log is
  a TRACKED file staged into the very commit it describes, so it records the
  subject, not a self-referential hash — same convention Stage 1 used for
  Task 7 Step 1. The hashes are returned to the controller by each stage.)

### Handoff to Stage 3 (QA canonicals — Task 8)

- Tree is committed and clean; every unit suite this lane can turn green IS green.
  The next red is by design: nothing yet exercises the four routes end-to-end.
- Task 8 Step 3's `COMBAT_BAND_FIXTURES` row must pin the **tuned** build recorded
  in the Stage 1 balance table above (lead `con` 14 / `str` 11 / `weapon_die` 4;
  scavengers `con` 10; `goblin_spear_ally` `con` 18) — the harness cells were
  measured at exactly that build and the row fails in both directions.
- `qa/manifest.json` anchor is the `rags_gate_check` entry; `AGENTS.md`'s seed
  table anchor is the `rags_gate_check` row. Both are RULING C anchors, not EOF.
- `test_dialogue.gd`'s new arm (Step 8) must prefix every local `f_` — a bare
  `var ctx` collides at PARSE time with a sibling lane's append, which reds the
  whole suite on the second merge.

## Stage 3 (QA CANONICALS / RE-GATE) — implementer

Stage entered at Task 8 (Tasks 1-7 committed: `562541c`, `b3bb220`).

- **Task 8 Steps 1-2** (four fixtures, all copied from `rags_gates_met_start`,
  `"version": 7`) — `rng_state` DERIVED via `tests/_derive_rng_state.gd` for
  seed 9 = `-6393592048753323481` (magnitude 6.4e18, far above the 1e6
  hand-typed tripwire). **DERIVE-THEN-RUN-UNTIL-WIN closed on the FIRST seed
  for all three combats**: `floodplains_price_fight_start` won
  `camp_ground_press` (`victories` 0 -> 1, `camp_ground_held` banked),
  `floodplains_price_betrayal_start` won `rags_scouting_party`
  (`drove_off_rags` + `rags_meeting_settled` both banked), and the HELP
  fixture's `corusdeer_range` cull won too. Each observed outcome is recorded
  in that fixture's own `_comment` per the plan (qa/fixtures is census-exempt).
- **Task 8 Step 3** — `COMBAT_BAND_FIXTURES["floodplains_price_fight_start"] = 2`.
  DECISION: inserted at the **head** of the dict rather than the tail; three
  sibling lanes append to the same const and a tail append is a designed-in
  four-way conflict (RULING C's spirit). Logged, not a plan deviation of substance.
- **Task 8 Steps 4-7** (four canonicals). Every cursor index derived from a REAL
  run, never the authored order: Krshia's settled hub renders **6** visible rows
  (the [Charming Smile] quiet-run row is visible-LOCKED at 5, the 8-gold
  alternate at 6); Rags's settled hub renders **2** (exit, then the F1 offer);
  the `winter` node renders **3** on every route (help / the route's own rung /
  leave). Gold ledger proved 12 -> 4. DECISION: **no `options`-array pin** in any
  of the four — four region lanes append rows to Krshia's and Rags's hubs this
  wave and a pinned list reds all of them at the train (the #172 retirement-node
  precedent). The exclusion proofs are payload-filtered/count-shaped instead.
- **Roster proof (Relc guard, D3)** — exact `combat.order` equals-pin derived
  from the run: `["rags_ally", "pc", "plains_scavenger_b",
  "plains_scavenger_lead", "goblin_spear_ally", "plains_scavenger_a"]`, plus
  both `side == "player"` asserts and the `contains` arm. `accomplishments.victories`
  (never a bare `victories`) pinned at 1.
- **Rack presence pair** — `ui_entities_rendered` `sprites` 8 on arrival
  (absent) vs 9 after the yoke (present), with a payload-filtered
  `assert_event_absent {"sprites": 9}` placed BEFORE the yoke as the ordering
  anchor. `ui_map_rendered` for `rags_camp` pinned at floor_cells 108 /
  blocked_cells 42, both read off the run.
- **Task 8 Step 8** — `test_dialogue.gd::test_rags_winter_offer_hides_for_a_betrayer`,
  every local `f_`-prefixed (`f_graph`, `f_offer`, `f_texts`, `f_ctx`, `f_hub`,
  `f_opts`, `f_ctx_betrayed`, `f_hub_betrayed`, `f_opts_betrayed`). Call inserted
  after `test_grimalkin_studies_gates_on_the_real_graph()`, not at the list end.
- **Task 8 Steps 9-10** — manifest: four entries spliced AFTER the
  `rags_gate_check` entry with a full placement proof (parse, 167 -> 171
  scripts, `rags_gate_check` at idx 151 and the four new at 152..155,
  byte-identity outside the splice region). AGENTS.md: four rows after the
  `rags_gate_check` row.
- **Task 8 Step 11** — PLAN/REPO CONTRADICTION, resolved in the repo's favour:
  `derive_qa_surfaces.py` has **no `--write` flag** (a bare call writes;
  `--check` diffs). Ran bare (`wrote surfaces for 171 scripts`) then `--check`
  (OK). `render_qa_notes.py --write` then bare = `PASS: QA notes match manifest`.
- **`check_doc_drift.py` is RED and this lane cannot make it green.** It fails
  with `plan lacks DONE/ACTIVE header` for all four v0.16 lane plan docs, a
  PRE-EXISTING condition proved at HEAD (`git stash` probe, same four lines).
  This lane added `> Status: **ACTIVE**` to its OWN plan doc, clearing one of
  the four; the remaining three (`invrisil`, `pallass`, `riverfarm`) are sibling
  lanes' owned files and off-limits here. **The gate goes green on the third
  sibling merge, or the wave-close PR owns the last one.**

### Task 9 — re-gate + machine playtest (all gates read as their own step)

| gate | verdict |
|---|---|
| `data_lint.py` | rc=0, OK 107 files / 23 maps |
| `comment_census.py --check` | rc=0, DATA 169,081 / 1,132,220 = **14.9%** (limit 15.0%); lane spend **2,405** vs ~2,924 budget — UNMOVED from Stage 2 (every Task 8 addition lives in `qa/` + `tests/`, both census-exempt for DATA) |
| `sync_agent_guidance.py` | rc=0, 15 canonical skills, Claude mirror exact |
| `render_qa_notes.py --write` then bare | rc=0, `PASS: QA notes match manifest` |
| `derive_qa_surfaces.py` (bare) then `--check` | wrote 171 scripts; `--check OK` |
| `check_doc_drift.py` | **rc=1 — PRE-EXISTING, see the Task 8 note above. Not this lane's to fix.** |
| `run_qa.sh load_gate headless` | rc=0, `QA_RESULT: PASS`, real result.json |
| every `tests/test_*.gd` (30 suites, 240s alarm) | **30/30 GREEN on all three checks** (rc=0 + `^PASS` + zero `SCRIPT ERROR\|Parse Error\|WARNING`); `test_content` additionally 0 `CONTENT_FAIL` |
| `tests/sim_combat_batch.gd` (600s alarm, unsharded) | rc=0, zero noise, **zero FAIL over 136 cells x 100 runs**. `camp_ground_press_t1_rags_ally` **0.71 / median 4 (2-5)**; `camp_ground_press_t1_spear_ally` **0.68 / median 4 (2-6)** — identical to the Stage 1 tuning readings, and no shipped cell moved |
| full `qa/ci_sweep.sh` (WI_SWEEP_JOBS=8, polled) | **`ci_sweep: ALL 171 script(s) green, no grep hits.`** |
| `--touching` re-gate, all five shared surfaces | quests 40 / floodplains+rags_camp 62 / the three dialogue graphs 17 / street 52 / combatants 110 — **0 nonzero, 0 missing** in every set. Named must-be-green scripts all rc=0: `rags_meeting_loop`, `rags_gate_check`, `inn_guests_ext_loop`, `inn_guests_gate_proof`, `stages_loop`, `crate_light`, `crate_talk`, `wrong_order_talk`, `pallass_walkthrough`, `door_chain_fight`, `floodplains_bestiary_loop`, `parley_gates_check`, `parley_talkdowns_loop`, `beast_tamer_loop`, `goblin_night_patrol_loop`, `regional_work_loop` |
| headless smoke boot (`--quit`) | rc=0, zero noise |

**Step 7/8 METHOD DECISION.** The five `--touching` sets union to ~150 of the
171 canonicals, so running them separately AND then a full sweep would be ~4x
redundant. The full sweep was run ONCE (it runs every script in all five sets)
and each set's verdict was read back from the sweep's own per-script `.rc`
files. Strictly stronger evidence than the subset runs, at a quarter the cost.

**Step 9 — windowed machine playtest, eleven shots, read by eye.** Three
canonicals run windowed on the real asset overlay (654 PNGs present in this
worktree; no `fetch_private_assets.sh` needed) plus FOUR throwaway shot scripts
for the phase ladder, the can-fail mouth pair, and a synthetic all-routes
`winter` node. **The throwaways were deleted after capture** (`zz_f1_*` in
qa/scripts + qa/fixtures) and `test_fixture_coherence` re-run clean afterwards
(124/124). Findings drained to `docs/VISUAL-LOG.md`: the three scavengers DO
read as one mass (D9 confirmed by eye — the exact question the Stage 1 row left
open), the camp mouth reads as ordinary scenery rather than an entrance, two
props argue with their own copy (`request_board` for hide racks, `barrel` for
the drying rack), the mood row TOOK at all three phases but the fire casts no
light, and both new hubs now sit at 5-6 visible rows. **None is a blocker.**

**Step 10** — CHOICE-LOG entry prepended above the `v0.15 WAVE CLOSE` heading
(newest-first). NOTE for the merge train: four lanes prepending there will
conflict; resolution is trivially "keep both blocks".

**Step 11 (PR) NOT DONE** — the implementer is forbidden to push or run `gh`.
