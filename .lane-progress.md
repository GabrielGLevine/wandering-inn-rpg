# Lane progress — v0.16 Invrisil (#306), branch `issue/306-invrisil-depth`

Append one line per completed task: task id, commit hash, gates run + verdicts.

- preflight (stage 1) — no commit — `godot --headless --import` DONE (734 steps, rc n/a);
  smoke `--quit` rc=0, zero `SCRIPT ERROR|Parse Error|WARNING` hits.
- Task 1.1 (boulevard_stationer prop -> door) — commit PENDING — gates: `data_lint.py` rc=0 "OK -- 106 files, 22 maps clean".
  Pin proof: invrisil_walkthrough has NO `press interact` at (20,2) (all 14 interacts enumerated, none at that cell);
  (20,1) still in `blocked` (row 1 fully blocked, 28/28); no `ui_map_rendered`/`ui_entities_rendered` count asserts on any boulevard script.
- Task 1.2 (boulevard_to_adventurers_rest door at (18,1), decor row deleted) — commit PENDING — gates: `data_lint.py` rc=0 clean.
  Cell proof: (18,1) already in `blocked` (row 1 = 28/28 blocked); row 2 carries ZERO blocked cells; (18,2) is pinned by NO script
  (all 9 boulevard-touching scripts enumerated: pinned row-2 cells are only (1,2),(14,2),(20,2),(23,2),(27,2)).
- Task 1.3 (data/maps/invrisil/stationer.json, 12x9, biome brothers_parlor) — commit PENDING — gates:
  * sprite one-liner over all 15 ids -> `[]` (every sprite registered).
  * wall/blocked audit: 37 wall cells, `blocked` == [[4,5]] only, wall∩blocked == {} , decor∩entities == {},
    every blocked cell has a decor row, arrival (6,7) free, door gap (6,8) not on a wall segment, all cells in grid.
  * `data_lint.py` rc=0 "OK -- 107 files, 23 maps clean".
  * `tests/test_content.gd` rc=1, zero noise-grep hits, ONE CONTENT_FAIL:
    `entity stationer_client conversation has no graph: invrisil_stationer_client`.
    DECISION (logged, not a blocker): this is the plan's own acknowledged ladder red — the graph is Task 2.2 (stage 2).
    Plan Task 2.1 Step 4 states the ladder "will red until Tasks 2.2-2.5 land, which is expected".
    Stage 1 CANNOT clear it without starting a dialogue task, which the stage goal forbids.
  * `tools/scene_dynamism.gd`: `stationer` composite 67.77 (c1 21.25 / c2 23.50 / c3 23.02), verdict `dynamic`, target >=50 MET.
    CAVEAT: the tool NOTEs `stationer` has no REGION_GROUPS entry and defaults to its own singleton group, which grants a
    free 0.85 in-region credit (tools/scene_dynamism.gd:665-672 calls this out as a trap). `tools/**` is NOT in this lane's
    FILE OWNERSHIP, so the row was deliberately NOT added; flagged for the close PR / controller.
  * regenerated `wandering_inn_game/docs/design/scene-dynamism-report.md` committed with the map (c4 utilization renormalises
    across all maps, so several shipped rows move by <1 point — deterministic, not a content change).
- Task 1.4 (data/maps/invrisil/adventurers_rest.json, 13x9, biome inn) — commit PENDING — gates:
  * sprite one-liner over all 17 ids -> `[]`.
  * wall/blocked audit: 39 wall cells, `blocked` == [[8,1]] only, wall∩blocked == {}, decor∩entities == {},
    every blocked cell has a decor row, arrival (6,7) free, (6,8) door gap off the wall, all cells in grid.
  * present_when shape: only rest_hat_hook + rest_corner_table carry it (requires-wrapped); rest_bravos uses encounter_when. OK.
  * fixture-cell sanity: (2,7) free facing (1,7)=rest_hat_hook; (9,5) free facing (9,6)=rest_bravos.
  * POPULATION_FLOORS pre-count by the test's own rule: stationer 11, adventurers_rest 10 — matches the plan exactly.
  * `data_lint.py` rc=0 "OK -- 108 files, 24 maps clean".
  * `tests/test_content.gd` rc=1, zero noise, FIVE CONTENT_FAILs, ALL forward references to later stages:
    rest_house_factor -> invrisil_rest_factor graph (Task 3.3); stationer_client -> invrisil_stationer_client (Task 2.2);
    rest_bravos/rest_hat_hook/rest_corner_table -> unproduced `hat_job_taken` (producer is Task 3.2, invrisil_wilovan.json).
  * `tools/scene_dynamism.gd`: adventurers_rest 70.56 (c1 24.22 / c2 23.50 / c3 22.84) `dynamic`; stationer 67.77. Both >= 50.
    Same singleton-region-group caveat as Task 1.3 (tools/** not owned by this lane).
  * NOTE for later stages: `rest_bravos.enemies` names `rest_bravo_a`/`rest_bravo_b`, which land in Task 3.4 (combatants.json).
- Task 1.5 (moods + LANDMARK_TOKENS + MAP_REQUIRES + POPULATION_FLOORS) — commit PENDING — gates:
  * `data_lint.py` rc=0 "OK -- 108 files, 24 maps clean".
  * `tests/test_fixture_coherence.gd` rc=0, `PASS: fixture coherence — 120/120 fixtures are reachable story positions`, zero noise.
  * `tests/test_content.gd` rc=1, zero noise, SAME five forward-reference CONTENT_FAILs as Task 1.4 and NO new one —
    i.e. the counted floors (stationer 11 / adventurers_rest 10) and both LANDMARK_TOKENS rows are accepted.
  ANCHOR NOTE (plan self-inconsistency, resolved): FILE OWNERSHIP says POPULATION_FLOORS inserts after `"mercantile_alleys"`,
  Task 1.5 Step 4 says after `"invrisil_boulevard"`. The task-level instruction was followed (after `"invrisil_boulevard"`).
  Both rows are Invrisil-exclusive, so ruling C holds either way. LANDMARK_TOKENS + MAP_REQUIRES used the `"mercantile_alleys"` anchor.
