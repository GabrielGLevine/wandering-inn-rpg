# v0.16 Pallass lane (#307) — running progress log

Append one line per task completed: task id, commit, gates + verdicts.

## Stage 1 (maps / interiors / doors)

- preflight: `godot --headless --import` rc=0; smoke `--quit` rc=0, zero noise.
- T0.1 profiles filled in place | commit d7666a9 | `git diff -U0` hunk @@ -516,8 +516,36 @@ only, nothing at/after the Hedault stub (:553) -> PASS
- T0.2 baseline | no commit | census `--check` rc=0 DATA chars=1113728 _comment=166676 ratio=15.0%; `data_lint.py` rc=0 (106 files/22 maps); `test_content.gd` rc=0 + ^PASS + 0 noise (control: tree was already green)
- T1.1 pallass_forge_hall interior + mood/LANDMARK_TOKENS/MAP_REQUIRES | commit edfa5c2 | data_lint rc=0 (107/23); test_content rc=0 +^PASS +0 noise; test_world_visuals rc=0 +^PASS +0 noise; scene_dynamism composite 62.46 "dynamic"; census rc=0 DATA 14.9% (+125 _comment / +5720 non-comment vs the 112+0.1765x budget)
- T1.2 forge_hall_door (15,3) + forge_reject_bin (10,4) | commit 43a2156 | splice proof: entities 15->17, all prior siblings + all other keys byte-identical, git diff 30 insertions / 0 deletions; data_lint rc=0; test_content rc=0 +^PASS +0 noise; ci_sweep over the six `--touching data/maps/pallass/pallass_forge.json` scripts (pallass_peek, pallass_round_trip, pallass_walkthrough, parley_gates_check, parley_talkdowns_loop, regional_work_loop) all green, run as three foreground --only batches because a 6-script sweep exceeds one foreground Bash budget
  - DECISION (plan gap, logged): `qa/scripts/pallass_peek.json:142` pins `ui_entities_rendered {sprites: 16}` for pallass_forge. Two UNCONDITIONAL entities move it to 18, so the pin was re-derived in this commit with a recorded rationale (the file's own v0.15 T4.1 precedent). The plan's crowded-cell audit covered walked cells and assert sets but not this COUNT pin. pallass_peek is Pallass-owned; no sibling lane touches it. `blocked_cells: 85` is unchanged (entities block in the sim, not in that payload). NEXT: Task 2.2 adds two entities to pallass_market -> `pallass_peek.json:11` must go 25 -> 27 the same way.
- CORRECTION: T1.2's real commit hash is 7672fdb (43a2156 was superseded by an --amend that fixed this log).
- T2.1 pallass_den_shop interior (incl. the two P2 quest props, per the task's own one-pass instruction) + mood/LANDMARK_TOKENS/MAP_REQUIRES | commit <this> | data_lint rc=0 (108/24); test_content rc=0 +^PASS +0 noise; test_world_visuals rc=0 +^PASS +0 noise; test_audio_data rc=0 +^PASS +0 noise; scene_dynamism composite 66.23 "dynamic"; census rc=0 DATA 14.8%
  - DECISION (plan/repo contradiction, resolved in the repo's favour and logged): the plan's `den_shop_keeper` draft carries `"conversation": "pallass_den_keeper"`, but that graph is created in Task 2.3 (a DIALOGUE task, out of this stage's scope) and `tests/test_content.gd:933` reds any entity naming a graph that does not exist. The key is OMITTED here; `talk_pool` alone satisfies `_validate_npc_interact_surface`. A binding STAGE-1 HANDOFF block + a new Step 0 were added to Task 2.3 in the plan doc requiring the key be restored in the same commit that creates the graph. Without it both P2 terminal rungs are unreachable and no gate says so.
  - DECISION (logged): `crystal_lamp` at (2,1) takes a WARM light [1.0,0.84,0.62]. The never-amber crystal-lamp quarantine in `moods.json` is scoped to `pallass_market`'s own mood row; the den shop is the deliberate warm-timber counterweight on the `inn` biome, which is the whole reason the room exists.
- T2.1 commit hash: 7cecf74
- T2.2 den_shop_door (12,4) + market_dray_rank (16,4) on pallass_market; lift_manifest_slate (23,4) + lift_cargo_pallet (19,4) on pallass_forge | commit <this> | splice proofs: market entities 24->26, forge 17->19, all prior siblings + all other keys byte-identical, git diff 69 insertions / 0 deletions; data_lint rc=0 (108/24); test_content rc=0 +^PASS +0 noise; test_world_visuals rc=0 +^PASS +0 noise; all 12 `--touching` canonicals for the two maps green (grimalkin_study_loop, mixer_alchemist_loop, pallass_peek, pallass_race_peek, pallass_round_trip, pallass_walkthrough, pallass_watchgolem_loop, parley_gates_check, parley_talkdowns_loop, regional_work_loop, spine_reach, trader_earn_loop), run as three foreground --only batches; census rc=0 DATA 14.8%
  - pallass_peek pins re-derived again in this commit: market 25 -> 27 (line 11), forge 18 -> 20 (line 142). POPULATION_FLOORS unedited: market 24 -> 26 and forge 15 -> 19, all four additions unconditional, so both floors only rise.
- T2.2 commit hash: b6b59c0
- STAGE-1 CLOSE gates (whole tree, all foreground + alarm-wrapped, three-part verdict each): test_fixture_coherence / test_reachability / test_copy_fit / test_shipped_ids / test_sprite_registry / test_portals / test_dialogue / test_quests -- every one rc=0 + ^PASS + 0 noise. load_gate headless rc=0, QA_RESULT PASS, 0 noise. Headless smoke --quit rc=0, 0 noise. derive_qa_surfaces --check rc=0 (167 scripts). render_qa_notes (bare check) rc=0 "PASS: QA notes match manifest" -- no manifest change this stage, so no --write was needed.
- OPEN / WAVE-LEVEL: `python3 scripts/check_doc_drift.py` is RED and was ALREADY red before this stage's first commit -- all four v0.16 lane plan docs shipped without a `> Status: **DONE**|**ACTIVE**` header. This lane's own doc now carries `> Status: **ACTIVE**`; the floodplains / invrisil / riverfarm plan docs still lack theirs and are NOT this lane's files. The gate stays red until each lane (or the wave close) adds its own header. Task 7 step 3 lists check_doc_drift as a lane gate -- it cannot go green from inside this lane alone.
- STAGE 1 COMPLETE at a clean task boundary: Tasks 0.1, 0.2, 1.1, 1.2, 2.1, 2.2. NEXT STAGE starts at Task 1.3 (the smith's commission) in the doc's stated order; do NOT re-run 2.1/2.2. Remember Task 2.3 Step 0 (restore the keeper's `conversation` key) and that Task 2.6's two den-shop props are ALREADY authored.
- NOTE: `tools/scene_dynamism.gd` REWRITES `wandering_inn_game/docs/design/scene-dynamism-report.md` as a side effect (whole-table churn: the two new maps shift every region's utilization column). That file is not in this lane's FILE OWNERSHIP and the plan calls the tool advisory-only, so the working-tree change was REVERTED rather than committed. Re-run the tool if the number is wanted; the close PR should decide who regenerates the report once. The tool also NOTEs that `pallass_forge_hall` / `pallass_den_shop` have no region-group entry and defaulted to singleton groups -- a one-line nicety for the close, not a gate.

## Stage 2 (quests / dialogue / combat data)

- T1.3 smith's commission: 2 hub options (appended after the always-available exit, exit row index stays 3) + 5 nodes (`commission`, `commission_brief`, `commission_drift`, `commission_report`, `broker`, `commission_settled`) | commit <this> | data_lint rc=0 (108/24); test_dialogue rc=0 +^PASS +0 noise; test_content rc=0 +^PASS +0 noise; test_copy_fit rc=0 +^PASS +0 noise; test_reachability rc=0 +^PASS +0 noise (250 consumed / 467 producers / 0 orphans); ci_sweep --only pallass_walkthrough rc=0 "ALL 1 green, no grep hits" (3-row hub pin holds); census rc=0 DATA 14.8% (cumulative vs T0.2 baseline: +21377 chars / +1255 _comment; non-comment +20122 -> budget 112+0.1765*20122 = 3663 >= 1255, PASS)
  - Shadow-out audit (plan step 3): the two shipped hub `text_variants` (`forge_golems_culled`, `seal_resolved`) are byte-identical and NO variant was added to `hub` -- the smith's greeting is unchanged in every shipped state. Softlock guard (step 4): `commission_report` keeps `"Still working on it."` ungated, `commission` keeps the decline, `hub` keeps `"What's the bench rated for?"`.
  - Copy-fit page audit: the two >200-char bodies were paginated by hand against `test_copy_fit.gd:683-712` before shipping -- `commission_brief` (234) splits 174/59 and `broker` (218) splits 162/55, both at real sentence boundaries. No orphan page (the `forge_runes` 12-char failure mode).
  - DECISION (plan/repo cycle, resolved in the repo's favour and logged): Tasks 1.3 / 1.4 / 1.5 are MUTUALLY dependent at the gate level -- 1.3's graph starts quest `tempered_standards` (1.5) and gates on `golem_recalibrated` (1.4); 1.4's `encounter_when` gates on `standards_commission_taken` (1.3); 1.5's quest cross-refs counters 1.3 and 1.4 produce. No ordering of the three is green as authored. Resolution (the stage-1 `den_shop_keeper` precedent): ship 1.3 minus its two forward references and restore each in the commit that makes it legal. DEFERRED-A (restore at 1.4): the `commission_report` option `[Tell her what happened to the calibration rig.]` and the `golem_recalibrated` `text_variant` on `commission_settled` -- both `requires` a counter with no producer until 1.4, which `test_reachability.gd:50` reds. DEFERRED-B (restore at 1.5): the `{ "quest": "tempered_standards" }` effect dict on `"I'll take the commission."` -- `test_content.gd:1122-1124` reds an unknown quest id. Binding Step-0 handoff blocks were added to Tasks 1.4 and 1.5 in the plan doc. Every commit on this branch stays green as a result.
- T1.4 temper golem: combatant row (hand-spliced AT THE ANCHOR, see decision), encounter entity on `pallass_forge_hall`, parley graph `forge_temper_golem.json`, `BESTIARY_CELLS` gated cell, plus the DEFERRED-A restore in `pallass_forge_smith.json` | commit <this> | data_lint rc=0 (109/24); test_combat_data rc=0 +^PASS +0 noise; test_combat_visuals rc=0 +^PASS +0 noise (passes BY EXCLUSION, ruling F -- `FIGURE_ROWS` has 4 entries, `audited` names no id from this lane); test_content / test_dialogue / test_reachability / test_copy_fit / test_world_visuals all rc=0 +^PASS +0 noise; FULL sim_combat_batch rc=0 +^PASS +0 noise over 135 cells; census rc=0 DATA 14.8%
  - BAND TABLE (all at 100 seeded runs; cell indexes are 0-based globals after the 32-cell matrix block): `forge_temper_golem_t5_sw14_solo` (idx 120) win 0.64 median 4 (3-5); shipped `forge_calibration_golem_t5_sw14_solo` (idx 119) win 0.71 median 4 (2-5); shipped `forge_calibration_golem_t4_solo` (idx 118) win 0.49 median 4 (3-5); shipped `seal_warden_t5_sw14_solo` (idx 112) win 0.59 median 5 (3-7). The new rig reads STRICTLY BELOW the shipped forge golem at the same `t4_spellsword14_party` build (0.64 < 0.71) -- that ordering is the band claim; the gate itself is the standing 0.55-0.95 / rounds 3-12 window (ruling A). No shipped combatant's stats moved.
  - TUNING (plan step 5, authorized by the step's own instruction): the drafted `con: 54` measured 0.59 -- inside the gate but only 0.04 above the 0.55 floor. Lowered to `con: 50` -> 0.64, which sits 0.09 above the floor and still 0.07 below the shipped golem. Both measurements recorded above; nothing else on the row moved.
  - DECISION (plan-internal contradiction, resolved and logged): the plan tells this task to splice the combatant with `splice_json.py --container combatants`, but ruling C binds the row to the anchor "immediately after the shipped `forge_golem` row" and `splice_json.py` can ONLY append at the END of a container (`scripts/splice_json.py:151-160` splices before the closing bracket; there is no --after). `forge_golem` is index 47 of 56, so the tool would have violated the anchor -- which is the whole four-lane conflict-avoidance mechanism. The row was hand-spliced at the anchor matching the file's 1-space indent (explicitly permitted: "Hand-edit matching the file's own indent, or use scripts/splice_json.py") and the tool's three proofs were reproduced by hand: parse OK; siblings 56 -> 57; `forge_temper_golem` is the DIRECT successor of `forge_golem`; byte-identity outside the splice proved by a prefix/suffix scan against `HEAD` (unmatched middle in the ORIGINAL = 0 chars, 686 inserted) and `git diff --numstat` = 24 insertions / 0 deletions. The encounter entity DID go through `splice_json.py` (new lane-exclusive file, no anchor): entities 7 -> 8, prior siblings and all other top-level keys byte-identical.
  - DECISION (plan step 3's grep is self-defeating, logged): the step says `grep -n trigger_radius pallass_forge_hall.json` "must return NOTHING", but the plan's OWN drafted `_comment` for the encounter contains the word `trigger_radius` in its warning prose, so a literal grep can never be empty. The real invariant was proven STRUCTURALLY instead: no entity in the map carries a `trigger_radius` KEY (JSON walk returned []), and the single literal hit is line 266, inside that `_comment`.
  - NOT RUN HERE, owed at Task 3.3: `ci_sweep.sh --touching data/combatants.json`. No shipped combatant row's stats moved (pure +1 append) and the full 135-cell harness is green, so no canonical's fight can have flipped; the crossing re-gate is Task 3.3's named job.
