# Lane progress — v0.16 Invrisil (#306), branch `issue/306-invrisil-depth`

Append one line per completed task: task id, commit hash, gates run + verdicts.

- preflight (stage 1) — no commit — `godot --headless --import` DONE (734 steps, rc n/a);
  smoke `--quit` rc=0, zero `SCRIPT ERROR|Parse Error|WARNING` hits.
- Task 1.1 (boulevard_stationer prop -> door) — commit PENDING — gates: `data_lint.py` rc=0 "OK -- 106 files, 22 maps clean".
  Pin proof: invrisil_walkthrough has NO `press interact` at (20,2) (all 14 interacts enumerated, none at that cell);
  (20,1) still in `blocked` (row 1 fully blocked, 28/28); no `ui_map_rendered`/`ui_entities_rendered` count asserts on any boulevard script.
- Task 1.2 (boulevard_to_adventurers_rest door at (18,1), decor row deleted) — commit PENDING — gates: `data_lint.py` rc=0 clean.
  Cell proof: (18,1) already in `blocked` (row 1 = 28/28 blocked); row 2 carries ZERO blocked cells; (18,2) is pinned by NO script
  (all 9 boulevard-touching scripts enumerated: pinned row-2 cells are only (1,2),(14,2),(20,2),(23,2),(27,2)).
- Task 1.3 (data/maps/invrisil/stationer.json, 12x9, biome brothers_parlor) — commit PENDING — gates:
  * sprite one-liner over all 15 ids -> `[]` (every sprite registered).
  * wall/blocked audit: 37 wall cells, `blocked` == [[4,5]] only, wall∩blocked == {} , decor∩entities == {},
    every blocked cell has a decor row, arrival (6,7) free, door gap (6,8) not on a wall segment, all cells in grid.
  * `data_lint.py` rc=0 "OK -- 107 files, 23 maps clean".
  * `tests/test_content.gd` rc=1, zero noise-grep hits, ONE CONTENT_FAIL:
    `entity stationer_client conversation has no graph: invrisil_stationer_client`.
    DECISION (logged, not a blocker): this is the plan's own acknowledged ladder red — the graph is Task 2.2 (stage 2).
    Plan Task 2.1 Step 4 states the ladder "will red until Tasks 2.2-2.5 land, which is expected".
    Stage 1 CANNOT clear it without starting a dialogue task, which the stage goal forbids.
  * `tools/scene_dynamism.gd`: `stationer` composite 67.77 (c1 21.25 / c2 23.50 / c3 23.02), verdict `dynamic`, target >=50 MET.
    CAVEAT: the tool NOTEs `stationer` has no REGION_GROUPS entry and defaults to its own singleton group, which grants a
    free 0.85 in-region credit (tools/scene_dynamism.gd:665-672 calls this out as a trap). `tools/**` is NOT in this lane's
    FILE OWNERSHIP, so the row was deliberately NOT added; flagged for the close PR / controller.
  * regenerated `wandering_inn_game/docs/design/scene-dynamism-report.md` committed with the map (c4 utilization renormalises
    across all maps, so several shipped rows move by <1 point — deterministic, not a content change).
- Task 1.4 (data/maps/invrisil/adventurers_rest.json, 13x9, biome inn) — commit PENDING — gates:
  * sprite one-liner over all 17 ids -> `[]`.
  * wall/blocked audit: 39 wall cells, `blocked` == [[8,1]] only, wall∩blocked == {}, decor∩entities == {},
    every blocked cell has a decor row, arrival (6,7) free, (6,8) door gap off the wall, all cells in grid.
  * present_when shape: only rest_hat_hook + rest_corner_table carry it (requires-wrapped); rest_bravos uses encounter_when. OK.
  * fixture-cell sanity: (2,7) free facing (1,7)=rest_hat_hook; (9,5) free facing (9,6)=rest_bravos.
  * POPULATION_FLOORS pre-count by the test's own rule: stationer 11, adventurers_rest 10 — matches the plan exactly.
  * `data_lint.py` rc=0 "OK -- 108 files, 24 maps clean".
  * `tests/test_content.gd` rc=1, zero noise, FIVE CONTENT_FAILs, ALL forward references to later stages:
    rest_house_factor -> invrisil_rest_factor graph (Task 3.3); stationer_client -> invrisil_stationer_client (Task 2.2);
    rest_bravos/rest_hat_hook/rest_corner_table -> unproduced `hat_job_taken` (producer is Task 3.2, invrisil_wilovan.json).
  * `tools/scene_dynamism.gd`: adventurers_rest 70.56 (c1 24.22 / c2 23.50 / c3 22.84) `dynamic`; stationer 67.77. Both >= 50.
    Same singleton-region-group caveat as Task 1.3 (tools/** not owned by this lane).
  * NOTE for later stages: `rest_bravos.enemies` names `rest_bravo_a`/`rest_bravo_b`, which land in Task 3.4 (combatants.json).
- Task 1.5 (moods + LANDMARK_TOKENS + MAP_REQUIRES + POPULATION_FLOORS) — commit PENDING — gates:
  * `data_lint.py` rc=0 "OK -- 108 files, 24 maps clean".
  * `tests/test_fixture_coherence.gd` rc=0, `PASS: fixture coherence — 120/120 fixtures are reachable story positions`, zero noise.
  * `tests/test_content.gd` rc=1, zero noise, SAME five forward-reference CONTENT_FAILs as Task 1.4 and NO new one —
    i.e. the counted floors (stationer 11 / adventurers_rest 10) and both LANDMARK_TOKENS rows are accepted.
  ANCHOR NOTE (plan self-inconsistency, resolved): FILE OWNERSHIP says POPULATION_FLOORS inserts after `"mercantile_alleys"`,
  Task 1.5 Step 4 says after `"invrisil_boulevard"`. The task-level instruction was followed (after `"invrisil_boulevard"`).
  Both rows are Invrisil-exclusive, so ruling C holds either way. LANDMARK_TOKENS + MAP_REQUIRES used the `"mercantile_alleys"` anchor.

## STAGE 1 CLOSE (maps / interiors / doors) — Phase 1 complete, Tasks 1.1-1.5

Stage-close gates (all foreground, alarm-wrapped, verdict read as its own step):
- headless smoke `--quit`: rc=0, zero noise.
- `python3 scripts/comment_census.py --check`: rc=0. `DATA chars=1132000 _comment_chars=168327 ratio=14.9% target<=15.0%`.
  Stage-1 lane delta measured with the plan's Task 6.1 Step 3 snippet: **1,651 new `_comment` chars** under `data/**`
  (adventurers_rest 929, invrisil_boulevard +227 net, stationer 330, moods 165). Budget is `112 + 0.1765 x new_noncomment`.
- Every `tests/test_*.gd` under the three-part bar (rc 0 + `^PASS` + zero `SCRIPT ERROR|Parse Error|WARNING`):
  27 of 30 GREEN. THREE RED, and every red is a forward reference to a stage-2/3 task — nothing else:
  * `test_content`  rc=1 noise=0 — 5 CONTENT_FAILs: stationer_client -> `invrisil_stationer_client` graph (Task 2.2);
    rest_house_factor -> `invrisil_rest_factor` graph (Task 3.3); rest_bravos/rest_hat_hook/rest_corner_table ->
    unproduced `hat_job_taken` (producer Task 3.2).
  * `test_combat_data` rc=1 noise=1 — `Assertion failed: encounter rest_bravos references unknown enemy rest_bravo_a`
    (combatant rows land in Task 3.4). NOTE: this assert HANGS the run to its watchdog — always alarm-wrap.
  * `test_reachability` rc=1 noise=1 — `zero-producer accomplishment gate(s): hat_job_taken` (same producer, Task 3.2).
    `test_reachability` is NOT in the plan's gate list but IS a real gate — stage 2/3 must re-run it after Task 3.2.
  All three clear when Tasks 2.2, 3.2, 3.3 and 3.4 land. The plan sanctions this ladder red at Task 2.1 Step 4.

## STAGE 2 (quests / dialogue / combat data) — Phase 2 complete, Tasks 2.1-2.6

- Task 2.1 (quests.json `a_setting_for_a_lady`) — commit a87181a — gates: `data_lint.py` rc=0 "OK -- 108 files";
  `test_quests.gd` rc=0 + ^PASS + zero noise; `test_content.gd` rc=1, zero noise, only forward-reference CONTENT_FAILs.
  Placement: splice_json.py appended, then a line-move to the ruling-C anchor with a PARSED-VALUE equality proof
  (order now price_of_a_favor / a_gentlemans_disagreement / a_setting_for_a_lady / what_the_seal_kept), then a reflow
  to the file's shipped compact-beat style, also equality-proved. Travel-beat arithmetic re-checked by hand:
  LANDMARK_TOKENS["mercantile_alleys"] carries "alleys" (test_content.gd:1362) and the resolve description says
  "in the alleys". CONTROLLER AMENDMENT A1 applied: report beat reads "Go back to the Lady with the ring box".
- Task 2.2 (data/dialogue/invrisil_stationer_client.json) — commit 3f5594d — gates: `data_lint.py` rc=0 (109 files);
  `test_dialogue.gd` rc=0 + ^PASS + zero noise; `test_content.gd` rc=1 zero noise (stationer_client graph red CLEARED,
  setting_commissioned now produced; new forward ref = plum_silk_locket, Task 2.4); `test_copy_fit.gd` rc=0 + ^PASS.
  AMENDMENT A1 applied at authoring: speaker "A Lady with a Ring Box" on all five nodes; `terms` carries the
  two-assets line verbatim; `the_stone` carries the Reinhart-adjacent notice line verbatim (house never named).
  A4 bar proved: zero `{addr}` / `{Addr}` tokens in her copy; zero em-dashes (literal, — escape and ASCII `--`).
- Task 2.3 (hedault_enchanting.json: hub option 7 + 4 nodes) — commit 9651c00 — gates: `data_lint.py` rc=0;
  `test_dialogue.gd` rc=0 + ^PASS + zero noise; `test_content.gd` rc=1 zero noise (heirloom_truth_kept and
  setting_assisted now produced). PIN-STABILITY PROVEN BY RUN, not assumed: `hedault_fragment_loop` seed 7 PASS
  (50 events, zero failures) and `spine_reach` seed 9 PASS (245 events) with ZERO script edits.
  Skill ids verified live in data/skills.json: appraise_goods, detect_magic both present.
  Dash audit on the new region: zero em-dashes in player copy; the only `--` are inside `_comment` keys, which is
  this file's own shipped idiom (:2, :94).
- Task 2.4 (items.json `plum_silk_locket`) — commit 3a1a538 — gates: `data_lint.py` rc=0; `test_items.gd` rc=0 +
  ^PASS + zero noise; `test_content.gd` rc=1 zero noise (the unknown-item red CLEARED). splice_json.py + line-move
  to the `hedaults_wardstone` anchor with a parsed-value proof. Zero `{addr}` in items.json.
- Task 2.5 (fence: 2 combatants + alley_fence_door + harness cell) — commit 8119d02 — gates:
  * MEASURE-FIRST (plan Step 4): the DRAFTED stats measured **0.48 win / median 2** — outside the 0.55-0.95 gate AND
    the 3-12 rounds band. Tuned ONLY the two new ids, five measured iterations, final:
    heirloom_fence str 12->6, con 26->40, weapon_die 5->3; fence_doorman str 14->10, con 30->48, weapon_die 6->4.
    Final measured **win 0.67, median 3** (min 2, max 6) — inside the gate with 0.12/0.28 margins and inside the
    plan's 0.62-0.70 design target. The measured pair is recorded in the cell's own comment.
    DECISION (logged): the plan's tuning ladder assumed an overshoot (+weapon_die, +con); the cell landed LOW and
    SHORT, so the reverse direction was taken — bulk up, damage down — which is the only lever that raises win rate
    and median rounds together (rounds are bounded by enemy effective HP vs PC round damage).
  * Full INVRISIL range (WI_CELL_RANGE=90:106) rc=0, zero noise, ALL FOUR shipped gated cells unchanged:
    hired_blades_t3_warrior10_wilovan 0.64 / median 4 (window .57-.71); hired_blades_t5_sw14_wilovan 0.82 / 4
    (.77-.87); alley_footpads_w2_solo 0.95 / 2 (.75-.98); boulevard_duel_ring_t3_solo 0.92 / 2 (.55-.95).
  * `test_combat_visuals.gd` rc=0 + ^PASS + zero noise (passes by EXCLUSION per ruling F).
  * `data_lint.py` rc=0; `test_content.gd` rc=1 zero noise — every I1 counter now produced.
  * `test_combat_data.gd` STILL RED and still the stage-1 forward reference: `Assertion failed: encounter
    rest_bravos references unknown enemy rest_bravo_a` (rows land in Task 3.4). NOTE: the assert ABORTS the encounter
    loop, so `alley_fence_door`'s own key/arena/enemy validation is NOT proven by this run — it is proven by the
    Task 3.4 re-run, which is the first green one. Always alarm-wrap: this assert hangs to the watchdog.
- Task 2.6 (stationer_client talk_pool_stages) — commit 876b601 — gates: `data_lint.py` rc=0;
  `test_content.gd` rc=1 zero noise (no new red — the stage shape is accepted); `test_copy_fit.gd` rc=0 + ^PASS +
  zero noise, which is the real bar for the two new ambient barks (2-line budget at 656px).

### Phase 3 complete, Tasks 3.1-3.5

- Task 3.1 (quests.json `the_hat_stays_on` + BOTH quests' ladder pins in tests/test_quests.gd) — commit c65a3cb —
  gates: `data_lint.py` rc=0 (109 files); `test_quests.gd` rc=0 + ^PASS + zero noise (the new pins run);
  `test_content.gd` rc=1 zero noise, only forward refs to Tasks 3.2/3.3.
  Placement: splice_json.py + line-move to sit immediately after `a_setting_for_a_lady`, parsed-value proved, then
  reflowed to the shipped compact-beat style (also proved). Landmark arithmetic re-checked live:
  LANDMARK_TOKENS["adventurers_rest"] = ["adventurer","invrisil"] (test_content.gd:1369) and the run beat says
  "the Adventurer's Rest". Every new test_quests local carries the `i_` prefix: i_setting, i_setting_both, i_hat,
  i_hat_both. Pins cover single-counter resolution, the co-bank last-match-wins case, the winner's grant, and both
  beat_index rungs, for BOTH quests.
- Task 3.2 (invrisil_wilovan.json: hub options 7+8, four nodes) — commit 1c4dd16 — gates: `data_lint.py` rc=0;
  `test_dialogue.gd` rc=0 + ^PASS + zero noise; `test_content.gd` rc=1 zero noise (hat_job_taken and hat_job_done
  now produced; only handoff_talked left, Task 3.3); `test_copy_fit.gd` rc=0 + ^PASS (option rows have no autowrap,
  so this is the real bar for the new option text); `test_reachability.gd` rc=1 with exactly one orphan left
  (handoff_talked -> Task 3.3).
  INDEX STABILITY PROVEN BY RUN: invrisil_disagreement_talk / _stealth / _fight and wilovan_address_f, all seed 9,
  all rc=0, `"passed": true`, zero noise, ZERO script edits. The sharp one (`_fight`) banks brothers_job_done at
  step 92 and then navigates steps 100-101 inside the `marker` node, never a re-rendered hub.
  Dash budget held: zero em-dashes and zero ASCII `--` in all four new nodes and both new option rows; one {addr}
  per new node, none in an option row.
- Task 3.3 (data/dialogue/invrisil_rest_factor.json) — commit 42f194b — gates: `data_lint.py` rc=0 (110 files);
  `test_dialogue.gd` rc=0 + ^PASS; **`test_content.gd` rc=0 + ^PASS + zero noise — the ladder red opened at
  Task 1.3 is CLOSED**; `test_reachability.gd` rc=0 + ^PASS ("254 consumed counters, 467 real producers, 0
  orphans"); `test_copy_fit.gd` rc=0 + ^PASS. Zero dashes of any form; 2 {addr} tokens.
- Task 3.4 (rest_bravo_a/b + harness cell) — commit 4b8bd4f — gates:
  * MEASURED AS DRAFTED, no tuning: `rest_bravos_t3_warrior10_solo` **win 0.78, median 3** (min 2, max 5) — inside
    the 0.55-0.95 gate and the 3-12 rounds band, and ABOVE the fence cell's 0.67, which is the relationship the plan
    asks for (the brawl is a failure state, so it is the softer fight). Recorded in the cell's own comment.
  * Full INVRISIL range (WI_CELL_RANGE=90:107) rc=0, zero noise, all four shipped gated cells still in window.
  * `test_combat_data.gd` rc=0 + ^PASS + zero noise — GREEN for the first time since Task 1.4, which is also the
    first run that actually validates `alley_fence_door`'s arena/enemies/allies/on_victory keys (the stage-1 assert
    aborted the encounter loop before reaching them).
  * `test_content.gd` rc=0 + ^PASS; `test_combat_visuals.gd` rc=0 + ^PASS; `data_lint.py` rc=0.
- Task 3.5 (parlor stage + wilovan_inn greet variant) — commit ea8a453 — gates: `data_lint.py` rc=0;
  `test_dialogue.gd` rc=0 + ^PASS; `test_content.gd` rc=0 + ^PASS; `test_copy_fit.gd` rc=0 + ^PASS;
  `inn_guests_ext_loop` seed 3 rc=0 `"passed": true` zero noise, ZERO script edits; `wilovan_address_f` seed 9
  re-run green after the parlor stage landed. Variant-shadow audit proved against the live fixture:
  `inn_guests_ext_start` does NOT hold hat_job_done, the greet node now has exactly ONE variant carrying exactly
  `requires` + `text`, and the option array is still 3 rows.

### PLAN GAP FOUND AND CLOSED — `tests/test_effect_text.gd` (commit 3d160ce)

`EXPECTED_ITEMS` is exhaustive in BOTH directions (`_test_items_exact`, :229-241), so Task 2.4's new item id red the
suite. Task 2.4 Step 3 named only `test_content` + `test_items`, and `tests/test_effect_text.gd` is not in the plan's
FILE OWNERSHIP — DECISION (logged, resolved in the repo's favor): add the one pinned row
`"plum_silk_locket": ["+1 HP", "Resonance 1", "Worth 30 gold"]` at the `hedaults_wardstone` anchor, the same anchor
this lane uses in items.json, so ruling C's method still holds. The suite's own failure message dictates the fix.
TRAP WORTH CARRYING: this red is INVISIBLE to two thirds of the bar — `_check`'s `quit(1)` is overridden by the
trailing `quit(0)`, so the run exits **0** and still prints `^PASS`. Only the zero-noise grep caught it.

## STAGE 2 CLOSE (quests / dialogue / combat data) — Tasks 2.1-3.5 complete

- Headless smoke `--quit`: rc=0, zero noise.
- `python3 scripts/comment_census.py --check`: rc=0. `DATA chars=1155959 _comment_chars=170934 ratio=14.8%
  target<=15.0%`. Lane total vs `main` measured with the plan's Task 6.1 Step 3 snippet: **4,258 new `_comment`
  chars** against **31,331 new non-comment chars** -> budget `112 + 0.1765 x 31331 = 5,642`, so the lane sits
  **1,384 chars under** and lands within 4% of the plan's 4,400 projection.
- Every `tests/test_*.gd` under the three-part bar (rc 0 + `^PASS` + zero `SCRIPT ERROR|Parse Error|WARNING`):
  **30 of 30 GREEN.** Every stage-1 forward-reference red is closed — test_content, test_combat_data and
  test_reachability all pass.
- Balance harness, both new gated cells and all four shipped Invrisil cells (WI_CELL_RANGE=90:107, rc=0, zero noise):

  | cell | win_rate | median_rounds | window |
  |---|---|---|---|
  | alley_fence_t3_warrior10_solo (NEW) | 0.67 | 3 | 0.55-0.95 |
  | rest_bravos_t3_warrior10_solo (NEW) | 0.78 | 3 | 0.55-0.95 |
  | hired_blades_t3_warrior10_wilovan | 0.64 | 4 | 0.57-0.71 |
  | hired_blades_t5_sw14_wilovan | 0.82 | 4 | 0.77-0.87 |
  | alley_footpads_w2_solo | 0.95 | 2 | 0.75-0.98 |
  | boulevard_duel_ring_t3_solo | 0.92 | 2 | 0.55-0.95 |

- Canonicals re-run this stage (all rc=0, `"passed": true`, zero noise, zero script edits): hedault_fragment_loop
  (seed 7), spine_reach (9), invrisil_disagreement_talk (9), invrisil_disagreement_stealth (9),
  invrisil_disagreement_fight (9), wilovan_address_f (9, twice), inn_guests_ext_loop (3).

### WHAT STAGE 3 MUST KNOW

1. **The Lady, not the Woman.** Controller amendment A1 renamed her everywhere. The plan's own Task 5.x QA drafts
   still say "A Woman with a Ring Box" — every fixture, pin and screenshot caption must say **"A Lady with a Ring
   Box"**. The graph, the entity display_name and every `speaker` string already do.
2. **The fence pair was RETUNED off the plan's drafted stats** (0.48/median 2 as drafted). Shipped values:
   heirloom_fence str 6 / con 40 / weapon_die 3; fence_doorman str 10 / con 48 / weapon_die 4 + power_strike.
   Any QA combat script for `alley_fence_door` must derive its seed against THESE numbers, not the plan's draft.
   The bravos shipped exactly as drafted.
3. **`tests/test_effect_text.gd` now carries a lane row** and is not in FILE OWNERSHIP — surface it to the
   controller for the merge train (one row at the `hedaults_wardstone` anchor).
4. **Task 2.2 Step 4's event-order note is live**: both `handover` options and `invrisil_rest_factor.exchange_three`
   carry `effects` + `end: true`, so QA must wait `dialogue_ended` FIRST, then `accomplishment_recorded`.
5. Phase 4 (Task 4.1, the Hedault profile stub) is UNSTARTED and still owed, as is all of Phase 5 and Phase 6.
6. `qa/manifest.json`, `AGENTS.md`'s seed table and both generated docs are untouched by this stage — no
   `derive_qa_surfaces.py` / `render_qa_notes.py` run was needed or made.

## STAGE 3 (profile / QA / gates) — Phase 4 onward

- Task 4.1 (Hedault profile, stub filled IN PLACE) — commit 2924d03 — gates:
  `git diff` shows EXACTLY the three stub lines replaced, zero EOF context, Pallass's two stubs untouched;
  `scripts/sync_agent_guidance.py` rc=0 "PASS: agent guidance (15 canonical skills, Claude mirror exact)";
  `scripts/check_doc_drift.py` rc=1 — DECISION (logged, not this lane's): all four hits are the four v0.16 plan
  docs' missing `Status:` headers, ALREADY fixed on main by 09ebdbe and the whole doc-drift rule DEMOTED to a
  non-required advisory job by user directive the same hour (main's CHOICE-LOG entry + 9550bb2). This branch was
  cut before that commit; adding the header to my own doc cannot green the check (the other three lanes' docs are
  also hit) and touching them is out of FILE OWNERSHIP. Resolves on the merge train.
  Citations spot-checked live before shipping: hedault-enchanting-spec.md:8-11 says exactly what the profile
  claims, and all four quoted Hedault lines exist verbatim in data/dialogue/hedault_enchanting.json.
- Task 5.1 (eight fixtures + COMBAT_BAND_FIXTURES +2) — commit 420ca0d — gates:
  `test_fixture_coherence.gd` rc=0, `PASS: fixture coherence — 128/128` (was 120/120), zero noise.
  BASE CHOICE (decided, logged): the plan says copy `near_invrisil`. Its POST_GAME_BACKBONE pass is a NAME-KEYED
  exemption (`POST_GAME_BACKBONE_EXEMPT`), so a copy under a new name fails 10 backbone arms + the 3-quest floor.
  Used `boulevard_night_footpads_start` instead — same region, same monotone chain, PLUS the full backbone,
  warrior 10 with `melee_hit` 57 (warrior L10's own `requires`), and the geared boulevard kit rather than a
  rusty sword. Every plan-named field (map/cell/facing/accs/quests/skills/removed/inventory/rng) is still what
  this lane edits.
  rng_state derived, never typed: `tests/_derive_rng_state.gd -- 9` -> -6393592048753323481 on all eight.
  Two loop fixtures sit at DUSK (actions_since_sleep 45) so the interiors' `dust_motes` ambience and the sconce
  pools are live for the Task 5.5 windowed read; the boulevard's two night encounters gate on phase:[night]
  and stay shut at dusk.
