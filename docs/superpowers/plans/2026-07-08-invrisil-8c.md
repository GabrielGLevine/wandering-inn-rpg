# Invrisil (issues #12 + #13, milestone 8c) — Implementation Plan

> Status: **DONE** — executed; retained as a design record.

> **For agentic workers:** dispatch-grade tasks; wi-running-the-machine
> governs; comment style per CLAUDE.md conventions.

**Goal:** the City of Adventurers — three maps, the crowd as a mechanic,
the Brothers of Serendipitous Meetings, and "A Gentleman's Disagreement"
with the game's first shipping moral fork.

**Design authority:** `docs/superpowers/specs/2026-07-06-invrisil-design.md`
+ `docs/archive/design/invrisil-art/{direction,picks,handoff}.md` (owned marble
floors + coin-gold accents — the public repo ships this city's signature
bundle-free) + `docs/design/city-identity-bible.md` INVRISIL (verb: DEAL;
quarantine: coin-gold 43° vs amber 25° vs bronze 31°) +
`docs/design/character-profiles-staging.md` "Invrisil (8c)" + issues
#12/#13 (danger lists bind) + the 2026-07-06 dialogue drafts
(task-dialogue-drafts-report.md — the lieutenant registers + BOTH epilogue
endings are pre-drafted; lift, don't reinvent).

## User-ratified (2026-07-08 — do not re-litigate)
1. **WILOVAN fronts the Brothers** (canon Gnoll gentleman-thief; wiki-verify
   his register at content time; Ratici may be referenced, not fielded).
   The HATS-OFF moment is his canon violence tell — staged, load-bearing,
   not a throwaway. The drafted "Mister Sable" register may inform a
   background Brother but no invented lieutenant fronts.
2. **The expose-vs-extort epilogue FORK SHIPS** — both drafted endings,
   real choice, both opaque-safe. No default ending.
3. **THE BED RULE (standing):** Invrisil ships a sleep site — the
   Brothers' parlor carries a guest room/couch (`sleep: true`, in-register
   sleep_toast, unconditional once the parlor is reachable). Blast-radius
   + render-bbox placement discipline (the Riverfarm cot lesson: compute
   REAL render bounds of neighbors, not just cell-Chebyshev).

## Locked design shapes
1. **Arrival:** a Brothers LETTER surfaces at the Guild board (the rumor-
   beat idiom 8b established) banking `invrisil_attuned`; one
   portals.json row — ZERO portal code (the proven contract; any code
   diff = STOP).
2. **The crowd:** 6-8 pool-less citizen extras (PixelLab batch from the
   art handoff or ≤12 fresh gens) — walk-on dressers, NO talk_pool, NO
   observe (the "crowd IS the message" rule; wiring dialogue onto one is
   a scope violation).
3. **Sneakable footpads:** 2-3 `trigger_radius` encounters in the alleys,
   K2-sneakable (the stealth showcase). The SAFE-PASSAGE reward retires
   them via the dormant machinery post-quest (dormancy as a REWARD — the
   spec's idea; rides existing remove/dormant machinery, verify which).
4. **The all-human hired-blades roster:** merchant-livery palette,
   explicitly NOT Watch-alike (canon-identity rule); distinct ids per
   member (#38 discipline).
5. **The fork's data shape:** two mutually-exclusive resolution
   accomplishments (`merchant_exposed` XOR `merchant_extorted`), both
   setting the shared `brothers_marker` (vendor/perk gate) — later
   content may read the fork, nothing this milestone depends on which.

## Lane map
- **Lane α: C1 (maps/cast/arrival) → C3 (quest) → CF** (share
  skeleton_scene + dialogue).
- **Lane β (∥ C1): C2 combat data** — owns combatants/arenas/harness.
  Interface: encounters `alley_footpads_a`/`alley_footpads_b` (sneakable
  placements are C1's), `hired_blades` (warehouse); arena
  `merchant_warehouse`; on_victory `forced_the_confession`; ally
  `wilovan` fields in the FIGHT path via ally_requires on a real
  come-along beat (C3's; the 8b hunter precedent — no teleporting
  allies).
- Registry appends lane α only. Licensed picks: bundle-BEFORE-data
  (bundle-v5 if needed; the art handoff says the signature surfaces are
  OWNED — verify what if anything is licensed).

### Task C1 — the three maps, the cast, the arrival
skeleton_scene: `invrisil_boulevard` ~28x18 (2-cell facades — the
scale-shock read; lamp rows ≤8 lights), `mercantile_alleys` ~20x14
(bent routes, crate dressing, the footpad placements SNEAKABLE — trace
K2's radius/sneak interaction before placing), `brothers_parlor` ~14x10
(borrowed elegance + THE GUEST COUCH per the bed rule). Cast: Wilovan
(profile + pools + the courtesy-as-menace register from the drafts),
the merchant-prince (NPC surface pre-quest), the street fixer
(observe/friendly-rich, rumor-by-the-cup bit), 6-8 extras (shape 2).
moods cards ("marble and coin" / "lantern maze" / "borrowed elegance").
portals.json row + the Guild-board letter beat. Fixture + canonical
`invrisil_walkthrough` (all 3 maps, the sneak-past POSITIVE+NEGATIVE
per the stealth_loop precedent, extras render, quarantine self-check
noted in report). Registry three-way. Windowed: scale-shock vs Liscor
side-by-side, the alley night card, the parlor.
**Danger:** #12's list verbatim + the bed-placement render-bbox rule.

### Task C2 — combat data (lane β, ∥ C1)
Footpads ×2 variants (sneak targets — tuned LOW-lethality; players who
fail the sneak shouldn't be walled), `hired_blades` 3-roster warehouse
fight (first all-human family; merchant-livery palette per shape 4;
Wilovan as gated ally — kit: canon-flavored rogue, tuned so the gated
band holds ~0.6-0.8 with him per the deep_descent precedent). Harness
cells + A/B; canon cites; ≤8 gens for rigs if the caches lack human
doublet variants.

### Task C3 — "A Gentleman's Disagreement" (after C1)
The quest (3-path parity → `cleared_the_brothers_name`): STEALTH/SKILL
(alley infiltration + the [Observe] true-books + keen-eye hidden-safe
beat), TALK (fixer + rival-merchant testimony; optional gold grease —
economy sink in band), FIGHT (warehouse + Wilovan come-along beat).
THE HATS-OFF BEAT staged at the confrontation (canon tell — direction
in the profiles doc). Resolution: the fork (shape 5, both drafted
endings lifted), the Brothers' marker → fence/vendor surface + the
safe-passage perk (footpads dormant — shape 3). Per-path canonicals ×3
+ the fork proven BOTH ways (one canonical per ending or one canonical
+ a unit-level fork proof — implementer's honest call). Registry.
**Danger:** #13's list verbatim; the fork's two endings must be
event-distinguishable for QA without leaking morality text into
opacity-violating surfaces.

### Task CF — close
Whole-8c opus review (composed: the fork × save/load, the safe-passage
dormancy × re-entry, sneak × quest-state, crowd perf on the biggest
map, quarantine check, the marker economy) + rotation (scale-shock
eyes-closed test, the hats-off beat read, the crowd texture, night
alleys) + VISUAL-LOG drain + HANDOFF checklist (+ the fork gets its own
checklist line — the user judges both endings in play) +
`Closes #12`/`Closes #13` + milestone close.
