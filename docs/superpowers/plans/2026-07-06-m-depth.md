# M-DEPTH Implementation Plan (chain step 7 — Liscor interiors)

> Status: **DONE** — executed; retained as a design record.

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Skills READ-ONLY for subagents. Controller commits per green task, explicit paths. FOREGROUND-ONLY verification in every brief.

**Goal:** The ratified seed (`2026-07-06-liscor-depth-seed.md`) + the Runner's Guild addition (user 2026-07-06): four interiors — the Adventurer's Guild (with THE REQUEST BOARD), the inn upstairs, the Watch barracks — plus market-stall depth. Every interior ships a mechanical surface.

**Architecture:** Small maps via the door machinery (sewers precedent); the board = one new data seam (rotating bounty pool, sleep re-armed — the talk-pool rotation idiom applied to a quest-shaped record). NPC relocation (Selys) is the one canonical-QA ripple center.

## Global Constraints

- Canon dressing per wiki (Liscor's Guild/barracks/inn layouts are described — verify at content time); direction card per interior; profiles for any NEW NPC first.
- Bounties: small, repeatable, opaque-safe (gold + counters; NO progress-toward); rotation deterministic (counter % pool — zero rng).
- Disclosure discipline: Selys's relocation reds gate_district_walkthrough + any script interacting with her at (26,4) — enumerate FIRST.
- Suite count per CLAUDE.md at execution (43+ post-A2).

---

### Task DP1: the Adventurer's Guild interior + Selys relocation

**Files:** skeleton_scene.json (guild map ~16x12: desk, board, notice wall, tables, 2-3 adventurer background NPCs [profiles first — canon-flavored walk-ons, e.g. a Gnoll archer + a Drake duo; talk_pool 1-2 lines each]; `guild_door` becomes a real door [door_when NOT needed — the Guild is public]; Selys MOVES from street (26,4) to behind the desk); moods.json (warm bureaucratic card); dialogue: Selys's graph gains desk-context lines; Olesm gains a Guild-interior presence variant? (trace — he stays at the frontage; a pool line references the desk).
**QA:** `guild_interior_walkthrough` canonical; re-path the Selys-touching scripts (disclosed set — quest_errand_* end at Selys!); windowed reads.

### Task DP2: THE REQUEST BOARD (the mechanical payoff)

**Files:** New data seam: `data/bounties.json` (pool of small bounties: id, giver-flavor, condition [existing counter shapes: kill N of an encounter class / deliver / clean / observe], gold reward, canon-voiced copy); sim: `board_bounties()` derives the ACTIVE 2-3 from the pool (rotation index = a sleep counter % pool; accept = banks accepted_<id>; completion = condition met while accepted → reward at board turn-in [gold + toast]); board = a prop in the Guild with a dialogue-like surface (trace the cleanest: a conversation graph FED from bounty data, or a bespoke panel — prefer the graph, zero new UI). 6-8 authored bounties v1 spanning pillars (fight/deliver/social/observe).
**Tests:** rotation determinism; accept/complete/turn-in; reward pathing; sleep re-arm swaps the slate.
**QA:** `board_loop` canonical (accept → fulfill via an existing encounter/chore → turn in → gold + slate rotation across a sleep).

### Task DP3: the inn upstairs + your room

**Files:** skeleton_scene.json (`inn_upstairs` ~12x8: hallway + 3 guest rooms + YOUR room [a bed that works — trace sleep()'s bed binding; your bed = a second sleep site, flavor-distinct toast "Your own bed." — NO mechanical difference v1 per ratification]; stairs door pair in the inn); dressing (Lyonette's room hinted — a locked door with an observe line); moods card (warm dim).
**QA:** extend inn_walkthrough OR a small `upstairs_walkthrough`; sleep-at-your-bed covered (the sleep beat parity — same events, different toast, disclose).

### Task DP4: the Watch barracks + market depth

**Files:** skeleton_scene.json (`barracks` ~14x8 off the gate: briefing room, Zevara's desk [she gains an interior presence — trace her placement vs zevara_intro's gate context: keep her at the gate, the barracks holds her DESK + a duty-sergeant NPC (profile first) + THE CELL room [empty v1, dressed — the quest-outcome surface for later]]; market: +2 interactive stalls (flavor/observe/one mini-buy) on the street).
**QA:** `barracks_walkthrough`; street-stall additions disclosed vs walkthrough counts.

### Task DP5: the Runner's Guild (user 2026-07-06 — the delivery loop)

**Files:** skeleton_scene.json (`runners_guild` ~10x8 off the street:
counter, the DELIVERY BOARD, a Street Runner NPC at the counter
[profile first — young, out-of-breath-professional voice] + a resting
runner walk-on); the delivery seam RIDES DP2's bounty machinery (same
data shape, `type: delivery` — condition = reach map/cell X with the
parcel item within the CURRENT WAKING [sleep fails+returns the parcel:
honest stakes, no timer UI — opaque-safe]; rewards gold per distance
band). 4-6 authored deliveries v1 (inn↔street↔floodplains↔sewers legs;
one "fragile" flavor variant). Movement-skill synergy noted in copy
("Runners move FAST" — the Skills wave's [Quick Movement] makes this
loop sing; ships fine without it).
**QA:** `delivery_loop` canonical (take → carry → deliver → paid →
slate rotates at sleep; the sleep-fail negative).

### Task DPF: gate + docs + opus review

- Full gate + windowed set (all three interiors + the board moment) controller-read; VISUAL-LOG drain.
- Playtest checklist: does the Guild feel like the canon Guild? Does the board loop pull you back after sleeps? Does upstairs make the inn feel like HOME?
- Opus hints: Selys-relocation ripple completeness (grep every selys reference); board × quest-machinery non-collision (bounties are NOT WIQuests quests — verify journal separation or deliberate integration); door pairs' return-cell integrity; bounty reward × economy band (D2 prices — a bounty shouldn't out-pay a quest).

## Self-review notes
- Seed scope-guard honored: 3 interiors + market depth; board in the Guild per the canon ratification; cell room ships dressed-but-inert (the hook, not the feature).
- The board is deliberately data+graph, zero new UI — if the graph shape fights, escalate before building a panel.
