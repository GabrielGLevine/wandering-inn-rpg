# Economy v1 Implementation Plan

> Status: **DONE** — executed; retained as a design record.

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Project skills READ-ONLY for subagents. Controller commits per green task. Briefs carry the FOREGROUND-ONLY verification mandate verbatim.

**Goal:** The ratified Economy v1 (spec `docs/superpowers/specs/2026-07-05-economy-v1-design.md`): diegetic gold earned across all three pillars, spent at Krshia's dialogue-shop, displayed in the inventory panel + toasts.

**Architecture:** One small sim seam (`gold` + `earn_gold`/`spend_gold` + a `gold` effect verb in the dialogue/interact effect vocabulary + loot-table gold entries through the ISOLATED loot RNG); everything else data + one inventory-panel line. Save additive/tolerant-default, no version bump.

## Global Constraints

- Gold is DIEGETIC (user call): earn/spend toasts + inventory line; NO always-on HUD counter; class/level opacity rules untouched.
- Loot gold rides `hash(run_seed, encounter_id)` — NEVER the live sim stream (M7 invariant).
- Prices/wages are DATA; v1 target "a day's honest work buys a potion; the jerkin takes several"; no mid-night feel-tuning.
- Affordability uses the SHIPPED greying mechanism (M4) — no new gating machinery.
- Suite 33 (post-Social) → 34; disclosure discipline for any script whose stream gains earn toasts.

---

### Task D1: sim seam — gold, effect verb, loot entries

**Files:** Modify `src/core/wi_game.gd` (`gold: int = 0`; `earn_gold(n, source)` / `spend_gold(n, sink) -> bool` [refuses when short, no debt]; both emit `gold_changed {delta, total, source}` + the toast ["Earned N gold." / "Paid N gold."]; the dialogue/interact effect applier gains a `gold: +/-N` verb beside `item`/`accomplishment`; expose gold to the dialogue ctx for affordability `requires` [trace how affordability greying reads costs today — M4 shipped it for something; REUSE its ctx key shape]); Modify the loot resolution site (loot table entries may be `{gold: N, chance: X}` — same isolated RNG, emits `loot_dropped {gold}` + earn path); Modify `src/core/save.gd` (additive `gold`, default 0); `src/core/wi_events.gd` (+`GOLD_CHANGED`).
**Tests:** `tests/test_sim_core.gd` (earn/spend round-trip; spend-refusal at insufficient gold emits the refusal toast idiom; effect verb applies from a dialogue choose; save round-trip); loot-gold determinism per run_seed.

### Task D2: content — wages, prices, Krshia's shop, quest rewards

**Files:** Modify `data/skeleton_scene.json` / the chore-prop + serve effects (`gold: 1` on dirty_table clean, serving_tray, patron serve — read where those effects live); Modify `data/dialogue/krshia_*.json` (shop options: healing item IF a consumable path exists in items.json — else gear-only [leather_jerkin if unowned? it's chest loot — pick 3-5 sensible items/prices, document choices], `requires` affordability + `gold: -price` + `item:` effects, canon shopkeeper copy); quest reward beats (errand keep-or-return: return path gains gold); Modify `data/items.json` (+`price` fields; any new shop-only item records); encounter loot tables (+gold entries, goblin fights).
- **DISCLOSURE:** work_loop/crate_*/inventory_loop streams gain earn toasts (FIFO queue — qualified waits); enumerate + list expected-reds.

### Task D3: display — inventory coin line

**Files:** Modify `src/ui/inventory.gd` (a gold line in the panel header area, UIChrome idiom, `_bb_escape` placeholder form if BBCode; extends `ui_inventory_rendered` payload with `gold`); windowed shot controller-read.

### Task D4: QA — economy_loop canonical + window close

**Files:** New `qa/scripts/economy_loop.json` (spec §5 route: chore earn → toast + gold_changed → Krshia buy → affordability NEGATIVE while broke [greyed option proven via dialogue_node options payload] → loot gold from a fight → inventory panel payload shows the total); re-path D2's disclosed reds; `CLAUDE.md` row + count; `ci_sweep.sh` += economy_loop.
- Exit: full sweep green (34 w/ social_loop, 33 if Track A parked), zero grep hits.

### Task DF: gate + docs + opus whole-branch review

- Method hints: gold effect × toast queue ordering (the nested-autosave class), spend-refusal path reachability, affordability ctx staleness across `dialogue_choose` re-gating (M4's per-node refresh — does gold spend re-grey siblings same-node?), loot-gold × seed invariance (canonical combat seeds MUST NOT shift — gold rolls isolated), save round-trip incl. defeat-reload.

## Self-review notes
- Spec §1→D1/D3, §2→D1/D2, §3→D2, §4→D1, §5→D4. §6 respected.
- Unknown to resolve at D1: the exact affordability-ctx key shape (M4 shipped greying — trace, reuse; if it turns out to be skill/class-only, extending it to a numeric gold compare is the ONE sanctioned addition, documented).
- D2's disclosure is the risk center (same class as Social S2) — subset-shipping wages (chores only, serve later) is the valve.
