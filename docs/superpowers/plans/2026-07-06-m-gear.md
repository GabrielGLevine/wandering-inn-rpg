# M-GEAR Implementation Plan (chain step 4)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Project skills READ-ONLY for subagents. Controller commits per green task, explicit paths while lanes live. FOREGROUND-ONLY verification in every brief.

**Goal:** The ratified §1 of `docs/superpowers/specs/2026-07-06-systems-depth-priorities.md`: resonance-limited equipment depth (canon magical-interference limiter), accessory slots, item lore for environmental storytelling, and a wider item pool.

**Architecture:** Resonance = data fields + one small sim seam (capacity check in `equip()`, mirroring the equipped⊆inventory invariant); accessory slots extend the M7 `equipped` dict; lore/effect rendering rides M-LEGIBILITY's L1 formatter + L2 cards (hard dependency — DO NOT start before LEGIBILITY closes). Balance via the E6 loadout-cell machinery.

**Consumes (from M-LEGIBILITY):** `effect_text.item_effect_lines()` (resonance line joins it), the inventory card layout (lore slot reserved by L2).

## Global Constraints

- Canon: the resonance/interference framing wiki-verified at content time (artifact interference is real canon — cite; the exact term the GAME uses is a user taste gate if wording gets invented).
- Visible currencies only (resonance IS visible: "Resonance 2/3" on the card + panel header — it's an item property like gold, not a soul stat). Capacity GROWTH is opaque-until-it-happens (a rare sleep-beat event with its own GDI line — no progress-toward).
- Refusals diegetic ("The charm's hum turns to static…" class copy).
- Tune data never sim; every new equippable gets a harness loadout cell (measured; gated only where a fight is DESIGNED around it).
- Suite count per CLAUDE.md at execution; disclosure discipline.

---

### Task G1: resonance + accessory slots (sim seam)

**Files:** Modify `src/core/wi_game.gd` (`equipped` gains `accessory_1/2/3` keys — trace the M7 dict shape + save round-trip; `equip()` gains the capacity check: sum of equipped items' `resonance` ≤ `resonance_capacity` [new sim field, additive save, default 2]; refusal path emits the established refusal toast idiom with the diegetic line; unequip frees budget); Modify `src/core/save.gd` (additive: capacity + the wider equipped dict, tolerant of old saves' 2-slot shape — MIGRATION-FREE by tolerant read, verify); combat build: accessories' `hp_mod`/`damage_mod`/`damage_reduction` fold through the SAME injection sites weapons/armor use (trace `_build_player_combatant` — the E2/E6 seams; NO new combat fields).
**Tests:** capacity math incl. the refusal; old-save tolerant load; accessory mods reach the combat build; equipped⊆inventory holds across the wider dict.

### Task G2: the item pool + lore lines

**Files:** Modify `data/items.json` — every EXISTING item gains `lore` (1-2 sentences, environmental storytelling per the spec: provenance, maker, what it implies; character/place profiles are the voice contract) + `resonance` (mundane 0; the charm 1; enchanted 1-3); NEW items (~6-10): accessories (rings/charms/talismans — canon-flavored, wiki-checked names), 1-2 tools; prices per the D2 band; sources spread across shop/loot/quest rewards (disclosure for any stream-touching reward change).
**QA:** content validation extended (lore + resonance presence/type checks in test_items/test_content).

### Task G3: UI — cards, slots, capacity readout

**Files:** Modify `src/ui/inventory.gd` (accessory slot rows; the L2 card renders lore + "Resonance N"; panel header gains "Resonance 2/3" beside gold — visible-currency tier); equip flow for accessories (trace the existing equip UI path); refusal surfacing.
**QA:** `inventory_loop` extension (equip an accessory; the capacity refusal negative — equip past capacity, assert refusal toast + state unchanged; disclose).

### Task G4: balance + canonical

**Files:** `tests/sim_combat_batch.gd` loadout cells for every new stat-bearing item (measured); re-check existing gated cells (accessories change nothing until equipped — construction untouched, verify).
**QA:** `gear_loop` canonical? Judgment: extend `inventory_loop` vs a new script — the resonance arc (buy accessory → equip → capacity refusal on the second → unequip → swap) may fit inventory_loop's shape; new script only if streams fight. CLAUDE.md + ci_sweep counts.

### Task GF: gate + docs + opus review

- Full gate + windowed set (cards w/ lore, capacity refusal moment, accessory in combat via the kit/HP change) controller-read.
- HANDOFF playtest checklist: does resonance read as a MAGIC rule or a videogame budget? Is any lore line a keeper/groaner? Does the refusal copy land?
- Opus hints: capacity × save-compat (old 2-slot saves; capacity default vs already-equipped-total edge), refusal path × shop buy (buy while at capacity — item lands in PACK not equipped, verify no auto-equip), accessory mods × mana_shield/damage_reduction ordering (the M7 floor≥1 chain), lore-vs-effect field separation (L1 lines never leak into lore), the E6 harness mirror stays byte-faithful with accessories.

## Self-review notes
- Spec §1 bullets: resonance/capacity→G1, slots→G1/G3, lore→G2, more items→G2, balance→G4.
- Capacity default 2 + the charm at 1 means a fresh player can wear charm+one-enchanted — feel-check at GF playtest; the growth-event design (WHEN capacity grows) is deliberately deferred to the playtest verdict + a user call (it's the taste center of the milestone).
- Hard dependency on LEGIBILITY is real: without L1/L2, resonance would ship as invisible math — the exact anti-pattern this trio exists to kill.
