# Prose naturalization round 2 — map register re-authorship (issue #397)

> Status: **ACTIVE**
> Ruling: user accepted controller rec 2026-08-07 — Option A scoped to
> the MAP register only (CHOICE-LOG 2026-08-07 block; issue comment on
> #397). Dialogue (Set A) stands as shipped. Design authority:
> `docs/prose-naturalization/narrator-bible.md` as amended by Task 1
> below; Phase-5 verdict in
> `docs/prose-naturalization/phase5/phase5-reconciliation.md`.

## Why (one paragraph)

The Phase-5 blind read killed the exit: revised maps scored 52/58
against an untouched-maps control of ~53/62 — the mechanical tells are
dead but ONE rhetorical engine survives (button-family closers on
36–58% of rows, the descriptor triad, the affordance formula, the
narrator's certainty about absent intent), and the pass's own
fix-templates are themselves visible. Trimming again deepens the
editing signature; the residual rows get RE-AUTHORED under tightened
doctrine. Maps only: dialogue outscored its own control and its keeps
read as deliberate peaks.

## Task 0 — freeze the work list (controller, BEFORE lanes)

Derive `docs/prose-naturalization/round2-worklist.jsonl` mechanically,
never by lane judgment:

- Re-run `phase5/reader1-scores.py` per-row over the CURRENT maps
  corpus; every maps row scoring ≥55 enters the list.
- Pattern census over ALL in-scope maps prose (same field list as
  `qa/scripts/extract_prose.py`): BUTTON-family closer match,
  descriptor-triad shape (2 concrete + 1 interpretive clause),
  affordance-formula match, simile-by-type. Every hit enters the list
  with its family tag.
- The two recorded corpus duplications ride along ("Low fence rail,
  bordering the tilled rows." ×3; "The bones rise and fall into step
  behind you." ×2 — two fields, identical text; the talk-only dupe
  gate cannot see these).
- EXCLUDE: `protected-keeps.json` + `protected-keeps-extra.json` rows,
  `holdout.json` rows (untouchable, both lists outrank this plan),
  LANDMARK-register rows with a KEEP-AS-IS disposition in
  `landmark-registry.json`.
- Expected size ≈220 (±40 is fine; if >320, stop and re-scope with the
  user — that would mean the census disagrees with the readers).

Freeze = committed to main before any worktree spawns (plan-docs-
before-dispatch rule).

## Task 1 — bible amendments (controller, Fable-authored, RULED)

Amend `narrator-bible.md` in place; each amendment marked **RULED
(round 2)** with a pointer to the CHOICE-LOG entry:

1. **§2 zero-inference default for SCENIC**: a scenic string states
   what is physically there; interpretation (intent, history, mood,
   what-it-says-about-the-owner) requires a landmark-registry entry.
   The witch_hut worked example gets rewritten to model the new
   default.
2. **Named ban: descriptor triad** (2 concrete details + 1
   interpretive clause). The shape itself is banned in SCENIC and
   FUNCTIONAL regardless of content quality.
3. **Named ban: affordance formula** ("X, good for Y" and variants).
4. **Per-file button-closer ceiling**: ≤1 button-family closer per map
   file (current corpus runs 36–58% of rows; the ceiling is the
   forcing function for ending-shape diversity).
5. **Anti-template rule for the fixes themselves**: the round-1 repair
   templates (one-word deflation coda, inserted self-correction) are
   ALSO named bans — round 2 is fresh authorship, and a lane caught
   trimming instead of re-authoring is a fix-wave finding.

Bans 2–5 ship as data_lint ADVISORY arms in the same commit (manual-
first rollout, same as every prose gate), promoted only at issue close
if the pass ends with zero legitimate exceptions.

## Task 2 — writing lanes (Opus, ~6, disjoint by map file)

Lane split by `inventory-classified/` region file, weighted by
work-list row count (freeze exact split at Task 0; expected shape:
liscor, inn, floodplains+garden, invrisil, pallass, dungeon+ruin+
sewers+riverfarm-residue). NOT Codex: prose voice under a
detectability bar is untested there and the failure mode is the
product.

Every lane brief carries, verbatim:
- Re-author from the FIELD'S JOB, never from the old sentence (fresh
  authorship rule; the old string may not be consulted after the first
  read).
- Canon guard: Book 17 bar, "Magical Door" never the Vol-9 name; never
  change facts, names, numbers, directions, prices, item/skill/quest
  names — findable in the same field.
- Untouchables: protected-keeps (both files), holdout.json;
  `extract_prose.py verify-untouched` before AND after the lane's work
  (rc≠0 = stop, report, no self-granted exclusions — exclusions are
  controller-only, code ∩ artifact).
- No reordering of map arrays; strings edited in place (positional
  prose pins).
- Pin-sync is lane-owned: re-derive every QA pin the lane's strings
  appear in from a real run (pin-delta file per lane under
  `docs/prose-naturalization/pin-deltas/round2-<lane>.json`), then the
  FULL unit bar + touching canonicals green before the lane reports.
- Per-file button ceiling and the named bans are acceptance criteria;
  the lane self-audits its own output against the Task-1 lint arms
  (advisory rc read, zero hits expected) and reports the count.

## Task 3 — composed gates + review (controller)

Anchored train onto `issue/397-prose-naturalization-r2`. Composed
bar: units ALL, full sweep, voice gates (dialogue + maps baselines)
CLEAN, verify-untouched rc=0, census ≤15.0%, data_lint (including the
new advisory arms — zero hits or each hit adjudicated in CHOICE-LOG),
`check_prose_duplication` clean. Adversarial review per lane +
whole-branch review; reviewers get the Task-1 ban list as method
hints ("grep the diff for triad shape / button families / trim
scars").

## Task 4 — exit: fresh blind read (Phase-5 redux, maps only)

Two NEW context-free readers (not the Phase-5 pair), disguised
control, same rubric. Sets: revised-maps sample (120 rows re-rendered
over round-2 text), untouched-maps control (like-for-like). Dialogue
is NOT re-read (shipped).

**Bar (rule at the table, with a currently-failing subject):**
revised-maps midpoint ≤45 on reader-1's scale AND ≥10 points below
that reader's own untouched-maps control. The CURRENT corpus fails
both legs (52 vs 45; 52 vs 53−10=43), so the bar can fail. Both
readers must land the structural verdict "no single surviving engine"
— a named surviving template family in both reports = EXIT NOT MET
regardless of score.

Exit met → close #397 via PR (issue-close template; bans promoted or
explicitly left advisory with reasons). Exit not met → STOP, surface
to user with the reconciliation; no round 3 without a ruling.

## Budget + sequencing

Round-1 shape suggests: Task 0+1 one controller sitting; lanes ~6 Opus
dispatches + fix waves; Task 4 two reader dispatches. Dispatch gates on
usage tier OK (wave dispatched at the 2026-08-07 session reset or the
next OK window). Worktrees `/tmp/wi-397r2-*`, branches `397r2/<lane>`.
