# M-ARC Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Project skills READ-ONLY for subagents. Controller commits per green task. FOREGROUND-ONLY verification in every brief.

**Goal:** The ratified M-ARC design (`docs/superpowers/specs/2026-07-06-m-arc-design.md`): three acts over the existing world, the Raskghar climax under Liscor, context+veto party for the descent, the GDI epilogue into free play. F1 (cold open) already shipped.

**Architecture:** Acts = counter-derived quest-machinery layer + a journal act-line. New content = one small map + one arena + two combatants + dialogue chains, all data. The veil gains its third mode (epilogue). ONE sim-adjacent risk: none planned — the boss gimmick stays inside existing effect machinery; any discovered need for new machinery ESCALATES, never ships silently.

## Global Constraints

- Canon (wiki) for Raskghar/Zevara/Olesm voice + facts; climax copy DRAFT → user taste-gate before the milestone closes (ship behind the gate, flag loudly).
- Stats hidden / opaque-until-sleep; act-line copy is results-only.
- Boss balance: harness GATED band 0.6-0.75 for the Relc comp; solo (veto) cell measured-only; tune DATA only.
- Suite 41 → 43-44; disclosure discipline (Zevara/Olesm graph edits touch cisterns_talk!); every commit green.

---

### Task A1: the act layer + journal act-line

**Files:** act definitions in the quests data family (trace whether quests.json's shape extends or a parallel acts.json is cleaner — counter-derived beats, the WIQuests pure-function idiom); `src/core/quests.gd` ONLY if the act read needs a helper (pure); journal act-line section (src/ui/journal.gd, the skills-panel precedent; act beats + current act header); Act I gate counters (trace first-street-arrival banking; add a flavor accomplishment at the liscor_gate transition if none exists — data seam); Act II gate (3-of-4 quests + second class — trace how "count completed quests" derives).
**Tests:** act derivation units (fresh=Act I; mid-counters=correct act; old-save shapes); journal payload extension.
**QA:** extend journal_skills or a small act assert in an existing canonical; `arc_flow` comes at A4.

### Task A2: Raskghar art + combatants + the descent map

**Files:** PixelLab gen per B recipes: raskghar_scout + raskghar_awakened (hulking bear-wolf, moon-grey, hard outline; combat_scale contained per the Relc lesson); data/combatants.json (scout pair: bruiser-class melee; awakened: high-HP + the slowed-rider heavy swing via the [Calming Touch]/frost precedent); data/skeleton_scene.json `deep_tunnels` map (small, cave family, darkest pin, door_when passage in the sewers gated on the Act III trigger accomplishment [A3 banks it — YOU define the key, document]; one scout route-fight, interact-only); data/arenas.json `deep_warren` (boss arena: walls for positioning play); data/moods.json entries; harness cells (scouts measured; BOSS: gated 0.6-0.75 Relc comp + measured solo).
**QA:** `deep_descent` canonical skeleton (descend → scout fight → reach the warren door) — the boss fight rides A3's chain (fixture until then); windowed mood + sprite shots controller-read.

### Task A3: the climax chain (dialogue + boss encounter + party veto)

**Files:** dialogue: Zevara summons (post-Act-II sleep world-beat — trace how a sleep-triggered dialogue beat can fire: the tremor beat may be a gated Zevara variant + a toast pointer rather than an auto-cutscene — design the cheapest honest shape, document), Olesm briefing, **the Relc joins/veto beat** (pre-descent: joins the boss roster via the ally mechanism; DECLINE branch banks went_alone → boss encounter fields no ally), seal-beat resolutions + post-game lines; the boss encounter entity in deep_tunnels/deep_warren wiring; climax copy DRAFTED canon-true + flagged for the user gate.
**QA:** extend `deep_descent` through the full chain at a pinned seed (+ the veto variant script if streams diverge — C3 precedent).

### Task A4: the GDI epilogue + free play + arc_flow

**Files:** sleep_veil epilogue mode (third mode: boss-victory-triggered; recount classes/levels [results only], the "world keeps counting" close, "The story continues — wanderinginn.com", fade to free play); post-game state (nothing resets — encounters re-arm per the existing sleep machinery; a post_game accomplishment gates the post-game dialogue variants); `arc_flow` canonical (fixture-accelerated whole-arc: act gates asserted via journal payloads through to the epilogue event); CLAUDE.md + ci_sweep counts.

### Task AF: gate + docs + opus whole-branch review

- Full sweep + units + harness (boss band holds) + web parity + windowed set; HANDOFF playtest checklist (does Act II feel like an act or a checklist? does the descent escalate? boss difficulty with/without Relc? does the epilogue land?); VISUAL-LOG drain.
- Opus hints: act derivation × old saves (every historical save shape lands in a correct act); the tremor beat's trigger honesty (can it fire during another dialogue? mid-combat?); boss × defeat-reload (lose to the boss → reload → chain state?); veto branch completeness (going alone must be winnable-or-honestly-brutal, never softlocked); epilogue × consolidation-prompt collision (both fire at sleep-adjacent moments — trace).

## Self-review notes
- Spec §1 acts→A1, climax chain→A2/A3, epilogue/free-play→A4, party veto→A3; §2 components map 1:1; §3 QA→A2/A3/A4; §4 non-goals respected.
- The tremor-beat trigger shape is A3's honest-design judgment point (no cutscene machinery exists — a gated variant + pointer toast is the likely floor).
- Boss gimmick deliberately modest (positioning + adds + slow rider): the FIRST boss should prove the frame, not invent machinery — M-JUICE already makes hits feel heavy.
