# Reachability wave — #417 / #421 / #423 / #424 (planned 2026-08-10)

> Status: **ACTIVE**

User dispatch 2026-08-10: address #417-424 (open set: 417, 421, 423, 424).
Two user directives ride the dispatch:

1. **#423 [Pick Lock] debut in Hedault's shop is viable**, BUT a
   legitimate path to the locked contents must exist for virtuous-path
   players. True use of [Pick Lock] = gaining access to gated areas;
   current locked doors in the world are the starting candidates.
2. **#424 (or sibling): systemic interactability reachability** — the
   defect class that made Hedault walk-unreachable gets a cheap
   systematic check, not a one-off fix.

## Rulings (wave-autonomy; CHOICE-LOG 28-30)

- **[Pick Lock]**: id `pick_lock`, ACTIVE exploration field skill
  (CHOICE-LOG 24 condition honored), granted **rogue L2** (slot empty;
  ladder becomes sneak/pick_lock/find_trap/dangersense/disarm_trap —
  class-expansion spec adds new classes only, shipped rogue table
  unclaimed). Debut gate = locked back room in Hedault's new shop.
  **Two-mode rule**: [Pick Lock] OR a key/unlock earned through
  Hedault's own content (candidate: `a_setting_for_a_lady` completion
  or enchant-commerce trust beat — lane proposes, controller ratifies).
  Contents identical on both paths. Additional [Pick Lock] modes go
  ONLY on existing gates whose fiction is a literal lock (bars,
  tripwires, wedged crates are NOT pickable) — per investigator
  inventory.
- **[Firefly] dead-content hint fix (in #421)**: strip "[Firefly]" from
  the three lying `locked_toast` hints (witch_hollow 1, ruin_surface 2).
  The kindle skill row + its use-copy (sewers burn_toast, runners_guild
  kindle_toasts) STAY — dead-but-honest content; grant path deferred to
  the class-expansion wave. Map-prose edit ⇒ voice-gate maps baseline +
  pin-sync discipline applies.
- **#424 architecture split**: grant-path orphan graph (skills / items /
  dialogue entry / door graph) = `data_lint` check, ADVISORY tier first
  (allowlist mechanism for code-granted ids). Cell-level
  interactable-adjacency/reachability = **GDScript unit suite over the
  real loader** (sim-truth; the #413 lesson — never re-derive sim
  predicates in a second language). Promotion to hard-fail per category
  once the shipped set is clean. The adjacency suite must FLAG
  pre-#423 Hedault when run against the old layout (proves the
  detector) and pass on the composed tree.

## Lanes

| Lane | Issue | Branch | Owned files | Notes |
|---|---|---|---|---|
| T | #417 | issue/417-touching-install-fixture | scripts/derive_qa_surfaces.py, qa/manifest.json (regen) | small; MERGES FIRST (manifest base for #423) |
| O | #421 | issue/421-dangersense-followups | src world.gd + dangersense_overlay.gd, new QA canonical(s), maps witch_hollow/ruin_surface (Firefly strip), voice baseline-maps | I3 reconcile-after-check, M2 teardown event, M5 duplicate arm collapse, M4 combat-context canonical |
| R | #424 | issue/424-reachability-lint | scripts/data_lint.py, tests/test_reachability.gd (new), allowlist | advisory tier; adjacency suite sim-truth |
| H | #423 | issue/423-hedault-shop | data/maps/invrisil/* (new enchanter_shop + boulevard + alleys), skills.json, classes.json (rogue L2), dialogue/hedault_enchanting.json, quests.json (if key path), hedault QA scripts + fixtures, steel_thread/invrisil_walkthrough | dispatches AFTER T merges |

Phase 1: T ∥ O ∥ R (disjoint files). Phase 2: H (after T).
Seam checks at merge: H's manifest rows regen on composed tree; R's
lint/suite re-run on composed tree (Hedault flag must clear; pick_lock
must show a grant path).

Close: composed re-gates + preflight --full + steel-thread windowed run
(route grows the shop leg) + VISUAL-LOG drain + ledger/HANDOFF.
