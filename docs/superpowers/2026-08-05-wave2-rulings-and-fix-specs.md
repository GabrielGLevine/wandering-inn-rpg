# 397 petition rulings (Fable, rolling — feed to the integrator)

## Pallass lane
1. Ledger-chain irony landing site: **REFUSED.** The chain now states its
   facts and the player assembles the irony — that is §2.3 working as
   designed. No new button, no reserve spend.
2. Four unlisted peaks (forge_hall apprentice TRY THE OTHER WAY ROUND;
   market clerk-writes-and-moves-on interior_flavor; forge "She says
   nothing about it. She wouldn't."; den_shop elbow observe):
   **GRANTED — add all four to protected-keeps.json** (integrator, with
   exact_text resolution).
3. Consequence-anon ("Guessing here costs somebody a week"):
   **PERMITTED CLASS.** Bible §6 gains it: a hypothetical future
   consequence-bearer is not an inferred absent agent. Integrator amends
   §6 with one line + this example.
4. §8 lettering proxy blind spot ("in a … hand"): **Phase 4 tooling
   item** — metrics lane adds hand/chalked/stamped context to the
   lettering classifier.
5. ISSUE_NAMED_WORK's now-unresolved pallass snippet: **retire the entry
   at Phase-4/close** (mark done-by-design, keep the audit trail).
6. Verbatim-identical civic observes + steam vents: **ACCEPTED as
   deliberate** — record in the lane's pin-delta doc header.

## Invrisil lane
1. [Appraise Foe] §1.4 ceiling exception (68-word evidence dump):
   **GRANTED as exception** — facts outrank the ceiling. The tricolon
   trim mandate is NOT granted (it would re-pin a green script for a
   taste win; leave the string byte-untouched).
2. Two negation keeps (Wilovan courtesy line; "Merchant-livery, not
   Watch steel"): **ACCEPTED** — both meet keep-criterion-as-function.

## Dialogue lane
- Skips (forge_temper_golem, rags_inn, ksmvr_plates, selys_delivery):
  **ACCEPTED** — W5/holdout/grant grounds verified by reviewer (pending
  its report; revisit only if the reviewer breaks a skip claim).

## Empty-regions lane
1. §1.4 exceptions for the two trapped_halls Act-V READING toasts:
   **GRANTED** — the prop's byte-frozen holdout sibling is 7 sentences;
   the ceiling cannot bind below the floor the holdout sets.
2. Four unlisted peaks (Yvlon observe; ruin intervals-nobody-eyeballed;
   trapped_halls e14 observe [make the bible citation binding];
   detect_magic breath simile): **GRANTED — add to protected-keeps**
   (pending its reviewer not breaking the claims).
3. ISSUE_NAMED_WORK self-test inversion (asserts pre-fix snippets still
   exist → fails once the ruled work is done): **REAL BUG — integrator
   fixes**: the check flips to "named-work strings NO LONGER match once
   their region's pass lands" or the entries carry a done flag.
4. §9.2 loosen-to-50 invocations (sewers 46%, deep_tunnels 47%,
   seal_vault 50%): **ACCEPTED** — all were at/above these shares
   before the pass.
5. RE_ANON split (four §6 motive-words vs three present-indefinites):
   **ADOPTED for Phase 4** — the four-word figure becomes the §6.4
   metric; report both.
6. Out-of-ownership stale pins flagged (seal_fed_start fixture,
   test_sim_core.gd:4001 exact-== ruin toast, :1942 begins_with A DOOR):
   **integrator work list** — these are pin-sync items like qa/scripts.

## Dialogue lane — review adjudications (Fable)
- hedault heirloom_truth G4 override: STANDS (issue problem-1 + bible §7
  outrank the 2026-08-03 W5 registry) but must be RECORDED in
  w5-directive.md (entry lapsed, superseded by #397) + pin-delta verdict.
- Integrator warnings: NEVER regenerate inventory.jsonl (it is
  verify-untouched's baseline); heatmap doc is a point-in-time targeting
  artifact — mark it superseded-after-Phase-3 rather than regenerating.

## 396 Lane C fix-wave spec (POST-RESET, from its review — pot ACCEPT, 3 Majors)
M1 witch_lesson orphaned: add graduate re-entry option in witch_ask
  (requires witch_lessons>=1, no effects, goto witch_lesson).
M2 legacy-graduate hole: starter option must ALSO hide for witch_lessons
  graduates — hide_when can't OR (AND semantics); agent finds the
  sanctioned shape (bool-gate requires{witch_lessons:0} if whitelisted,
  else restructure), discloses, probes the graduate cohort.
M3 teach-gate belt untested: after M1, add makings_loop leg on the
  graduate path asserting teach option ABSENT (hide_when witch_lessons +
  requires chain both proven).
Minors: pot observe fill-variant; restore peaceful-OR derivation comment.
Controller still owes: read lanes/l396c-evidence PNGs (lambs-in-pen read
+ tend prop under east HUD panel question).

## Pallass lane review results (MERGE-BLOCKED, fix wave POST-RESET)
H1 self-test green→red = the ISSUE_NAMED_WORK inversion bug (already
  ruled, integrator fixes tool-side; ALL region lanes trip it).
H2 maps voice gate hard-fails all region lanes BY CONSTRUCTION (its
  skeleton bakes non-talk prose verbatim). RULING: integrator masks
  #397's prose-field list in map_skeleton (gate guards talk surfaces +
  structure only) OR controller re-baselines composed-tree at close.
  Prefer masking (keeps the gate meaningful mid-pass); disclose in PR.
H3 pallass_forge:885 once_per_waking lost refusal+re-arm payload AND
  contradicts :882 observe (inside vs outside) — pallass fix wave.
M1 den_shop:239 restore a short resolution clause (exemption found).
M3 three byte-identical stall observes — differentiate (same treatment
  as their toasts got).
M5 market lift observe transient-motion claim vs locked gate — fix.
Monoculture re-vary: 3-4 negative-non-event closers + the two new
  "doesn't look up" endings (three now in one region).
L6 market:533 Eagle Eyes thematic closer = missed indicted work — fix.
Petitions: pallass peaks 3/4 confirmed by reviewer (jars toast REJECTED
  as keep); ordinance-44 variant flagged as possible LANDMARK petition
  (elevator_pass_stamped-gated, earned) — Fable rules at integration:
  GRANT as landmark from reserve? Defer to close; leave string as-is.
Petitions must be WRITTEN into the branch (M4) — fix wave adds them.

## Inn lane (landed clean during quiesce; review PENDING post-reset)
Petitions (rule at integration): 3 keeps (desk cup-ring microhistory,
kettle §1 exemplar, "Innkeeper! Anyone?" §9 exemplar) — GRANT all three;
§9.2 exception inn_upstairs 55.6% (baseline 66.7%) — ACCEPT;
seal_fed lore_notes 4-site re-bake — INTEGRATOR work list (coordinated
edit, greens today).
SHAs: 8b385d84 + 534168ac (rewrites+reshape), 7110eb53 (pin-deltas, 22
rows). Needs its adversarial review post-reset (same method as pallass).

## Invrisil lane review results (CHANGES REQUESTED, fix wave POST-RESET)
H1 boulevard rooftop_line.locked_toast: false gate cause ("no way up
  from this side") — the blocker is [Eagle Eyes]; mirror the
  counting_house "takes an eye you don't have yet" pattern.
M2 boulevard_coyle_front.observe: restore the chain's pre-interaction
  pointer (side entrance/wagons/night payload) — the rewrite kept
  ornament, cut information, on an UNINDICTED string.
M3 anon summary: region actually 11.1-13.6%, misses ≤10% — fix 2-3 more
  anon strings honestly (no laundering) + correct the report arithmetic.
M4 monoculture: 43% on two templates; stationer runs "enumeration + one
  anomaly" 4× consecutively — re-vary at least stationer's four + break
  the "while you stand there/watch" tic (0→3) + the adjacency twins
  (stationer e3/e4, mercantile 1231/1252, parlor 16/17, boulevard 16/17).
M5 [Appraise Foe] petition RULED: S1+S2 GRANTED verbatim (gated dialogue
  quotes them); S3 (hidden safe foreshadow) DENIED — trim to 2 sentences,
  zero fact loss; re-pin invrisil_disagreement_stealth accordingly (it
  pins the VARIANT — verify which string it pins before editing).
L6 invrisil_extra_1: "Two doors numbered eleven" contradicts her frozen
  talk_pool (name mismatch, not number dup) — fix the fact.
L7 one garment two descriptions — reconcile. L9 Wilovan friendly_line:
  keep granted, cut ONE of the two interpretive moves. L10 bottle_shelf:
  REVERT (unindicted churn). L12 lesson only. L13 integrator re-derives
  pin sites (already planned).

## 396 Lane D review results (break-tests all RED correctly; fix wave POST-RESET)
H test_reachability _validate_retired: nested assert prints but rc=0 —
  make it halt like the :51 orphan gate (exit-code red, not grep-only).
M test_content blind to DIALOGUE-shaped producers of retired counters
  (the exact deleted shape) — extend its retired check to dialogue
  effects, or document test_shipped_ids as sole referee + fix the
  overclaiming comment; agent's call, disclose.
M manifest row 143 (thicket_keeps_talk) fixture field still
  thicket_talk_start — fix to thicket_legacy_start + regen notes; note
  thicket_talk_start now orphaned (keep or delete, disclose).
M riverfarm_fight_start LEADING _comment three-ways false (build/allies/
  solo) — rewrite: geared t3_warrior10, solo briars, minority-draw
  framing (~1-in-3 joint), re-derivation warning (most re-derivation-
  prone canonical; any briar/wolf/gear touch likely forces rng_state
  re-search).
L winter_teeth_talk: pin the traced-path resolution text (ladder proof;
  invrisil_house_name_talk:971 precedent).
CONTROLLER NOTES: (a) shared-bank exposure — "Just passing through."
  (_shared_lines just_passing_through) is pinned in 3 winter scripts and
  used by 8 conversations; verify 397 dialogue lane never touched
  _shared_lines (it reported bank-edit rule compliance) + integrator
  guard; (b) wi-writing-qa-scripts skill folds at close (Fable-only):
  assert_event_count + 3 more actions missing from "complete set" table;
  event-order lesson (dialogue_started → dialogue_node →
  ui_dialogue_shown — waiting on ui first advances cursor past the hub
  node).

## Empty-regions review results (SHIP w/ bounded fixes POST-RESET)
M2 deep_tunnels:126 restore the WITHHELD species name (Raskghar named on
  the region's first prop, ahead of every in-world source) — keep the
  layered-ash evidence clause, re-anonymize the keeper.
L1 dungeon_approach:227 eyeless-face toast restates its own observe —
  re-author with a real dread beat (not counter-flat).
L2 trapped_halls:344 "Something"→"Nothing" is counter-gaming (§10 named
  move) + momentarily misparses as safe — restore an honest indefinite.
L3 "very bad idea" idiom: one narrator two treatments — unify (either
  both stay or both go; lane's call, disclose).
M1 reporting: restate anon results on the 7-word target measure
  (dungeon 18.0%, sewers 16.7% — MISS with enumerated irreducible
  floor; ruin 9.3% clears). No laundering to close the gap.
M3 = H2 maps-gate class (already ruled, integrator masks).
PETITIONS RULED: detect_magic.variants[1] 5-sentence exception GRANTED
  (the 6-sentence frozen sibling makes a 2-ceiling incoherent);
  skill_uses.observe.variants[0] 4-sentence: GRANTED at 4 (the Pisces
  beat is worth its cost — §3 defensible). Lane's 4 peaks: reviewer
  audited all 21 flagged survivors, no favorites — the 4 peak GRANTS
  stand (from earlier ruling); petitions must be WRITTEN to the branch
  in the fix wave. ruin:170 "Packed by someone" — KEEP (present-party
  pronoun, reviewer's read adopted).

## Liscor lane (landed clean during quiesce; review PENDING post-reset)
SHAs 52353c8e + 57147f9f. 30/97 changed; anon 25.8%→11.3% (11
residuals, all reasoned); 66/68 sweep green, 2 pin reds + test_sim_core
:3571 — all three PROVEN payload-only by sync-then-revert.
Petitions (ruled): peaks 1-2 (runners_guild boots-still-laced, street
one-shout-from-tipping) — GRANT to protected-keeps; 3 (hat-letter copy
doc closer) — GRANT as diegetic-document exception (§1.1 does not bind
in-world documents' own voice; record in bible). Finding 4
(biomes.json street.interior_flavor duplicate template string) —
integrator work list: rewrite it there too (it is player-facing the
moment any map declares biome:street; out of the 847-corpus by
extraction scope, not by principle). Finding 5 (last ellipsis strings:
one is inn's — that lane owns it; one is riverfarm's "..." ambient —
rides the riverfarm pass post-396). Review post-reset.

## Floodplains+garden lane (landed during quiesce; review PENDING post-reset)
SHAs 8e94a5c8 + 7462194c + b8f3b64d. 35/101 changed; garden touchable
anon 0/9 (25% floor is holdout/protected arithmetic); semantic
absent-agent 8→0; sweep 68/68 GREEN because the lane SYNCED ITS OWN 6
PINS (beast_tamer_loop, gh330_companion_gate_loop x2,
floodplains_price_help x2, garden_walkthrough) — a brief deviation
(integrator owns qa/scripts) but proven red-on-base/green-after and
DISJOINT from every other lane's pin scripts. RULING: accept as done;
integrator treats fg pins as PRE-SYNCED and its pin-delta file as
informational; reviewer verifies the 6 pin edits alongside the prose.
Petitions RULED: yoke line keep — GRANT (present-behaviour inference,
pallass den-shop argument; paid for by the [7] dedupe); wild-snap
locked_toast 3-sentence exception — GRANT (two-word middle beat; gate
requirement carried by three sibling toasts). Self-test exit 1 = the
known ISSUE_NAMED_WORK bug (integrator fix, already ruled).

## Dialogue fix wave (landed during quiesce) — 397/dialogue COMPLETE
SHAs 0ffea9ff + 4680190e. All 14 review fixes applied: HIGH-1 G4 lapse
recorded at w5-directive.md:27 + proper card-voice rewrite; HIGH-2
false grate claim gone; 6 rejects re-done; both dropped shortlist
graphs (zevara_intro, invrisil_stationer_client) judged + executed;
clipped verdicts 3→1 (survivor is BASE text); pin sites now 7 (2 new:
invrisil_setting_talk:99, invrisil_house_name_talk:1074;
zevara summons_send has zero). Gates: verify-untouched 0, voice gate
CLEAN hard=0 warn=0 all 9 files, corpus flagged 31→20/577. Dialogue
lane needs NO further fixes — light re-read rides the integrator
review. NOTHING left in flight; all seven map lanes + dialogue landed.
