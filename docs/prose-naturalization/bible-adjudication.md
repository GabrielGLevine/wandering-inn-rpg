# Narrator-bible adjudication — GH#397 (Fable, 2026-08-05)

Rulings on all 13 [ADJUDICATE] items plus the two reserved questions.
Fold these into `narrator-bible.md` (drop -DRAFT), keep this file beside it
as the ruling record.

**Folded in 2026-08-05 (fix wave).** `narrator-bible.md` is RULED: every rule
the draft marked `[ADJUDICATE]` now carries its ruling, and §11 of that file is
the index back into this table. Two arithmetic corrections were applied while
folding — the landmark budget is **8 spent / 4 reserve**, not 7/5
(`mercantile_alleys` carries two *distinct* beats, not two renderings of one),
and the quest-prop reclassification count is **281** as the review said, which
became 278 once skill-outcome was re-scoped by path. The ceiling of 12 and
every ruling below stand unchanged.

| § | ruling |
|---|---|
| 1.4 | **ADOPT** 2-sentence functional ceiling, `copy` excepted (3–5). Ceiling counts sentences, not clauses — a one-sentence hint with an atmospheric first clause already satisfies rule 1.2. Loosen only by petition with the string in hand. |
| 2.5 | **SOFT TARGET, never a gate:** in a file with ≥8 scenic strings, ≥⅓ should carry zero inference. Phase 4 reports it; no lane is scored on it. Pair with §10: zero-inference must not converge on one rhythm. |
| 3.3 | **KEEP as read-only principle.** No tooling test exists; it binds the Phase-5 cold read and reviewer judgment, not Phase-2 mechanics. |
| 5 (ceiling) | **ADOPT 12 beats** (7 spent, 5 reserve, controller allocates by petition; spending is a swap). The "ratifies current state" worry is answered by the reserve — a chain that genuinely wants more petitions and swaps. |
| 5 (victory_toast) | **ELIGIBLE.** Relc demotion stands. §4 rule 1 applies unchanged — the CHAIN earns it, and post-combat is the most template-prone surface, so any landmark victory_toast gets an explicit petition, never a self-grant. |
| 6.4 | **ADOPT** corpus ≤12% advisory. Per-region: unpeopled (garden/dungeon/ruin/sewers) ≤15% — the deepest cuts, from 44–58%, because the emptiness is the content; populated ≤10%. The absent-builder counter-case is honored by §6 rule 1 (actions the evidence shows), not by a looser number. |
| 7.2 | **ADOPT 8** (from 29). Keep-criterion-as-function ratified verbatim, including all four worked examples (statue = partial keep; seal_vault = keep correction, lose CAPS; ruin = keep, restaged; hedault deferred to Phase 3). |
| 8 | **PERMIT diegetic lettering, BAN emphasis (0).** Mechanical proxy adopted: diegetic CAPS must be attributed to a physical written surface named in the same string (sign, plate, board, `copy` heading). Phase 4 reports lettering-context CAPS and bare CAPS as separate classes; the bare list is the work queue; judgment stays the final arbiter. |
| 9.2 | **ADOPT 40%** single-ending-shape cap per file, advisory — but Phase 4 computes current per-file distribution BEFORE any lane treats it as binding; if most currently-good files violate it, loosen to 50% rather than force churn. |
| 9.4 | **Adjacency = entity-list order** (cheap, deterministic) as the computed check; grid proximity stays a cold-read lens where a map has obvious prop clusters. |
| 10.4 | **ADOPT, report-only to controller/reviewer.** Per-file sentence-length stdev is computed in Phase 4 but NOT published in Phase-2 lane briefs and has no target number — an unseen metric can't be gamed. |
| §0 (tiers) | **Registers replace tiers for map prose.** The dialogue tier system does not extend here; §0's argument is ratified. |
| (hard gates) | **No new hard gates this pass.** Inherited bans (mid-line ellipsis, "the whole of", CAPS-emphasis) ship as ADVISORY in maps mode now; promote to hard at issue close only if the pass ends with zero legitimate exceptions — the same manual-first rollout the dialogue gate had. |

Also ratified as binding Phase-2 doctrine:
- One inferred microhistory per map file (the witch_hut rule).
- Guard 10.1: default action on a scenic string is LEAVE IT ALONE. Work
  queue = 50 demotion candidates + inherited-ban instances + anon-agent
  reduction + negation/geometry shapes. A lane that rewrote most of its
  region has misread the assignment.
- `protected-keeps.json` entries are calibration reading, mandatory before
  a lane's first rewrite.
