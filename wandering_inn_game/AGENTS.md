# Active Godot project guidance

Model-neutral guidance for `wandering_inn_game/`, the active Wandering Inn RPG project.

## What this is

Fresh Godot 4.7 project, QA-first: every feature must be agent-verifiable, no human playtest. See
`docs/superpowers/specs/2026-07-01-wandering-inn-v4-agent-qa-foundation-design.md`
for architecture rationale + north star (BG3-in-Wandering-Inn, team of 1, [Skills] usable outside combat).

**Product constraints (repo-wide):** HP readouts + damage numbers player-visible (playtest decision, M2); raw attribute scores (STR/DEX/etc.) stay out of player-facing text by default — a convention enforced by `test_effect_text`'s tripwires, not a hard ban (softened 2026-07-13: clarity tradeoffs allowed, update the tripwire in the same commit). Lore canon from Wandering Inn Wiki, never invented.

## Commands

**Run discipline (bites every session):** failed `assert` HANGS headless run forever — alarm-wrap long invocations
(`perl -e 'alarm 45; exec @ARGV' /usr/local/bin/godot ...`; macOS has no `timeout`). Grep every run output for
`SCRIPT ERROR|Parse Error|WARNING` — unit suite can print `PASS` yet hide swallowed SCRIPT ERROR. Full gate discipline:
`.agents/skills/wi-verifying-changes` skill.

	# Run the game (windowed)
	/usr/local/bin/godot --path wandering_inn_game

	# Import pass — REQUIRED after creating any new .gd file (class_name registration)
	/usr/local/bin/godot --headless --path wandering_inn_game --import

	# Headless smoke check
	/usr/local/bin/godot --headless --path wandering_inn_game --quit

	# Unit tests (pure classes only — SceneTree scripts, run individually)
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_sim_core.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_event_log.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_combat_data.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_combat_sim.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_progression.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_dialogue.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_quests.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_save.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_content.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_sprite_registry.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_water_shoreline.gd   # #411 dual-grid shoreline mapping + generated-sheet polarity pin
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_combat_visuals.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_audio_data.gd
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_effect_text.gd  # M-LEGIBILITY L1: WIEffectText exact lines + drift tripwires + forbidden-vocab
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_acts.gd          # M-ARC A1: act-derivation
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_items.gd         # M7 E1: items/equipment validation
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_traversal_seams.gd  # Skills Wave K1: freeze/burn seams -- the property table's BYTE-IDENTITY referee
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_interactions_table.gd  # #348: property table (mirror contract, injection, new carriers as data, authored precedence, verb/placement binding)
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_input_hints.gd      # Issue #18 S3: WIInputHints label table + device classification
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_portals.gd          # Magical Door plan Task D4 (issue #8): WIPortals catalog filter + graph derivation
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_settings.gd         # Issue #77: text-scale math + drift tripwire, settings.cfg round-trip, WIAudio UI-bus-routing fix, hint reset_hints() flips, board_renderer.gd reduce-motion gate sites
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_world_visuals.gd    # GH#104/#94: world.gd wiring contracts (raw-source slicing) -- the presence reconciler's light-unregister/LIGHT_BUDGET/hide_sprite discipline
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/test_interactable_reachability.gd  # GH#424 (ruling 30): can a player STAND next to every entity WIInteractions.dispatch answers -- all three interactable kinds plus every prop arm (PROP_ARM_KEYS mirrors interactions.gd:63-189); ambient scenery is excluded but COUNTED in the PASS line, never silently. Real loader + real is_cell_blocked, orthogonal-only (interact() faces player_cell + a CARDINAL facing). Floods the world at its most OPEN (all Skills, ice on every freezable cell, present_when/burnable/cuttable blockers cleared) so a #398 skill-gated pocket is not mistaken for a defect; each erased gate is then held to the SAME adjacency rule (a gate you cannot stand next to seals its own pocket). TWO waiver lists, different semantics: KNOWN_DEFECT_WAIVERS (a named issue is fixing it; a stale row REDS) and DESIGN_WAIVERS (comment-backed deliberate park, never goes stale). Blink is unmodelled and the run discloses it. CAPABILITY -- is a Skill learnable, does every gate still have a live carrier -- is data_lint's check_content_reachability, never re-derived here

	# Balance harness — 200 seeded AI-vs-AI fights; THE authority on combat data tuning
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/sim_combat_batch.gd
	# Path-diversity harness (GH#160) — archetype playstyles through the REAL
	# WIProgression sleep sequence + an equal-playtime power battery; GATES
	# pooled terminal entropy >= 2.0 bits and combat-lane [Spellsword] share
	# <= 0.45. MIRROR CONTRACT with WIGame.sleep()'s progression order.
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/sim_class_paths.gd
	# Progression-pace harness (GH#211, harness-first) — Act I->III traces:
	# REAL autoplay fights banked through the REAL WICombatBanking deposit
	# path + mirrored WIProgression sleep order. Reports per-act total-level
	# bands (report-only until ratified; determinism leg asserts). Baseline
	# p50 bands 2026-07-19 (pre-weight): warrior 6/14/16, caster 7/15/16,
	# helper 10/21/26 — the #211 problem quantified (Act-II overshoot +
	# table-ceiling plateau + ungated chore pace).
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/sim_progression_pace.gd
	# Sharded (ARCH-1): partitions cells across N parallel godot processes and
	# diffs each shard vs a baseline git ref (formalizes the manual git-stash
	# byte-identity method). WI_CELL_RANGE=LO:HI / WI_CELL_COUNT_ONLY=1 are the
	# underlying env-var hooks sim_combat_batch.gd itself honors (both unset =
	# unchanged single-process behavior, proven byte-identical).
	wandering_inn_game/scripts/harness_shard_diff.sh --shards 4 --baseline-ref main
	# Difficulty-tier sweep (GH#360a) — the 135 cells at x0.75/x1.0/x1.3 via
	# WI_DIFFICULTY_MULT on the ONE batch driver. THREE GATES, ON BY DEFAULT
	# (#384 item 1, ratified 2026-08-04): inert-at-Silver (the explicit x1.0 leg
	# is identical to a plain run), per-cell monotonicity, and extreme flips with
	# a NAMED whitelist — each entry pins the Silver it was argued from and
	# LAPSES (reds) when that number moves. --report-only explores a candidate
	# tune without a red.
	wandering_inn_game/scripts/difficulty_tier_sweep.sh
	# Class-parity leg (GH#360b) — mirror-matched builds at total-level bands
	# 10/14/18 over a fixed arena set; reports max spread per band. The WHOLE-BAND
	# spread is still NOT gateable (ai_kit coverage confound). The ratified gate
	# (#384 item 2) is the ai_kit-STRATIFIED envelope, report-only by default:
	/usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/sim_class_parity.gd
	# Envelope GATE ON — what the merge train runs against a candidate branch.
	# rc!=0 = a stratum busted its ceiling, OR a ratified ceiling went unevaluated
	# (a stratum can vanish when a grant flips a row MUTE->SPOKEN).
	# WI_PARITY_BAND=<10|14|18> narrows the run and is only ever a PARTIAL fence.
	WI_PARITY_ENVELOPE_GATE=1 /usr/local/bin/godot --headless --path wandering_inn_game --script res://tests/sim_class_parity.gd

	# QA scripts isolate user:// by default via HOME=.godot_home/qa-<script>-<pid>.
	# Use --user-dir DIR after the mode to choose a stable isolated HOME explicitly.
	# Combat QA scripts REQUIRE a fixed seed (fights are deterministic per seed).
	# Per-script seed + one-line purpose: the canonical seed table below.
	# Per-script current purpose/fixture/tier index: docs/QA-SCRIPT-NOTES.md.
	# Exact route/assertions live in qa/scripts/<name>.json.
	wandering_inn_game/qa/run_qa.sh <script> headless --seed=<seed>

	# QA playtest scripts (THE verification tool — prefer this over manual reasoning)
	# Full canonical sweep in one command (what CI runs — .github/workflows/ci.yml):
	wandering_inn_game/qa/ci_sweep.sh                           # all at pinned seeds + grep discipline; --only a,b,c to restrict; list MIRRORS the seed table below — keep in sync
	# QA tiering (ARCH-1, docs/superpowers/specs/2026-07-12-full-game-architecture.md
	# §2.3): qa/manifest.json's per-script "tiers" (smoke/full, smoke subset of
	# full, drift-checked every invocation) and "surfaces" (maps/fixtures/
	# skills/systems, GENERATED ONLY by scripts/derive_qa_surfaces.py — never
	# hand-edit, drift-checked every invocation via --check).
	wandering_inn_game/qa/ci_sweep.sh --tier smoke               # load_gate + one canonical per subsystem, <3min, runs on every CI push
	wandering_inn_game/qa/ci_sweep.sh --touching <path>[,<path>] # maps changed paths -> surface tags -> the crossing canonicals (lane re-gates)
	wandering_inn_game/scripts/derive_qa_surfaces.py             # regenerate qa/manifest.json's surfaces after adding/editing a QA script or fixture
	wandering_inn_game/scripts/derive_qa_surfaces.py --check      # verify surfaces aren't stale (what the drift check runs); ALSO fatal when a manifest row's `fixture` contradicts its script's own `fixture_save` (#396: both present must match — the derivation prefers the script's, so a stale row used to stay green forever)
	wandering_inn_game/qa/run_qa.sh load_gate headless          # loads every .gd/.tscn/.tres; catches parse/compile errors. NATIVE-ONLY (see below)
	wandering_inn_game/qa/run_qa.sh inn_walkthrough headless   # full inn journey, no screenshots
	wandering_inn_game/qa/run_qa.sh inn_walkthrough windowed   # same + screenshots (a window opens briefly)
	# Output: qa_output/<script>/result.json, events.jsonl, *.png — read the PNGs to see what a player sees

	# Fully headless web QA (zero windows): exports, serves, drives under headless Chromium
	wandering_inn_game/qa/web/run_web_qa.sh inn_walkthrough              # add --skip-export to reuse last build
	wandering_inn_game/qa/web/run_web_qa.sh combat_walkthrough 9         # seed is threaded through window.__WI_QA__ -> JavaScriptBridge (M3 T8)
	wandering_inn_game/qa/web/run_web_qa.sh mouse_loop 9 --touch         # touch-context mode (issue #105) — see below
	# Output: qa_output/web_<script>/result.json + *.png
	# NOTE: load_gate is native-only — DirAccess cannot enumerate the packed res:// in the
	# web export, and the gate fails loud ("scanned zero resources") rather than false-passing.
	# Real server (issue #105): run_web_qa.mjs serves build/web/ via a REAL local
	# node http server (random free port, correct MIME per extension — .js
	# text/javascript, .wasm application/wasm) rather than Playwright route
	# interception. Route interception is gone entirely, not kept for anything: it
	# used to be needed for nothing real (window.__WI_QA__ is set via
	# addInitScript, __WI_QA_SHOT__/__WI_RESULT__ are read via page.evaluate,
	# screenshots via page.screenshot — none of that is network traffic), and it
	# actively broke audio — AudioWorklet module fetches run in a separate worklet
	# execution context whose requests bypass Chromium page-level request routing,
	# so under interception every web QA run silently failed to load the
	# AudioWorklet and ran audio-silent, in every version, undetected because
	# nothing asserted on it. Every web run now permanently captures console
	# errors and page errors (not just QA_-prefixed progress lines) and runs an
	# audio smoke after the QA script's own result: (a) fails loud if any
	# 'Failed to create PositionWorklet' / 'Unable to load a worklet' line
	# appeared anywhere in the run: (b) fails loud unless both worklet module
	# files (*.audio.worklet.js, *.audio.position.worklet.js) were requested and
	# returned 200 — tracked via the HTTP SERVER's own request log, NOT
	# Playwright's page.on('response')/CDP Network domain: verified empirically
	# that AudioWorklet module fetches are invisible to every Playwright/CDP
	# network event in headless Chromium (page.on('request'/'response'), a raw
	# CDP Network.enable session, and page.on('requestfailed') were all tried) —
	# they demonstrably reach the server (confirmed via the server's own access
	# log, 200 status) but never surface as a page-level network event, so the
	# server log is the only reliable source. The runner's exit code now reflects
	# BOTH the QA script's own result AND the audio smoke — a script can PASS its
	# own assertions while the run still fails loud on broken audio.
	# --touch mode (issue #105, the #106 prerequisite): runs the same DSL script
	# through a Playwright browser context configured with hasTouch:true, plus
	# one real page.touchscreen.tap() smoke after the script's own result is
	# captured (so it can't perturb assertions), proving Chromium accepts a real
	# touch dispatch on the real-server-hosted canvas with no page/console error.
	# This is groundwork only, not a new DSL tier: qa/test_driver.gd's click_*
	# actions (`_inject_mouse_click`) push InputEventMouseButton directly into the
	# SceneTree root's viewport — an engine-internal call that never touches the
	# browser/DOM, identically whether --touch is set or not — so no existing
	# script exercises the browser's real touch pipeline yet. What --touch mode
	# establishes: the harness can stand up a genuine touch-capable context
	# against the real-server build with zero regressions (`mouse_loop` is the
	# canonical proof — every click_* action still passes, driven engine-internal
	# as always), and that project.godot carries no `emulate_mouse_from_touch`
	# override (confirmed absent — Godot's default `true` applies), so a real
	# device touch auto-emulates a mouse event the SAME mouse-consuming code
	# (world.gd, hotbar, etc.) already handles — the "touch → mouse is free" win
	# the mobile-web exploration spec (docs/superpowers/specs/2026-07-12-mobile-
	# web-exploration.md) banks on. Issue #106 builds the actual `touch_*` DSL
	# tier (real per-step page.touchscreen.tap calls at live element coordinates)
	# on top of this. Runner-side only — no new manifest entries, no new scripts,
	# `--touch` is a plain runner flag threaded run_web_qa.sh -> run_web_qa.mjs.

Fresh project, ZERO known-harmless warnings. Any SCRIPT ERROR, Parse Error, or WARNING in any run = regression.

## Architecture

Full mechanism detail, rollout narrative, design rationale for every system: `docs/ARCHITECTURE-HISTORY.md`. This section current-state-only, one short paragraph per live system.

- **Sim core (`src/core/wi_game.gd`)** — `WIGame`, pure `RefCounted` sim. PURITY RULE: no autoload/Node/scene-tree refs in sim code, ever; dependencies injected (config dicts, event-sink `Callable`, seed). Keeps sim headless-testable, mass-simulatable.
- **ObservableBus (autoload)** — single pipe for every player-visible / QA-relevant domain event (`emit_domain_event`, JSONL log + signal). Never `print()` anything player should see; UI nodes render from bus events, emit `ui_*_rendered` confirmations back onto bus.
- **Game (autoload)** — owns `WIGame` instance, wires event sink to bus, owns save/load file I/O.
- **Presentation (`src/world/world.gd`, `src/ui/message_layer.gd`, `src/world/main.tscn`)**
  — thin: forwards input to `Game.sim`, renders state from bus events. All UI built in code; content in `data/*.json`. `main.tscn` = only hand-authored root scene (`project.godot`'s `run/main_scene`) — `src/world/world.tscn` orphaned byte-identical duplicate, nothing references it. Render pipeline (M5): 1280×720 window, 320×180 world `SubViewport` scaled 4×, centered (fractional `canvas_items` stretch, not integer). Environment schema (M5 E3): maps use `floor_layers`, `walls.segments` (walls-v2 records; `WIGame.segment_cells` = single cell-expansion source shared by sim blocking + tests), `decor`, `scatter`. Field held-key movement (2026-07-05): held keys re-issue `move_player` on each move-tween `finished` signal, gated same as fresh press; collapses to 0 duration under QA/headless — unobservable to tests. Full detail: "Presentation" + "Demo feel + render pipeline (M5)" + "Environment schema (M5 E3)" blocks in `docs/ARCHITECTURE-HISTORY.md`.
- **Field hotbar (`src/ui/field_hotbar.gd`, Three Pillars P2)** — overworld twin of combat action bar; shows PC's known field-tagged skills (`Game.sim.known_skills()` filtered by `skills.json`'s `field: true`), hides during combat, reuses `WIHotbar` for render. Number keys route through `world.gd` existing movement/interact gate. **Tab-primed select (issue #58):** Tab arms/disarms the same pad-cursor seam `[`/`]`/LB/RB already drive (`world.gd`'s `_field_slot_index`) — primed, arrow left/right cycle the highlighted slot (wrapping) instead of moving the player, up/down are swallowed (no drift while aiming), confirm fires the highlighted slot and cancel disarms (both pre-existing branches), and a direct number-key press also disarms for one-shot consistency. Every selection change draws a small floating `[Skill Name]` label above the highlighted slot (`display_name` is already bracket-formatted) and emits `UI_FIELD_HOTBAR_SELECTION_RENDERED` (distinct from `UI_FIELD_HOTBAR_RENDERED`, which means "the slot list changed" — this one means "the highlight changed").
- **TestDriver (autoload, `qa/test_driver.gd`)** — inert unless `--qa-script=...` passed. Executes declarative JSON playtests: injects real input, waits for bus events (`payload_contains` subset match), screenshots, asserts on `Game.sim.snapshot()`, drives fights via `combat_autoplay`, writes `result.json`. Per-script index: `docs/QA-SCRIPT-NOTES.md`; exact routes/assertions: `qa/scripts/*.json`; script list: canonical seed table below.
- **Combat (`WICombat`, `src/core/combat/wi_combat.gd`)** — pure per-encounter tactical sim: 12×8 grid, 4 AP/turn, precomputed initiative, seeded RNG, reaction hooks (riposte, momentum). Movement economy: `move_pool` (3 free steps/turn) spends before AP; `dash()` costs 1 AP for +3 pool. LoS = supercover raycast (symmetric, corner rule); line skills clip at first wall, hit everything in line (friendly fire real). MP: `max_mp = 8 + int/2`, granted only when combatant holds any `mp_cost` skill. **PC death = immediate defeat**, even with living ally (`_check_end` checks `pc.alive` first) — any future combat-data/sim change must re-check this failure mode; `defeat_ally_alive` = canonical proof. `WISkillEffects` = active-effect registry; `WICombatAI` = deterministic role AI (`melee`/`ranged`/`caster`/`inert` profiles — PC's own combatant config defaults to `melee`, so `combat_autoplay` never casts spell or fires line-skill refusal for player; see Gotchas). Presentation decomposed into components (M6.5): `combat_screen.gd` = only mode FSM + input dispatch; `board_renderer.gd`/`combat_view.gd`/`combat_playback.gd`/`combat_hud.gd`/
  `targeting_controller.gd` each own one concern — new presentation code goes in matching component, never back into `combat_screen`. Combat controls: arrows move active unit directly (move pool before AP), number keys = hotbar slots, Dash arms confirm gate (Enter/Esc), never fires instantly. Full detail: "Combat (M1)" / "Combat depth (M3)" / "Combat controls" / "Combat presentation components (M6.5)" blocks in `docs/ARCHITECTURE-HISTORY.md`.
  **Area terrain (GH#21, [Ice Floor]):** `WICombat.terrain` = `Vector2i cell -> {kind, expires_after_round, applies}` map, populated only by `WISkillEffects.resolve_active`'s `icy_floor` arm (Chebyshev-radius blast around TARGET's cell, walls/out-of-bounds excluded), purged in `_advance_turn` round-rollover branch. Standing on or stepping onto terrain cell applies its `applies` statuses via `_apply_terrain_status` (two call sites: `move_active`, `_start_turn`). **AI selects it since #90**: `combat_ai.gd`'s area_skill arm (`_act_area` in `_act_ranged`) casts icy_floor/blast_damage when the blast catches >=2 living enemies and zero allies — goblin_shaman carries it live; an enemy-cast zone is real. `ice_floor_loop` = player-cast canonical; `shaman_zone_loop` = the ENEMY-cast/area-arm canonical.
  **AoE blast damage (GH#71, [Flame Pillar]):** `blast_damage` effect type — instant damage sibling to `icy_floor`, sharing its exact area derivation (`WISkillEffects._radius_area`, renamed from `_icy_floor_area` when this task generalized it): Chebyshev-radius blast around the TARGET's cell, walls excluded, no new targeting mode (same enemy-cycling UI as `icy_floor`/`spell_damage`). Every living occupant of the area is hit regardless of side (friendly fire real, including the caster's own cell) via `_resolve_line_damage`'s multi-hit application shape — no terrain write, no status write, no expiry seam (contrast `icy_floor`, which glazes+slows for `duration_rounds`). First grant: fire_mage L11 `flame_pillar` (3 AP/5 MP, radius 1, range 3 — costs more than `flame_jet`'s 2 AP/4 MP single-line skill). No `icon` field (every fire-blast icon asset is already claimed by a shipped skill; degrades gracefully like the dozen other iconless skills, disclosed as an art-pass gap). **AI selects it since #90** (the same `combat_ai.gd` area_skill arm as `icy_floor`) — `rift_vermin_c` carries it live in the rift_vermin_leak/trapped_halls_snare rosters (both GATED bands re-verified). `flame_pillar_loop` = canonical proof.
  **Range+LoS attack seam (GH#70, [Archer]):** every combatant carries `weapon_range` (`WIKeys.WEAPON_RANGE`, defaults 1/melee; `wi_game.gd`'s `_build_player_combatant` threads it from the equipped weapon's items.json `range` field — PC-only today, no enemy carries a ranged weapon). `WICombat.in_weapon_range(attacker, target)` is the ONE gate both `attack()` and `skill_effects.gd`'s `damage_mult` resolver route through: `weapon_range <= 1` special-cases to the bare pre-existing `is_adjacent` check (no `has_los` test — melee never had one, and folding it into the general formula would let a diagonal-adjacent corner-wall case silently refuse a fight that used to connect); `weapon_range > 1` is `chebyshev <= weapon_range and has_los`. Byte-identical for every pre-GH#70 weapon (`sim_combat_batch.gd` proof). **Riposte gates on melee only** — `_resolve_hit`'s existing `melee` flag (physical-vs-magical math, not spatial) already excludes a bow's basic Attack from provoking counter_strike; a ranged hit still tallies `ranged_hit` unconditionally of that flag. Targeting UI (`targeting_controller.gd`'s Attack-slot candidate filter) widens/narrows the SAME distance check to the actor's own `weapon_range` instead of bare adjacency, `has_los` applied uniformly as before. Kit: `power_shot`/`quick_nock` reuse `damage_mult` verbatim (mult 2.0/0.7), `piercing_shot` reuses `line_damage` verbatim — zero new effect types. Earn: `ranged_hit` tallies on every landed hit from a `weapon_range > 1` combatant (`_resolve_hit`), OR banked directly by the barracks' `archery_butt` prop (item-gated on carrying any bow, `requires_weapon_family` — the #59 conversation item-gate's small parallel for a bare prop) — practice alone clears [Archer]'s `gained_by` threshold (3), no kill required, matching every other earned class's onboarding parity. `archer_earn_loop` = canonical proof.
  **Windup / telegraph mechanism (issue #82, boss design language):** `effect.windup_rounds` (only 1 supported) turns a skill into a two-beat DECLARE-then-RESOLVE: `use_skill` routes it to `WISkillEffects.declare_windup` instead of `resolve_active` — freezes the target cell set (blast/line derivation, `_radius_area` reused verbatim), spends the skill's own cost (a windup's `ap_cost` is typically the WHOLE turn — the "telegraphed AP spike"), emits `WINDUP_DECLARED {id, skill, cells}`, and deals NO damage yet. `WICombat.windups` (actor id -> `{skill_id, cells}`) parks the pending declare; `_start_turn` resolves it FIRST, at the caster's own next turn (`_resolve_windup`, the same multi-hit shape `blast_damage` uses but `melee=true` — STR-based, matching a physical attacker's own basic-Attack math, not blast_damage's INT-based spell default). **The caster is EXCLUDED from its own resolution, by id, never by cell** (F1 controller ruling): an adjacent range-1 declare geometrically always contains the caster's own cell and the melee AI never repositions after declaring, so caster-inclusion made every resolution a guaranteed self-hit — a tax, not a choice. Deliberate asymmetry vs player-cast `blast_damage`, which keeps caster-own-cell friendly fire as a chosen, card-warned risk; everyone ELSE in the frozen cells is still hit regardless of side. Counterplay is real, not flavor: whoever occupies the frozen cells AT RESOLUTION takes the hit, not whoever stood there at declaration — moving out whiffs it entirely. **Downed-clears is structural, not a special case**: `_advance_turn` already skips `_start_turn` for a dead combatant, so a caster killed before its next turn simply never resolves (no posthumous damage). `combat_ai.gd`'s melee profile gains ONE arm (`_windup_skill_id` + `windup_cadence`, a skill-level field): on a cadence round, adjacent, affordable — declare, ahead of the plain attack/power_strike branch. **[Dangersense]'s first real payoff**: the DECLARE's feed line + caster flash are universal (`WINDUP_DECLARED` rides `AI_PLAYBACK_TYPES` — the trap comment's own class, a mid-AI-turn event with a render arm); the frozen-cell board overlay (`board_renderer.gd`'s `add_terrain`/`expire_terrain` reused under a new `windup_danger` kind, PURELY presentation — no coupling to the sim's own `WICombat.terrain`) renders ONLY for a PC holding `dangersense` (`combat_playback.gd`'s enqueue-time `_pc_holds_dangersense` read), applied/cleared unconditionally outside `with_visuals` — the SAME "persistent state has no post-drain skip-resync" contract `TERRAIN_ADDED`/`TERRAIN_EXPIRED` established. Combat state (including `windups`) is NEVER save-serialized — verified (`WISave.serialize` has no `combat` field at all; a mid-fight save is structurally impossible), so no version bump was needed. First (and today only) holder: `slam` (`vault_construct`, the Guardian Construct boss, 8d C3) — `sim_combat_batch.gd`'s `vault_construct_t4_party` cell is GATED (0.55-0.95 win rate, 3-12 median rounds) against the real T4 Horns party.
  **AI profile variety (issue #83, gap-analysis: 28/33 combatants ran identical lowest-HP-melee AI):** three new `WICombatAI` profiles, all composed from EXISTING verbs (`attack`/`move_active`/`dash`/`use_skill` — no new effect types, no new `WICombat` state beyond the pure-derived `alive_allies_of`, the mirror of the pre-existing `alive_enemies_of`). **`skirmisher`** — attacks exactly like melee while it still has AP for another swing (STATELESS "already attacked" signal: `AP >= ATTACK_COST`, re-derived fresh every `_act_once` call, no new field), then spends leftover `move_pool` retreating from the nearest foe instead of standing in melee range. **`guard`** — when nothing is adjacent, its movement GOAL is its lowest-HP living ally's cell (`alive_allies_of`'s own hp-asc sort picks the ward), not the nearest/weakest enemy melee chases — body-blocks by positioning, not by any new redirect mechanic; once adjacent to a foe it fights exactly like melee (no windup arm — a future guard-profile windup holder needs the arm ported explicitly). **`coward`** — at/above `COWARD_FLEE_THRESHOLD` (0.3 of max_hp) is melee verbatim (including melee's own windup arm); below it, NEVER attacks: retreats from the nearest foe first, then (once no retreat step improves distance — cornered) rallies toward its NEAREST living ally instead (proximity-based, deliberately distinct from guard's HP-need-based pick). No new mid-AI-turn event types were introduced, so `AI_PLAYBACK_TYPES` needed no new entries. **Adoptions onto shipped rosters** (re-derived per the region-tier table, before/after logged in each combatant's own `_comment` + the relevant `sim_combat_batch.gd` doc block): `river_wolf_a`/`river_wolf_b` → `skirmisher` (`river_wolf_c`, the alpha, stays melee) — MEASURED-only, win_rate rose (safer, not more dangerous, is the expected direction for retreat-adding behavior). `rift_vermin_a`/`rift_vermin_b` → `coward` — GATED 0.55-0.95, held with margin (0.74→0.81 relc-cell). `ruin_ward_a`/`ruin_ward_b` → `guard` (the `ruin_guardian` escort roster ONLY, GATED 0.55-0.8) — `ruin_guardian_w8_relc` dropped 0.69→0.54, just under its floor; restored to 0.59 by nerfing `ruin_guardian`'s OWN con (28→24). **The one-id-two-contexts trap this adoption surfaced**: the `trapped_halls_snare` roster originally shared `ruin_ward_a/b` by id and inherited `guard` with them — but the snare has no boss to escort, so guard wards clustered around each other instead of engaging and eroded the FIGHT route's cost to 0.94 (the exact 8dC1 illusory-cost finding, second occurrence). No per-encounter AI override exists; the fix is the 8dC1 roster-only pattern — `snare_ward_a`/`snare_ward_b` (combatants.json), stat clones of ruin_ward with plain melee ai (a distinct id IS the override; mirror any future ruin_ward stat retune by hand, no inheritance exists in the catalog), swapped into the snare's `enemies` (the dungeon region's map file, data/maps/dungeon/) + `DUNGEON_CELLS` — restoring `trapped_halls_snare_t4_solo` to its pre-adoption 0.69/3r. `vault_construct_t4_party`/`_guided` (melee, untouched) stayed byte-identical throughout (0.86/6r, 0.78/6r). Two pinned-`rng_state` QA fixtures (`door_chain_fight_start`, `door_chain_sequence_break_start`) had their `ruin_guardian` leg flip from a win to a loss at the OLD seed after the retune — re-derived via `tests/_derive_rng_state.gd` to a new shared seed (both fixtures' own TRAP comments carry the story); `riverfarm_fight_start`'s river-wolf leg needed no re-derivation (the skirmisher adoption only made that fight safer).
- **Progression (`src/core/progression.gd`)** — ACCOMPLISHMENT-COUNTER driven (doing `[X]` things levels `[X]`, never chosen), resolved only at sleep beat, order: class gains → level-ups → consolidation offer → evolutions. Evolution at level 10 picks Replacement (dominant axis), Generalist grant (balanced, below dominance), or defers (Waiting). Consolidation offers `[Spellsword]` when two parent lines both qualify; refusable forever, round-trips in saves. **OPAQUE-UNTIL-SLEEP user-locked**: never render progress-toward text (no "3/12 uses", no percentages) — toasts/journal/prompts show RESULTS only. Full detail: "Action-driven classes (M6)" block in `docs/ARCHITECTURE-HISTORY.md`.
- **Story spine** — `WIDialogue` (`src/core/dialogue.gd`) = pure conversation-graph walker; possession-gated visible options (`requires`, hidden) vs retired options (`hide_when`, shown-but-inert); effects returned to `WIGame.dialogue_choose`, which applies them + re-gates ctx per node advance. `WIQuests` (`src/core/quests.gd`) — quest progress = pure function of accomplishment counters. `WISave` (`src/core/save.gd`) — versioned full-state round-trip (current `VERSION := 6`; `_migrated` composes each version step); file I/O only in Game autoload. Maps/doors = data (`data/maps/<region>/<map>.json`, composed by `WISceneCatalog` — issue #100 split; `start_map`/`player` in `data/scene_root.json`). Full detail: "Story spine (M2)" block in `docs/ARCHITECTURE-HISTORY.md`.
- **THE REQUEST BOARD (`WIBounties`, `src/core/bounties.gd`, M-DEPTH DP2)** —
  rotating bounty pool (`data/bounties.json`), deliberately NOT `WIQuests` quest (no journal entry, no beat text): `board_bounties()` derives active 2-3 postings from `times_slept % pool.size()` (zero rng, talk-pool rotation idiom at pool-window granularity). `accept_bounty`/`turn_in_bounty` (`wi_game.gd`) ride existing machinery only — plain accomplishment counter (`accepted_bounty_<id>`/`completed_bounty_<id>`) + shared `earn_gold` router; bounty condition (the `quests.json` complete_when dict shape) evaluates DELTA-SINCE-ACCEPT (current counter minus baseline snapshotted at accept), never absolute read. Board prop (`guild_board`, `board: true`) browse-only; accept/turn-in at Selys's desk (`selys_delivery.json` "Take on a posting."/"Turn in my posting." hub options) via TWO code-built `WIDialogue` graphs (`WIBounties.build_picker_graph`/`build_turnin_graph`) — real conversations, built at runtime from current slate, not static file, so existing dialogue-panel UI + QA `wait_for_event` idiom need no new code path. `WIGame._build_dialogue_ctx`'s `board_accepted` bool = SECOND sanctioned non-accomplishment `requires`/`hide_when` gate (after `gold`, Economy v1 D1) — `WIDialogue._meets`/`_progress_gated` recognize it explicitly. Full detail: "THE REQUEST BOARD (M-DEPTH DP2)" block in `docs/ARCHITECTURE-HISTORY.md`.
  **THE DELIVERY BOARD (M-DEPTH DP5)** rides same machinery over second pool (`data/deliveries.json`, Runner's Guild): same `active_slate` rotation, same `condition_met` (all-delta; `delivered_<id>` counter produced ONLY by `WIGame._check_delivery_arrival` — Chebyshev-1 adjacency to destination anchor from real `move_player`, parcel-in-pack guard; arrival removes parcel via `remove_item`/`item_lost`, sim's one inventory-removal seam). Sleeping on undelivered parcel FAILS run (parcel returns, no pay — this IS the abandon; no hand-back option by design). Vess's counter (`vess_counter.json` + `delivery_accepted`, FOURTH sanctioned gate) transacts; `runner_board` (`delivery_board: true`) browses. Full detail: "The Runner's Guild delivery loop (M-DEPTH DP5)" block in `docs/ARCHITECTURE-HISTORY.md`.
- **The Magical Door / portal menu (`WIPortals`, `src/core/portals.gd`,
  Magical Door plan Task D4, issue #8)** — pure STATIC-ONLY derivation + code-built `WIDialogue` graph (`WIBounties.build_picker_graph` precedent, not instantiated ARCH-4 sub-sim shape — class owns no WIGame field to mutate). `data/portals.json` rows (`{id, display_name, map, cell, requires_accomplishment, arrival_toast}`) = anchor-stone-per-region seam every future region milestone (#10/#12/#16) appends row to, zero code. `WIGame.attuned_destinations()` filters catalog by accomplishment gate; prop carrying `portal_menu: true` (`pantry_door` in inn, `street_anchor_stone` in street) opens `WIPortals.build_portal_graph()` once its `portal_menu_when` gate met (the `door_when`/`contains_when` idiom) — menu excludes anchor's OWN map, each anchor only offers somewhere else. Choosing destination fires `travel_to` dialogue effect, resolved by `WIGame._travel_to_portal` via `transition()` ONLY (O2 rule: `move_player`/`_check_trigger_radius` never touched, arrival can never trigger proximity fight — `portal_menu` = canonical proof). Awakening beat = sleep()-beat hook: with D3's three beat-3 counters banked, every further sleep banks PLAIN accomplishment counter `door_study_sleeps` (opaque-until-sleep — zero progress text, only Pisces's own `talk_pool_stages` lines shift, `pisces_magic.json`); at N=3 `door_awakened` banks, `sleep_veil.gd` catches (veil's fourth cameo, after class/level toasts, F1 opener, A4 epilogue) to queue GDI's own line under same black veil — door prop never speaks. Full detail: "The Magical Door / portal menu (Task D4)" block in `docs/ARCHITECTURE-HISTORY.md`.
- **Character creation (`char_creation.gd`, M-ARC §5, picker recomposed issue
  #42)** — New Game → one-step 2x3 sprite-picker grid (six idle-animated AnimatedSprite2D PC variants via `WISpriteRegistry`: Human/Drake/Gnoll × Male/Female) → name. Picking card sets `pc_race`+`pc_gender` together. Three cosmetic sim fields (`pc_name`/`pc_race`/`pc_gender`), additive save, no mechanical effect. QA auto-skips creation with everyman defaults unless script opts in. Full detail: "Character creation (M-ARC §5)" block in `docs/ARCHITECTURE-HISTORY.md`.
- **Equipment & Gear** — pure sim state, `WIGame.inventory` (ids) + `equipped` (5 keys: `weapon`, `armor`, `accessory_1/_2/_3`, per M-GEAR G1 2026-07-06) + `container_state`. Combat reads equipment once at `start_combat` build (weapon-family-gated kit, `damage_mod`/`hp_mod`/`damage_reduction`). Loot RNG isolated from live sim stream (`hash(run_seed, encounter_id)`-derived) — never shifts combat seed. `resonance_capacity` (default 2) caps total accessory resonance; refused equips get diegetic placeholder toast. Inventory UI: `src/ui/inventory.gd`. **Consumables (issue #92 R1/R2):** an item's `use_effect` field (`{"heal": N}` or `{"next_fight": {mod_dict}}`) resolves through the ONE new pure resolver, `WIItems.resolve_use` (`src/core/items.gd`) — heal is combat-only (`WIGame.combat_use_item`, reuses `WISkillEffects`' own "heal" arm via a synthetic skill dict, flat 1 AP), next_fight is field-only (`WIGame.use_item`, banks `pending_meal` — a ONE-SHOT buff dict folded into `_build_player_combatant`'s build at the very next `start_combat` then cleared, generalizing `well_fed`'s own +2 hp_mod precedent). Consumption = `inventory.erase` (items never stack) — bought → carried (one) → used → gone → repurchasable, the real sink R5 wires a vendor onto. Combat hotbar surfacing: a heal-shaped carried item is ALWAYS opt-in via `hotbar_loadout`'s shared array (`item:<id>` tokens, toggled from the inventory panel's confirm key exactly like journal.gd's skill toggle) — never auto-shown in AUTO mode (an open-ended item catalog would otherwise reflow/renumber skill slots unpredictably); item tokens are filtered out before the skill side's own `apply_loadout` call, so toggling an item never blanks an un-curated skill kit. Combat dispatch reuses Dash's own niladic no-target confirm gate (`Mode.DASH_CONFIRM`, generalized — `_confirm_bar_action`/`_cancel_bar_action` in `combat_screen.gd`). Full detail: "Equipment (M7)" + "M-GEAR (resonance gear)" blocks in `docs/ARCHITECTURE-HISTORY.md`.
- **Atmosphere / lighting / ambience (`src/world/atmosphere.gd`,
  `src/world/ambience.gd`, presentation-only)** — per-map/per-phase mood color grade (`data/moods.json`, ship-neutral-first: identity `[1,1,1]` everywhere except tuned rollouts) via single `CanvasModulate`; per-arena overrides for arenas that shouldn't inherit field grade (e.g. `cave_mouth`, always dark). Lights (`PointLight2D`, phase-gated energy, ≤8/map budget) + ambience emitters (`GPUParticles2D` presets, hard on/off per phase, ≤6/map) register with `atmosphere.gd` — single `apply()` refreshes color + every light + every emitter together. Three small canvas shaders: foliage sway (always-on), water shimmer (floodplains pond overlay), vignette (identity 0.0 everywhere today). Field name tags RETIRED (R3) — combat name tags gone too, HP/MP numeral readout stays; `WIWorldLabels` survives combat-stats-only. `visual_states` seam lets prop/npc swap sprite/tint/light on accomplishment counter or container-opened flag (e.g. `dirty_table` cleaning up, `unlit_lantern` lighting). Full detail: "Atmosphere / mood grade (B1)" / "Light layer (B2)" / "Ambience layer + shaders (B3)" / "Atmosphere rollout R1/R2" / "Label removal + visual affordances (R3)" blocks in `docs/ARCHITECTURE-HISTORY.md`.
- **Legibility formatter (`WIEffectText`, `src/core/effect_text.gd`,
  M-LEGIBILITY)** — the ONE formatter for every player-visible mechanical line (item cards, skill/hotbar cards, status glossary). Pure/static, VISIBLE-CURRENCY only (HP/MP/AP/dice/cells/gold/rounds; raw stats + percent-toward forbidden in anything it emits) — rendered line can never drift from data it describes. Skills Wave Task K4 wired real sim consumers for `heal` (second_wind self-only heal, capped at max_hp) and `move_pool_bonus` at 0 AP cost (quick_movement/battlefield_awareness, real turn-start passive) — both cards un-suppressed now. GH#21 wired last ghost skill, `icy_floor`: "new cell-targeting mode" K4 worried about turned out unnecessary (cast still targets combatant id, same as every other active skill — area derived FROM that target's cell, not aimed at bare cell), card now generates "N AP, N MP — glaze an RxR patch of ground at range R for N rounds. Slows." from `effect.{range,radius,duration_rounds,applies}`. Full detail: "M-LEGIBILITY (L1-L5)" and "GH#21 [Ice Floor] area terrain effect" blocks in `docs/ARCHITECTURE-HISTORY.md`.
- **Art pipeline (`WISpriteRegistry`, `src/world/`, M4)** — builds SpriteFrames/TileSets from `data/sprites.json`/`data/biomes.json`. Committed curated extracts in `assets/` (licenses in `assets/LICENSES/`); browse via `docs/asset-index.md`, never by loading PNGs into context. Any tile/sprite region pick must be verified by windowed screenshot. Full detail: "Art pipeline (M4)" block (layout specifics superseded by M5 render pipeline above, mechanics still apply) in `docs/ARCHITECTURE-HISTORY.md`.
- **Settings + accessibility (`WISettings` autoload `src/ui/wi_settings.gd`, `src/ui/settings_panel.gd`, issue #77)** — `WISettings` owns fullscreen/text-scale/reduce-motion (persisted via `ConfigFile` at `user://settings.cfg`, section `video`/`accessibility`; Master/Music/SFX volume stays owned by `WIAudio`, section `audio`, SAME physical file — every writer reloads-from-disk immediately before its own `.save()` so the two independent `ConfigFile` objects can never clobber each other's section). Config-file idiom, NOT save data: never touched by `WISave`/`Game.load_slot`/`Game.reset`, so every setting survives New Game/defeat-reload/save-load untouched by construction. `WISettings` is deliberately autoload-reference-free (no bare `ObservableBus`/`Game`/`WIAudio` identifiers) so it stays directly `load()`+`.new()`-able under bare `--script` tests (`tests/test_settings.gd`) — the SAME reason it can never run inside a `--script` SceneTree test in the first place (autoloads aren't instantiated there — see Gotchas), which is WHY `test_copy_fit.gd`'s font-size==14 settle is safe unconditionally, not just when `settings.cfg` is absent. Text scale (100%/115%/130%, `TEXT_SCALE_STEPS`) mutates `UIChrome.THEME`'s `default_font_size` + every named type-variation's `font_sizes/font_size` directly, always re-derived from the BASE ints (never the theme's current value, so repeated cycles can't rounding-drift); message_layer.gd's/combat_hud.gd's wrapped-line budget math reads `label.get_theme_font_size(...)` live at render time, so it recomputes correctly with zero wiring in either file — the panel WIDTH/HEIGHT pixel budgets (TOAST_TEXT_WIDTH, READOUT_TEXT_WIDTH/HEIGHT, etc.) are DELIBERATELY left untouched (they're the real fixed on-screen rect, verified against each panel's own build code; rescaling them would tell the wrap math there's more room than the panel actually has). `settings_panel.gd` (reachable from BOTH `pause_menu.gd`'s and `title_screen.gd`'s own "Settings" row, each spawns/owns a fresh instance every swap — see main.gd's `_spawn_title`/`_spawn_ui_layers`) is the shared UIChrome row-list panel (pause_menu.gd's exact idiom) plus a Controls reference sub-page built FROM `WIInputHints.LABELS` (kb/pad columns) + a small local mouse-affordance table — can never drift from `test_input_hints.gd`'s pinned glyph table. Tutorial-hint replay: `message_layer.gd`/`combat_screen.gd` each expose a static `reset_hints()` (called via `load(path).call("reset_hints")`, never a compile-time reference — both files carry bare autoload identifiers) that clears the SAME process-lifetime one-shot flag their own GAME_RESET listener already re-arms — presentation-only, never touches an accomplishment counter. Reduce-motion gates board_renderer.gd's shake/flash juice family (`shake_board`/`impact_flash`/`flash_chip`/`flash_cells`) through ONE shared `_reduce_motion()` check (folded into the existing `_juice_enabled()` QA-collapse gate for the first two, a direct early-return for the latter two). `settings_loop` = canonical proof (title boot path + in-game pause, no fixture needed).
- **Save-slot picker + defeat interstitial (issue #78)** — `Game.MANUAL_SLOTS` (`src/core/game.gd`) widened save/load from one hardcoded "manual" slot to 3 (`"manual"`/`"manual_2"`/`"manual_3"`, "manual" first so it's still the picker's default row-0 selection). NO save-format change: `WISave.metadata(data)` (`src/core/save.gd`) is a pure read function (not a persisted field) deriving `{pc_name, top_class, top_level, map}` straight from fields the save already carries, `_migrated()`-tolerant of the same version drift `apply()` handles, never touching a live `WIGame`; `Game.slot_metadata(slot)` peeks a save FILE the same way (parse -> `WISave.metadata` -> stamp `mtime`) without applying it. pause_menu.gd's "Save"/"Load" rows (ROWS indices 1/2, UNCHANGED — sub-menu, not a new top-level row) now open a 4-row picker (3 slots + "Back") showing each slot's metadata summary or "Empty"; title's "Continue" keeps its OLD direct newest-wins-load mechanics unchanged (zero index/flow re-pin) but gained a small caption underneath previewing what it will load (`_refresh_continue_caption`) — a deliberate scope split: the interactive multi-slot picker lives in pause (where slot choice matters, reachable in-game), Continue stays the one-keypress fast-resume. `UI_SLOT_PICKER_RENDERED`/`_HIDDEN` are the new confirmations (fires once per open, payload carries the full 3-slot summary array). Defeat: `Game.load_slot(slot, reason)` gained an optional `reason` param riding `GAME_LOADED`'s payload — ONLY combat_screen.gd's true-defeat branch of `_close_banner` passes `"defeat"` (never the voluntary Abandon/pause-Load/title-Continue paths); `main.gd`'s `swap_to_world(new_game, defeat_reload)` reads it and calls `sleep_veil.gd`'s NEW FOURTH mode, `play_defeat()` — presentation ONLY, over the already-complete `resolve_combat -> teardown_board -> load_slot(...,"defeat")` sequence (no restructuring of the defeat flow itself; the slot name itself changed under issue #88, see below). Reuses the SAME black-veil/line-reveal machinery as the opener/epilogue (diegetic register: "[Defeat.]" / "You wake at <Map>, the fight undone." / a retry nudge), then a REAL terminal choice (not an auto-fade) — Confirm dismisses into the reloaded world, Cancel goes to the title screen — via a dedicated `_defeat_choice_pending` await, deliberately NOT sharing the opener/epilogue's skip-only `_opener_advance` flag. QA/headless collapses to an instant `UI_DEFEAT_VEIL_RENDERED{lines,map}` coverage emit with NEITHER beat run (same `_is_qa()` contract as every other veil mode) — the interactive Confirm/Cancel choice is human-windowed-playtest-gated, same precedent as the opener/epilogue's own skip-ability. `defeat_reload` = canonical proof (asserts `reason:"defeat"` + the coverage event, still lands in the reloaded world exactly as before this task).
- **Onboarding & save safety (issue #88, gap-2)** — three independent fixes to a single "progress destroyed unpredictably" class of bug. (1) **Defeat rewinds exactly the fight**: `game.gd`'s `_on_domain_event` writes a DEDICATED `auto_pre_combat` slot the instant `COMBAT_STARTED` fires (WISave never serializes `combat`, so writing here is equivalent to writing strictly before `sim.combat` is built) — `combat_screen.gd`'s true-defeat branch now loads `auto_pre_combat` (was `auto`), so a loss always rewinds to the exact pre-fight cell, making sleep_veil.gd's "the fight undone" copy literally true. `auto` itself is UNTOUCHED by this (Abandon/"Load Autosave" in pause_menu.gd keep reading the general last-checkpoint slot, unchanged — `combat_abandon.json`'s own pin proves it). **DIALOGUE-COMMITTED fights snapshot EARLIER (fix wave, the #89 cross-lane guard)**: `WIGame.dialogue_choose` emits `PRE_COMBAT_CHOICE {encounter}` BEFORE its effects loop whenever the chosen option carries a `start_combat` effect (pure — an `_emit` through the injected sink, no autoload touch) — game.gd snapshots on THAT, so the committing choice's own accomplishments land INSIDE the rewind (relc_descent: lose [Go together.] → `relc_joined_descent` is GONE; a re-chosen [I go alone.] banks `went_alone` with NO Relc fielded — without this, #89's solo-praise dialogue variant could praise a solo that never happened). Double-write guard: `_choice_snapshot_armed` makes the same fight's COMBAT_STARTED skip its post-effects write (which would resurrect the leak); disarmed by `DIALOGUE_EFFECT_FAILED{start_combat}` when the deferred start is refused — wi_events.gd's PRE_COMBAT_CHOICE doc pins the exactly-one-synchronous-follow-up contract. Relies on the EXACTLY-ONE-VERB effect-dict invariant, now ENFORCED by `test_content.gd`'s `_validate_effect` (`DIALOGUE_EFFECT_VERBS`). RNG asymmetry, both CHOSEN: the COMBAT_STARTED snapshot's rng_state is ONE DRAW PAST pre-combat (start_combat's combat-seed `randi()` already consumed) so a retry REROLLS the fight; the PRE_COMBAT_CHOICE snapshot is PRE-DRAW, so an identical re-choice replays the same seed. `relc_descent_rewind` = the fix wave's canonical proof. `PHASE_CHANGED` (fires unconditionally on every sleep, even a grant-less one, per wi_game.gd's `sleep()`) was added to the general `save_auto()` trigger list, closing the "slept, banked nothing level-shaped, then died and lost the whole waking" gap. (2) **New Game stops clobbering**: `title_screen.gd`'s "New Game" row now opens an overwrite-confirm panel (reusing `_format_slot_summary`, the Continue-caption idiom) whenever `_newest_save_slot()` is non-empty; collapses instantly under `_is_qa()` (the sleep_veil.gd precedent — every existing canonical's New Game keeps working unconditionally, auto-resolving to "Yes", with the `UI_NEW_GAME_CONFIRM_RENDERED` coverage event still firing for QA to pin; a human windowed playtest is what exercises the real Confirm/Cancel, same as every other veil mode). `game.gd`'s `reset()` also arms `_rotate_auto_pending`, consumed by the FIRST `save_auto()` after — rotates the OLD `auto` to `auto_prev` (a byte-copy, `install_fixture_save`'s copy shape) before the fresh run's own first autosave overwrites `auto`, so a New Game never silently destroys the previous run's last checkpoint outright (one hop of history survives). Neither `auto_pre_combat` nor `auto_prev` is ever offered by `_newest_save_slot()`/pause's slot picker — same "hidden bookkeeping slot" precedent as the `playtest` slot. (3) **classless_solo measured cell** (`sim_combat_batch.gd`) — zero classes + solo (no Relc) crossed against the `goblin_ambush` composition (= `goblin_encounter_1`'s own roster): MEASURED win_rate 0.07, the strictly-worst reachable first-20-min matchup, never gated. `goblin_ambush_tutorial`'s `real_ones` tutor line gained a SECOND-level fallback split (`requires_ally`+`solo_fallback_line`, `combat_hud.gd`'s `_tutor_line_text`) — when classless AND Relc is structurally absent (`ally_requires` unmet), the line drops Relc's voice entirely for an unattributed system/journal line reusing `data/help_content.json`'s "Classes & Levels" body VERBATIM (`test_content.gd`'s `_validate_tutor_line_help_consistency` is the drift tripwire). `defeat_reload` extended: a proximity-triggered `goblin_encounter_1` leg (before Relc is ever met) proves a reload landing squarely inside a still-live `trigger_radius` never insta-refires (`_check_trigger_radius` runs ONLY from `move_player`, never from a `load_slot` restore) — `assert_event_count combat_started` is the precise proof. `title_flow` extended: a second New Game (once `auto` exists) pins the confirm panel's coverage event + exact summary text, the collapse-bypass (no hang), and the `auto_prev` rotation.
- **Audio (`WIAudio` autoload `src/audio/wi_audio.gd`, data `data/audio.json`, issue #76)** — every sound is data (`events`: `skill_resolved`/`attack_resolved`/etc. keyed rows, subset-payload matched; `music`: `map_changed`/`combat_started`/sting rows, FIRST-MATCH-WINS so an arena-keyed combat variant must precede the generic `music_combat` fallback). **Round-robin `variants`** (footstep surfaces, PC hurt barks): a presentation-side counter (`_variant_index`, keyed by id) advances one entry per play, wrapping — deterministic, never the sim's own seeded RNG (audio variety isn't gameplay); `stream` must equal `variants[0]` (drift tripwire, `test_audio_data.gd`). **Ducking**: `DIALOGUE_STARTED`/`DIALOGUE_ENDED` tween the "Music" AudioServer bus itself (not a per-player volume_db) down by `MUSIC_DUCK_DB` (-6dB, `MUSIC_DUCK_FADE_SEC` 0.2s) relative to the user's OWN slider (`get_bus_volume`, never the bus's current reading, so repeat cycles can't drift); a re-entrancy depth counter (`_duck_depth`) means nested duck requests share one duck. Headless sets the target dB directly (no Tween — headless frame deltas aren't wall-clock-paced, so a real Tween settles unpredictably depending on `wait_frames` count), provable via `assert_audio_bus_volume_db` (raw dB, `qa/test_driver.gd`, distinct from `assert_audio_bus_volume`'s 0-10 slider-formula variant). **Vault boss music** (`music_combat_vault`) rides the SAME pre-existing arena-keyed `combat_started`/`{arena}` seam as `music_combat_deep_warren` etc. — no new mechanism, just another data row ahead of the generic fallback. **Wordless combat barks** (`pc_hurt`/`pc_death`, Super Dialogue Audio Pack v1, Dillon Becker, CC BY 4.0 — credited in `ATTRIBUTION.md`) ride a dedicated `Voice` bus (`BUS_NAMES`' 4th, untouched by the Music duck or the SFX slider) — PC-only this wave (Erin is never a combat `id`; no payload distinguishes a goblin from any other enemy). `delve_fight`/`defeat_reload`/`upstairs_walkthrough` carry the live proofs (vault music + windup tell, defeat stinger + ducking + pc_death, sleep-beat stinger respectively).

- **World reactivity (issue #80)** — the ctx `phase` key: `WIGame._build_dialogue_ctx` carries `"phase": phase()` (the EIGHTH sanctioned single-key `requires`/`text_variants` gate, `WIDialogue._meets`'s `phase` arm — mirrors `race`'s placement/semantics exactly: visible-locked not hidden, NOT in `_progress_gated`), value shape `{"phase": [<phase strings>]}` (encounter_when/visual_states' own array-membership convention, not race's bare string). Phase CANNOT change mid-conversation (`_tick_action` only fires from move_player/interact/a PC combat turn, none of which run while `dialogue != null`) — a node's variant stays fixed for the whole conversation. Dusk/night `text_variants` shipped on 10 majors' hubs (Erin/Selys/Relc/Krshia/Olesm/Pisces/Zevara + region anchors Eloise/Wilovan/the Pallass tier clerk), always inserted FIRST in each array (lowest priority — a real progress-based reaction still wins at night, "later match wins"). **Presence** (`WIGame.entity_present`/`_present_gate_met`) — `present_when` gained the SAME `{"phase": [...]}` shape `_encounter_gate_met` already used (door_when/contains_when/portal_menu_when stay `requires`-only, untouched); `hungry_patron`/`ilvo`/`resting_runner` (pure FLAVOR NPCs, never a quest-giver/vendor) carry `present_when:{phase:[day,dusk]}` and are genuinely absent at night (walkable, not just invisible — `entity_at`/`is_cell_blocked` both read live). **SAME-MAP GHOST, FIXED (GH#104, follow-up to #80)**: unlike every prior `present_when` producer (cross-map only), the phase clock advances on WHATEVER map the PC stands on — a present_when-gated entity's render used to only reconcile EXISTENCE on `MAP_CHANGED` (a real door crossing, or a QA-only same-map `teleport`), leaving a stale sprite (or a stale gap) until the player next left and re-entered that specific map. `world.gd`'s `PHASE_CHANGED` handler now also calls `_reconcile_entity_presence()` (~20 lines): diffs every on-map `present_when` entity's live gate against `_entity_visuals`, frees the newly-absent and builds the newly-present via the same `_make_entity_visual` construction `_build_entities` uses, and re-emits `UI_ENTITIES_RENDERED` when the count actually changed — no MAP_CHANGED required, live the same beat the sim-side `is_cell_blocked`/`entity_at` flip already was. Distinct from `_refresh_entities_watching_phase` (LOOK-only re-render of an already-built visual, e.g. the witch's elder/young swap) — both fire off the same PHASE_CHANGED hook, never merged (see `entity_present`'s own doc comment for the full trace). A `requires`-gated producer (ceria_inn et al.) still needs a DIFFERENT-map accomplishment bank — this fix only closes the phase-shape's same-map case, no reconciler runs on `ACCOMPLISHMENT_RECORDED`. **Night texture**: ONE new night-only `encounter_when:{phase:[night]}` slot per outdoor region beyond the shipped `river_wolf_pack` precedent — `goblin_night_patrol` (floodplains, goblin_raider+goblin_shaman+relc, arena `goblin_ambush`) and `boulevard_night_footpads` (invrisil_boulevard, footpad_lookout+footpad_bruiser solo, arena `mercantile_alley`) — both `respawns:true`, `trigger_radius:1`, MEASURED-only in `sim_combat_batch.gd` (a night ambush by design, not a win-rate contract, same as `river_wolf_pack`). `street`/`mercantile_alleys`/`pallass_market` were assessed and EXCLUDED (too heavily pinned by existing canonicals, or not genuinely outdoor). `atmosphere_check` extended with both proofs live (Erin's dusk/night line + hungry_patron's day→night→day presence flip, sim- and render-side); `goblin_night_patrol_loop`/`boulevard_night_footpads_loop` are dedicated fixture-based canonicals for the two new slots (`goblin_night_patrol_start`/`boulevard_night_footpads_start`, `actions_since_sleep` pre-set past `night_at` — no in-script grind needed).

**Canonical QA seed table** (source of truth: `qa/manifest.json` — `ci_sweep.sh` verifies this table against it). Run any script: `qa/run_qa.sh <script> headless --seed=<seed>` (fixture-based scripts load save via title Continue whose own `rng_state` OVERRIDES `--seed`; seed shown below = pinned/held either way). Full sweep: `qa/ci_sweep.sh` (parses `qa/manifest.json`; hard-fails if this table drifts from it — keep both in sync when adding script). Two screenshot-only utilities (`title_peek`, `street_peek`) excluded, not canonical. Compact generated inventory: `docs/QA-SCRIPT-NOTES.md`; exact route/assertions: `qa/scripts/<name>.json`.

| script | seed | purpose |
|---|---|---|
| `load_gate` | none | native-only resource compile/load gate |
| `inn_walkthrough` | 9 | full inn journey, no screenshots in headless; issue #40's canonical diagonal leg (move_diag out-and-back, net zero) opens it |
| `map_transition_fade` | 9 | #87 live inn-door crossing; headless collapse + opt-in paced transition frames |
| `dialogue_walkthrough` | 9 | Erin/Selys story path (meets Relc, then `goblin_encounter_2`) |
| `dialogue_hub_loop` | 9 | conversation hub loop-backs + `hide_when` sweep |
| `quest_errand_fight` | 9 | errand path with a goblin fight |
| `quest_errand_parley` | 9 | errand path through parley |
| `save_load_roundtrip` | 9 | save/load persistence path |
| `combat_walkthrough` | 9 | core combat proof; `ally_requires` roster gate negative-then-positive |
| `tutorial_flow` | 9 | the onboarding's own end-to-end proof (spar → gift → ambush → street) |
| `level_up_loop` | 9 (fixture `post_tutorial`) | level-up loop; fight 2 tests riposte |
| `mage_unlock_loop` | 9 | [Mage] earned from Pisces; full cold-start arc + mage kit fielded |
| `line_of_sight_denial` | 9 | wall-aware ranged AI proof (positive `has_los` gate) |
| `defeat_reload` | 2 | losing seed; defeat loads the pre-combat snapshot (`auto_pre_combat`), no `game_reset`; issue #88: a proximity-triggered `goblin_encounter_1` leg (classless_solo) proves a reload landing inside a still-live `trigger_radius` never insta-refires, then the original interact-triggered `chieftains_raid` leg (Relc fielded) |
| `relc_descent_rewind` | 1 (fixture `relc_descent_rewind_start`) | issue #88 fix wave: THE `pre_combat_choice` canonical — a dialogue-committed fight's defeat rewinds the committing choice too (lose [Go together.] → `relc_joined_descent` GONE by whole-dict assert; the [I go alone.] re-choice banks `went_alone` and the pinned solo initiative order fields NO relc — the #89 composition guard) |
| `defeat_ally_alive` | 3 (fixture `near_defeat`) | THE canonical proof of the PC-death-is-instant-defeat rule |
| `title_flow` | 9 | title screen flow; no combat; issue #88: a second New Game (once `auto` exists) pins the overwrite-confirm panel's coverage event + exact `_format_slot_summary` text, TestDriver's `_is_qa()` collapse-bypass (no interactive hang), and the `auto`->`auto_prev` rotation on the next autosave |
| `playtest_boot` | none | Issue #43: debug-only title 'Playtest States' picker -- boots `near_garden` via the real fixture-copy-then-Continue path |
| `combat_move_input` | 9 (fixture `post_tutorial`) | movement-first arrows + Dash refill via real input |
| `combat_touch_input` | 9 | Issue #106 (mobile web v1): combat touch event parity -- adjacent-cell tap = arrow-move equivalent (incl. diagonal/far/pool-empty no-ops), Dash armed then a confirm-chip tap fires it (byte-identical to Enter), Attack targeting opened then a tap missing every candidate cancels (Esc-equivalent, AP unspent), reopened Attack + a tap on the non-default candidate (training_dummy_b) re-points the aim then a confirm-chip tap fires on it specifically |
| `combat_abandon` | 9 (fixture `post_tutorial`) | pause opens mid-fight (HOTBAR resting mode), Abandon returns to last autosave; combat_resolved never fires |
| `class_evolution_loop` | 9 (fixture `near_evolution`) | grind → sleep → `class_evolved` + evolved kit (#123: fixture honestly banks met/sparred_with_relc — Relc allies straight off the fixture, fight3 runs the sleep-nudge pool stage + sparred-variant `relc_intro` with the spar at index 2 of 4, spar win re-banks sparred_with_relc → 2) |
| `cook_chef_loop` | 9 (fixture `near_cook_chef`) | Cook threshold → `class_evolved` to [Chef] |
| `mixer_alchemist_loop` | 9 (fixture `near_mixer`) | Wave D-1 (#155): [Mixer] bench casts → `class_evolved` to [Alchemist] → [True Synthesis] two-component consume (the String\|Array `requires_item`/`remove_item` seam) |
| `runner_courier_loop` | 9 (fixture `near_runner_courier`) | delivery threshold → `class_evolved` to [Courier] |
| `hedge_witch_loop` | 9 (fixture `near_hedge_witch`) | fixture-banked [Hedge Witch] gain → [Witch] + combat kit |
| `necromancer_loop` | 9 (fixture `near_necromancer`) | fixture-banked [Necromancer] gain + L3 combat kit |
| `scout_consolidation_loop` | 9 (fixture `near_scout_consolidation`) | rogue/archer lines → [Scout] floor + combat kit |
| `beast_tamer_loop` | 7 (fixture `near_tamer`) | Wave D-2 (#156): wounded-corusdeer soothe → `class_gained` [Beast Tamer] at sleep; `once_per_waking` grind guard |
| `tamer_bond_loop` | 7 (fixture `near_tamer_bond`) | Wave D-2 (#156): [Lesser Bond] wolf tame → crab refusal joke → boon-carrying roster fight → tamed bond persists sleep |
| `druid_consolidation_loop` | 7 (fixture `near_druid_consolidation`) | tamer/mage lines → [Druid] floor 14 + [Peace of the Wild] kit |
| `gh330_companion_gate_loop` | 7 (fixture `near_tamer_bond`) | GH#330 R4: `present_when.companion` as a PAIR — the wolf's cache cell is walkable with no bond, blocking with the wolf, walkable again with the razorbeak; plus the pond vantage's lead |
| `gh330_deer_arc_loop` | 7 (fixture `gh330_deer_arc_start`) | GH#330 R3: the wounded corusdeer stands (corusdeer_doe rig), heals, and leaves — worn grass + one-time shed antler, then corusdeer_range's ungated vouched soothe |
| `gh330_lamb_pen_loop` | 7 (fixture `gh330_lamb_pen_start`) | GH#330 R2: the hunter's lamb pen as a [Beast Tamer] door outside the floodplains — wool yield, waking cap, class gain at sleep, then the `tended_beasts` variant |
| `room_upgrade_loop` | 7 (fixture `near_room_upgrade`) | GH#92 D3: Erin tier-1 room purchase → gold spend + `room_tier_1` bank (HP step pinned in test_sim_core) |
| `watch_donation_loop` | 7 (fixture `near_watch_donation`) | GH#92 D3: Ashgrave supply-fund donation → repeatable sink banks `donated_watch_supplies` |
| `dialogue_numkey_loop` | 7 (fixture `near_room_upgrade`) | GH#171: number keys pick numbered dialogue options; no field-cast leak |
| `hedault_enchant_loop` | 7 (fixture `near_hedault`) | GH#142: enchant swap — fee, base consumed, variant granted |
| `bed_nudge_loop` | 9 (fixture `pre_first_sleep`) | GH#130 bed nudge fires pre-sleep, retires after (`slept` counter) |
| `raskghar_entry_loop` | 9 (fixture `raskghar_entry_start`) | GH#152 Act III entry chain, no hand-banks past the report |
| `consolidation_flow` | 9 (fixture `near_consolidation`) | offer/decline/re-offer/accept → [Spellsword] |
| `save_migration` | 1 | v2→v3 migration + v1 pause-load rejection + stale-slot defeat-reload |
| `save_rename_migration` | 1 (`legacy_seed` `near_pallass`) | #111 safe project rename: run_qa.sh's `legacy_seed` seeds a PRE-rename user dir (the `Wandering Inn RPG v4` app_userdata sibling) with a real save + settings.cfg BEFORE launch; `Game._ready` carries it into the fresh `user://` before the title enumerates, so Continue lights up on first launch of the renamed build and loads byte-faithfully (guild/(8,3)/60g). Pure copy/no-clobber/sibling-derivation unit-pinned in `test_save_rename`; every other canonical (no `legacy_seed`) proves the no-op boot path |
| `consolidation_reload` | 9 | pause-load reconstructs the consolidation prompt mid-offer |
| `generalist_loop` | 9 (fixture `near_generalist`) | Generalist evolution path + balanced-mastery grants |
| `lantern_check` | 9 | [Light] `lit_the_common_room` payoff, after the Pisces mage arc |
| `gate_district_walkthrough` | 9 | enters street for real via `liscor_gate`; gate NPCs + Selys |
| `relc_tutorial` | 9 | full `relc_spar` tutor_lines beats + opacity/persistence teeth |
| `work_loop` | 9 | inn work-loop, Helper leg (chores → `class_gained` → level 2) |
| `crate_fight` | 9 (fixture `post_tutorial_street`) | "Missing Crate" FORCE path; issue #69's Klbkch ally-gate roster proof (`chatted_with_klbkch` fields him, not Relc) |
| `crate_talk` | 9 (fixture `post_tutorial_street`) | "Missing Crate" WATCH path (no combat) |
| `d2_shop_shot` | 9 | shop-panel geometry canonical (findings 11/13): the charms menu caps at 684 + scrolls (`panel_capped` pin) — promoted from peek-orphan after the unbounded panel trapped a playtester |
| `crate_light` | 9 | "Missing Crate" SKILL path ([Light]-studies `cellar_door`) |
| `journal_skills` | 9 | journal skills-by-class panel; pre/post-first-use reveal |
| `inventory_loop` | 9 | THE inventory/equipment loop; weapon-gated kit proof |
| `atmosphere_check` | 9 | mood-grade/phase-clock proof; day→dusk→night→day cycle; issue #80: Erin's phase-gated line at dusk + hungry_patron's present_when day↔night flip (sim- and render-side); GH#104: the SAME-MAP flip leg (no teleport) proves the live PHASE_CHANGED existence-reconciler |
| `field_skills_loop` | 9 (fixture `near_tactician`) | field-skill hotbar loop (Basic Cleaning → Observe); K2b loadout assign/unassign + remapped-slot proof |
| `hotbar_tab_loop` | 9 (fixture `near_tactician`) | Issue #58: Tab-primed field-hotbar select -- prime/re-prime, wrap-in-place move_right with player_cell unchanged (arrows captured while primed), Esc disarm, confirm fires the number-key-equivalent stream |
| `social_loop` | 9 (fixture `social_loop_start`) | Social Pillar v1 proof; rotating talk pools + `goblin_parley`'s Warrior-gated intimidate → LIVE [Diplomat] earn at the sleep (#123: fixture no longer pre-holds the class); post-earn [Charming Smile]-gated Watch persuade fires as the issue #50 gate's live proof |
| `sewers_walkthrough` | 9 (fixture `near_sewers`) | Liscor sewers proof; grate-gate seam + vermin fight |
| `property_seams` | 9 (fixture `sewers_property_seams`) | #348 property table slices 1+2, one leg per outcome verb: freeze_cell (ice floor on water, walked onto) + thaw_cell (a SECOND channel frozen then taken back by the same fire water refuses -- per-cell, first channel stays frozen, blocked step south proves it re-shut) + remove_scorch (debris) + state_set three ways in the runners' guild, teleported to (kindle the hearth, [Light] the wall lamp, [Basic Cleaning] the mud table), each banking the CARRIER's own counter once, plus the one-way second kindle falling to ambient + three authored REFUSALS with their own copy (fire at water, frost at a lit hearth, frost at a person), each pinning toast AND ui_toast_rendered + a null cell (untagged prop refused) |
| `cisterns_fight` | 9 (fixture `cisterns_fight_start`) | Quest 1 FIGHT path (clear `shield_spiders` nest); issue #69's other Klbkch-allied fight (seed re-verified, not independently roster-asserted -- crate_fight carries that proof) |
| `cisterns_talk` | 9 (fixture `cisterns_talk_start`) | Quest 1 TALK path (persuade Zevara, no combat) |
| `cisterns_scout` | 9 (fixture `cisterns_scout_start`) | Quest 1 SKILL path ([Appraise Foe] the `nest_ledge`) |
| `wrong_order_loop` | 9 (fixture) | "The Wrong Order" inn-local give→cook→report loop |
| `wrong_order_talk` | 9 (fixture) | "The Wrong Order" TALK path (Krshia smooth-over, no combat) |
| `wrong_order_fight` | 9 (fixture) | "The Wrong Order" FIGHT path (clear `supplier_scavengers`) |
| `economy_loop` | 9 (fixture `economy_loop_start`) | the coin arc: chore earn → loot → shop → spend |
| `char_creation` | none | THE character-creation UI proof (sprite-pick (2x3 idle grid, #42) + name committed via the tappable Begin control (#106), downstream pins held verbatim from the keyboard-commit era = click/keyboard equivalence proof) |
| `deep_descent` | 9 (fixture `deep_descent_start`) | the Raskghar descent + JOIN-Relc boss victory |
| `climax_chain` | 9 (fixture `climax_surface_start`) | the tremor beat + Zevara summons + Olesm briefing |
| `climax_seal` | 9 (fixture `climax_sealed_start`) | the seal beat + journal Act III advance |
| `arc_flow` | 9 (fixture `near_act3`) | THE WHOLE ACT III ARC PROOF, tremor through epilogue |
| `status_first_encounter` | 9 (fixture `near_mage_cast`) | status glossary + first-encounter combat-feed surface |
| `ice_floor_loop` | 9 (fixture `near_ice_floor`) | GH#21: [Ice Floor] area terrain effect -- cast/friendly-fire/standing-slow/expiry, live |
| `gear_loop` | 9 (fixture `gear_loop_start`) | resonance-gear UI proof (accessory rows, capacity refusal) |
| `stealth_loop` | 9 (fixture `near_ambush_sneak`) | the [Stealth] seam: skip an ambush, break it, positive control |
| `blink_bypass_loop` | 9 (fixture `wave_b_blink_start`) | Issue #132: Sneak, ward, and cardinal blink answer the same road ambush and bank three distinct counters |
| `ward_loop` | 9 (fixture `wave_b_ward_start`) | Issue #132: Greater Hearthward suppresses the road ambush for two sleeps, then expires into a positive-control fight |
| `animate_companion_loop` | 9 (fixture `wave_b_animate_start`) | Issue #132: animate a ruin bone pile, field `skeleton_ally` in combat, and clear the follower on sleep |
| `rogue_earn_loop` | 9 (fixture `near_rogue`) | K3 [Rogue] earn: `recovered_crate_watch` -> sleep -> `class_gained` -> [Stealth] fielded |
| `mage_invisibility_loop` | 9 (fixture `near_mage_invisibility`) | Issue #22: the Mage's [Invisibility] seam -- distinct visual, skip an ambush, break it with the OTHER sneaks-tagged skill ([Stealth]), positive control |
| `stages_loop` | 9 (fixture `krshia_stage3_pre`) | Social Pillar II: `talk_pool_stages` base->final (Krshia), unlocked hub topic + shop discount perk surface |
| `guild_interior_walkthrough` | 9 (fixture `near_guild`) | M-DEPTH DP1: guild_door real-door round-trip, Selys-behind-the-desk (pool + graph incl. desk-context node), board/notice-wall dressed props, Renn/Ilvo/Yelra walk-on pool lines |
| `board_loop` | 9 (fixture `board_loop_start`) | M-DEPTH DP2: THE REQUEST BOARD goes live -- browse/accept/fulfill/turn-in at Selys's desk (delta-since-accept, gold payout), slate rotation across a sleep + the "slate rotated overnight" line |
| `bounty_rank_loop` | 9 (fixture `bounty_rank_loop_start`) | Issue #163: rank-scaled bounties -- a Silver-rank player (swordsman 13, effective_power 13.0) accepts `bounty_gossip_tea`, its SILVER tier LOCKS (`accepted_bounty_tier`), the journal Postings detail shows the silver copy + 8-gold payout, completion via the delta contract across a sleep (tier stays locked), turn in for the silver 8 gold (not bronze 2); can-fail proven |
| `upstairs_walkthrough` | 9 | M-DEPTH DP3: the inn's upstairs -- stairs door pair, Lyonette's locked door (observe+locked_toast), sleep-beat parity proven live at the PC's own bed (same events, one extra flavor toast) |
| `barracks_walkthrough` | 9 (fixture `near_barracks`) | M-DEPTH DP4: the Watch barracks + market-depth stalls -- `bread_stall` (flavor/observe) + the `peddler`'s mini-buy (`watch_issue_gambeson`, 20g), `barracks_door` real-door round-trip (the relocated `watch_guard`, zero prior QA touches), Duty Sergeant Dresk Ashgrave's talk_pool, Zevara's desk (she stays at the gate), THE CELL (locked_toast, empty-v1 dressed) |
| `delivery_loop` | 9 (fixture `near_runners_guild`) | M-DEPTH DP5: the Runner's Guild delivery loop -- `runners_guild_door` round-trip, Vess's counter (talk_pool bark + `vess_counter` graph), THE DELIVERY BOARD browse, take a slip -> a real walked carry -> Chebyshev-1 arrival handoff at `krshia_stall` -> turn-in pay (1g exact total), the sleep-fail negative (parcel returns on the night ledger, no pay, `delivery_failed` armed) + slate rotation across the sleep + the one-shot failed-run bark; GH#27: a second, failure-free sleep proves Vess's OWN plain-rotation bark (distinct from both Selys's board line and Vess's failure line) fires once per rotation, not twice in the same waking |
| `stage3_perks_loop` | 9 (fixture `stage3_perks_pre`) | Issue #23: the `once_per_waking` dialogue seam's own proof -- Erin's daily meal (`well_fed` + hp_mod) and Relc's spar wager (+1g), both live: eat/win -> option gone same waking -> sleep clears `well_fed` + the bank -> both options back |
| `ruin_walkthrough` | 9 (fixture `near_ruin`) | Magical Door plan Task D1 (issue #8): enter the gated ruin via a fixture with `horns_dig_started` (Task 2.3 re-gate), real `ruin_door` door_when transition, walk the new `ruin_surface` map's bounds, read the pedestal-locked toast (`anchor_stone_pedestal`), round-trip exit via `ruin_exit` -- routes AROUND both new dormant encounters (`rift_vermin_leak`/`ruin_guardian` have no combat data yet, D2's job) |
| `horns_dig_flow` | 9 (fixture `horns_dig_start`) | 2026-07-26 main-quest restructure (Task 2.3): THE `horns_dig` canonical -- Ceria's dig invitation played live at the inn (`ceria_intro` on `ceria_inn`, through her talk_pool), the ruin seam on its new `horns_dig_started` gate, the camp join (`ceria_dig_camp`), the `ruin_guardian` fight + shipped rune pair, and the pedestal reveal (`recovered_anchor_stone` + `pedestal_breached` + `door_retrieved` on one open). EXTENDED in Task 2.4 through the mounting scene and out the other side: the haul home, Pisces hanging it at the pantry (`door_mounting`), `door_mounted` completing `horns_dig` and auto-starting `door_that_goes_elsewhere`, and a live ride through the Magical Door to Liscor off the new day-one portal gate -- THE canonical main-quest-start proof for the wave |
| `horns_dig_plates` | 9 (fixture `horns_dig_plates_start`) | 2026-07-26 main-quest restructure (Task 2.4 follow-up, controller ruling on the three-pillars gate): the NON-FIGHT breach canonical. `horns_dig`'s breach beat promises "fight what guards it, walk the plates, or read the wardwork" but had shipped FIGHT-ONLY -- every other `pedestal_unsealed` producer sits behind the door chain, which since the restructure starts AFTER this pedestal opens. Presses the locked plinth, walks the shipped ordered plate pair, presses the plinth again (banking `pedestal_unsealed` off the new dig-gated variant) and opens the pedestal to the same reveal `horns_dig_flow` reaches through the guardian. Whole-run `combat_started`/`skill_used` absence is the negative proof; the guardian is left standing. The `[Detect Magic]` wardwork leg is proven at sim level in `test_sim_core.gd` |
| `door_chain_fight` | 9 (fixture `door_chain_fight_start`) | Magical Door plan Task D3 (issue #8): the FIGHT leg + full end-to-end chain -- Pisces sends the player to clear `rift_vermin_leak` (inn), the ruin recovery run (`ruin_guardian` fight + `anchor_stone_pedestal` opens), Krshia's `resonant_catalyst` purchase -- `consult` + `buy_catalyst` complete (the 2026-07-26 restructure moved the retrieval to its own quest, so `recovered_anchor_stone` banks here without completing a door beat). Task 2.4 restored a LIVE start leg: the run opens at the inn with the haul home and unhung and plays the pantry MOUNTING scene (`door_mounting` on `pisces_mounting`) for real -- `door_mounted` + the `door_chain_started` legacy alias banked and `door_that_goes_elsewhere` auto-started before the consult, taking the scene's SHORT exit where `horns_dig_flow` takes the long one |
| `door_chain_talk` | 9 (fixture `door_chain_talk_start`) | Magical Door plan Task D3 (issue #8): the TALK leg -- persuades Pisces (zero combat), banks the SAME `door_understood` as the FIGHT leg, then opens `anchor_stone_pedestal` WITHOUT ever fighting `ruin_guardian` (the pedestal's `contains_when` OR-gate proof from a non-FIGHT producer) |
| `door_chain_scout` | 9 (fixture `door_chain_scout_start`) | Magical Door plan Task D3 (issue #8): the SKILL leg -- `[Observe]`s `pantry_door_runes`, reports to Pisces (banks the SAME `door_understood`), then buys Krshia's `resonant_catalyst` (`bought_catalyst`) |
| `door_chain_sequence_break` | 9 (fixture `door_chain_sequence_break_start`) | The sequence-break canonical: fights `rift_vermin_leak` BEFORE ever consulting Pisces (`door_chain_started` pre-banked, `met_pisces` still 0) -- proves the finding-50 recap option ("About the pantry door -- I understand what it is now...") surfaces and its `door_recap` node renders from a REAL fight-first run, then completes the chain (ruin recovery + Krshia's catalyst) exactly like `door_chain_fight`'s own tail |
| `door_awakening` | 9 (fixture `door_awakening_start`) | Magical Door plan Task D4 (issue #8): the full awakening chain from a beat-3-ready fixture through 3 real sleeps -- 2 silent study beats then `door_awakened` banks + the GDI line queues under the sleep veil -- to the awakened `pantry_door`'s interact rewire (opens the portal menu, not the flavor toast). Issue #92 R4 EXTENSION: also proves the resonance-growth payoff -- pre-bump equip refusal on the already-carried `anchor_sliver` (resonance 3), one further sleep past the awakening banks `resonance_grown` (resonance_capacity 2->3), the same equip then succeeds |
| `portal_menu` | 9 (fixture `portal_menu_start`) | Magical Door plan Task D4 (issue #8, the O2 rule): fast-travel round-trip -- menu at the inn anchor -> street arrival -> menu at the street anchor -> return to the inn -- pins the `map_changed` pair in order and asserts no `combat_started`/`player_moved`/`player_blocked` fires anywhere in the run (portal travel is `transition()` only) |
| `garden_walkthrough` | 9 (fixture `near_garden`) | Issue #9 Task G1 (spec §5 RATIFIED): the Garden of Sanctuary unlock-to-rest loop -- the qualifying sleep banks `garden_door_unlocked` silently (act >= III AND K=2 of 4 ratified legs) while resolving the LIVE [Helper] grant the script's own Kth-leg clean earned (#123: the fixture no longer pre-holds helper), Erin's `talk_pool_stages` acknowledgment, `garden_door` opens for real at the inn, day-bright identity + fountain + misted-rise clearing live on `garden_sanctuary`, rest-bed sleep parity at `garden_bed`, round-trip exit -- plus a whole-run no-violence sim-guard cross-check (`combat_started` never fires) |
| `garden_unlocked_entry` | 9 (fixture `garden_unlocked`) | Issue #48 Part 5b: smoke canonical for the NEW `garden_unlocked` fixture (the position immediately after the reveal, `garden_door_unlocked` already banked) -- one interact steps straight into `garden_sanctuary` and the very next interact round-trips back to the inn; `garden_walkthrough` already covers the Garden's own content end-to-end, this just proves the new fixture boots into a working position |
| `riverfarm_walkthrough` | 9 (fixture `near_riverfarm`) | 8b Task R1 (issue #10): door arrival via the portal contract's first exercise (locked shape 1) -- village walk (headman talk_pool + the charmed villager's `echo_of` tell, locked shape 3) -- the night wolf gate NEGATIVE at day (`encounter_when`, locked shape 2, canonical-pinned) -- the hollow round-trip -- the witch's two-form read across a REAL live mid-waking phase crossing (no sleep, locked shape 4) -- routes around `briar_collectors`/`briar_collectors_deep` (live encounters post-R2 merge; interact-only, never faced -- `combat_started` absent whole-run) |
| `witch_cottage_reachability` | 9 (fixture `near_riverfarm`) | Issue #117: the cottage's sole cardinal approach `[3,8]` is reachable from the hollow door landing; a distant mouse click stops there, a second click interacts, and keyboard bump+interact emits the same accomplishment + rendered toast |
| `riverfarm_fight` | 9 (fixture `riverfarm_fight_start`) | 8b Task R3 (issue #11) 'The Price of a Favor' FIGHT path -- give, clear `briar_collectors` then `briar_collectors_deep` (the escalation R2 tuned; the deep victory banks the shared `blight_lifted` convergence), report, completion; plus the night-wolf POSITIVE leg (a real actions-since-sleep burst to night, `river_wolf_pack`'s `trigger_radius` fires and is won) |
| `riverfarm_talk` | 9 (fixture `riverfarm_talk_start`) | 8b Task R3 (issue #11) 'The Price of a Favor' TALK path -- no combat: give, Eloise's [Calming Touch]-gated mediation (GH#64 re-gate; a renegotiated year of hearth-tending banks `blight_lifted`), report, completion, plus the shared post-conditions every path converges on (village visual_states BRIGHTEN, the charmed villager's echo RETIRES via `talk_pool_stages`, the witch-cottage VENDOR unlocks) + the GH#65 stone leg: portal-menu exclude/include pins around the 18g attunement-stone purchase and the post-buy shop re-entry retire proof |
| `riverfarm_skill` | 9 (fixture `riverfarm_skill_start`) | 8b Task R3 (issue #11) 'The Price of a Favor' SKILL path -- no combat: give, the gauntlet in sequence (`basic_cooking` the offering -> [Light] the threshold candles -> [Observe] the true knot, existing field-skill seams) -- the last step banks `blight_lifted` directly -- report, completion |
| `longhouse_walkthrough` | 9 (fixture `near_riverfarm`) | Riverfarm depth: the longhouse's real interior -- `longhouse_door` round-trip (walked around the building's own footprint), the headman's-corner ledger prop, sleep-beat parity at the relocated `riverfarm_guest_cot` (same event stream + the flavor toast held verbatim from the old open-lawn cot) |
| `spine_reach` | 9 (fixture `spine_reach_start`) | 2026-07-26 main-quest spec §4 (Phase 6): the pilgrimage spine `where_the_door_reaches` end to end -- Pisces's spine-start arm (gated `door_awakened`, banks the quest + `spine_started`, speaks the foreshadow line), then ALL THREE capstone arms live off a save whose region chains are already CLOSED (the revisit-reachability position spec §8 requires): Eloise raising the feeding ward herself, Hedault cataloguing the residue unbidden, Grimalkin on the forge tier's rune-feeding ledger -- beats 1/2/3 complete on `lattice_witch_lore`/`lattice_hedault_reading`/`lattice_forge_rune`. Exact option-array pins on the Pisces/Eloise/Grimalkin hubs (presence + the witch arm's `hide_when` retirement); the greet is re-read after every bank to walk the ascending `text_variants` contract. NEGATIVE: the quest must NOT complete -- the `return` beat waits on Phase 7's `seal_descent_agreed`. No combat |
| `seal_open` | 1 (fixture `seal_open_start`) | 2026-07-26 Act V (#270 as amended by main-quest spec §7), the FIGHT path: Pisces's descent ask (all three lattice counters; banks `seal_descent_agreed`, starts `what_the_seal_was_feeding`, and COMPLETES the spine's `return` beat -- Phase 6's accepted red) -> the SKILL-FREE reading at `seal_kept_door` (two bare interacts stage Pisces's narration into `read_the_seal_runes` then `read_the_feeding_ward`) -> WE OPEN IT (`seal_opened`) -> the door's only approach cell wakes `seal_warden` through its `trigger_radius` (tuned solo fight, harness cell `seal_warden_t5_sw14_solo`) -> `door_when` into the new `seal_vault` -> the anchor's open banks `seal_resolved`, grants `seal_anchor_rune`, completes the quest. Exact Pisces greet pin; the journal already reads `act_v` at load |
| `seal_fed` | 9 (fixture `seal_fed_start`) | 2026-07-26 Act V, the TALK path: same descent + reading, then Pisces points at Olesm and pointing banks NOTHING (asserted); his Act V hub row opens the standing-posting pitch where the witch pattern renders BOTH legs ([Charming Smile] selectable off `diplomat` 1, the 12-gold alternate beside it), and the Skill leg banks `seal_kept_fed` + `seal_resolved`. NEGATIVES: `seal_opened`/`seal_rewarded` never bank, `combat_started` never fires |
| `seal_reward` | 9 (fixture `seal_reward_start`) | 2026-07-26 Act V, the SKILL path: the reading's richest arm -- [Detect Magic] at `detected_wardwork` 3 lands the QUARTET payoff (four readings, one lattice, this door is where it drains to) -- then the re-ward fork, [Hedge Remedy] VISIBLE-LOCKED beside the selectable [Detect Magic] one, Skill-gated pitch + ITEM-gated close CONSUMING `guardian_ward_fragment` (`item_lost` asserted) to bank `seal_rewarded` + `seal_resolved`. NEGATIVES: the door never opens, no combat |
| `finale_after_merge` | 9 (fixture `finale_merge_start`) | 2026-07-27 Phase 8, THE FINALE vs A PENDING MERGE: `UI_SLEEP_VEIL_FINISHED` opens consolidation_prompt's modal, so a sleep that both OWES the ending and OFFERS a merge would drop the finale's black over that choice. A TALK-resolved save parked on the inn bed at exactly the [Spellsword] gate (`warrior` 10 + `mage` 11): sleep -> offer -> the prompt opens with the curtain PROVEN still down -> DECLINE -> the finale rolls that same night at 10 lines, both parents still standing in the recount |
| `olesm_chess_loop` | 9 (fixture `olesm_chess_pre`) | Issue #25: Olesm's chess-match payoff -- the stage-3 hub option visible-but-GREYED while 2g-short (no debt), a real Serve earns the shortfall, the honest counter-move beat (wrong-move loop, then the correct move) banks `chess_with_olesm` + the wager's +2g win side, the hub option retires (`hide_when`) and the shorter rematch `talk_pool_stages` line takes over post-sleep |
| `invrisil_walkthrough` | 9 (fixture `near_invrisil`) | 8c Task C1 (issue #12): portal arrival (a new destination row, `invrisil_attuned` pre-banked in the fixture -- real play banks it via Eloise's post-Riverfarm stone purchase, `riverfarm_talk`'s own canonical) -- the crowd (a pool-less extra's static dialogue line) + Coyle's pre-quest talk_pool -- the alleys' bent route with BOTH sneak legs (NEGATIVE: a real LOW-lethality fight resolves; POSITIVE: `assert_event_count` pins `combat_started` at exactly 1 for the whole run) -- the fixer Cups' rumor-by-the-cup hub (one real gold-gated purchase) -- THE GUEST COUCH sleep site (last beat, phase-advancing) |
| `invrisil_disagreement_stealth` | 9 (fixture `near_invrisil_stealth`) | 8c Task C3 (issue #13) 'A Gentleman's Disagreement' STEALTH/SKILL path -> EXPOSE fork: the `counting_house_ledger` prop's [Appraise Foe] beat (banks `lifted_true_ledger`) -- accuse Coyle with the ledger, corner him, EXPOSE (`merchant_exposed` + `merchant_fate_decided`, quest beat 1, no gold) -- report to Wilovan (`brothers_job_done` + `cleared_brothers_name` + the shared `brothers_marker` item/accomplishment, quest beat 2 + `quest_completed`) -- THE SAFE-PASSAGE REWARD proven (`entity_removed` on both alley footpads) -- zero combat the whole run |
| `invrisil_disagreement_talk` | 9 (fixture `near_invrisil_talk`) | 8c Task C3 (issue #13) 'A Gentleman's Disagreement' TALK path -> EXTORT fork: Cups' testimony chain via the free Persuade branch (banks `fixer_will_testify` + `persuaded_someone` -> `secured_testimony`, C1's `invrisil_fixer.json`'s first real coverage) -- accuse Coyle with the testimony, corner him, EXTORT (`merchant_extorted` + `merchant_fate_decided` + 40 gold, quest beat 1) -- report to Wilovan (quest beat 2 + `quest_completed` + the shared `brothers_marker` + safe-passage reward) -- proves the fork's OTHER ending + the economy-sink gold effect + the issue #89 fence leg (marker-gated buy, exact gold delta, sold-out retirement) |
| `invrisil_disagreement_fight` | 9 (fixture `near_invrisil_fight`) | 8c Task C3 (issue #13) 'A Gentleman's Disagreement' FIGHT path -> EXPOSE fork: the come-along ask LIVE (Wilovan's hub -> ways -> fists_door -> 'Come with me.' banks `wilovan_will_fight`, this lane's own new ally-gate wiring) -- win the `hired_blades` warehouse fight with Wilovan fielded as the gated ally (banks `forced_confession`) -- confront Coyle via the post-fight hub entry, proving `cornered`'s own `forced_confession` 'beaten register' text_variant -- EXPOSE, report, `quest_completed` + safe-passage reward -- the only one of the 3 scripts fielding a real combat ally |
| `invisibility_combat_loop` | 9 (fixture `near_invisibility_combat`) | Issue #46: [Invisibility]'s combat read -- self-cast untargetable status live, solo pc vs goblin_raider/goblin_shaman (no ally, exactly one valid target): enemy turn passes without targeting the invisible pc, pc's own Attack breaks it, the next enemy turn can target pc again |
| `mouse_loop` | 9 (fixture `near_tactician`) | Issue #57/#84/#109: mouse support -- distant prop click walks to nearest CARDINAL approach cell and stops without interacting, hotbar slot CLICK fires the number-key-equivalent stream and disarms a Tab-primed cursor, adjacent click faces+interacts in one click, click-to-walk on open ground (exact per-step count), keyboard press mid-path cancels, a title-row click reaches Continue, a pause-row click Resumes, dialogue-option clicks pin the exact keyboard-equivalent dialogue_choice payload; issue #109's own diagonal-click repro -- a click from a DIAGONALLY-adjacent cell paths to the nearest cardinal neighbor instead of firing a doomed interact, a second click (now cardinal-adjacent) is what actually fires it |
| `field_chips_loop` | 9 (fixture `near_tactician`) | Issue #109: the field-mode HUD launcher chips -- journal/inventory chips toggle_open()/close their panels (ui_journal_shown/hidden, ui_inventory_shown/hidden), the pause chip opens pause then the already-clickable "Settings" row (issue #84) opens Settings for real (ui_settings_shown + ui_settings_rendered), Back + Resume close back out -- player_cell/actions_since_sleep untouched throughout (presentation-only) |
| `flame_pillar_loop` | 9 (fixture `near_flame_pillar`) | GH#71: [Flame Pillar] blast_damage effect, live -- a single cast hits an enemy adjacent to a second enemy AND the caster's own cell (friendly fire across enemy+enemy+self), walls excluded from the blast radius (icy_floor's own shape, reused), no terrain/status write |
| `dungeon_peek` | 9 (fixture `cisterns_scout_start`) | 8d Phase B/C1 (issue #14): teleport-based canonical for `dungeon_approach`+`trapped_halls`, independent of the quest; bounds lap, gallery-seal anchor, dormant-guardian foreshadow, the new door pair, all 4 trap classes (pressure plate trigger, dart-slit nudge + the `requires_item` gate refusing without `trap_kit`, illusory-floor block+reveal, snare/vault now REAL `kind:encounter` entities correctly gate-closed at this fixture's unmet state), round-trip exit, whole-run `combat_started` absence |
| `delve_fight` | 1 (fixture `delve_fight_start`) | 8d C1/C4/D1 (issue #14) 'What the Seal Kept' FULL CHAIN: accept the stipend at Olesm -> the reopened gallery (B3 `door_when` live) -> recruit Ceria/Yvlon/Ksmvr (`horns_party_formed`) -> FIGHT the promoted snare (`trapped_halls_snare` arena, solo T4) -> the vault boss (4-ally party, `windup_declared` live, GATED PARTY_CELLS band) -> the FIND beat (`seal_kept_door`'s gated variant) -> report, `quest_completed`, 15g paid |
| `delve_skill` | 1 (fixture `delve_skill_start`) | 8d C1/C4 (issue #14) SKILL route: `dart_slit_a`'s disarm now gates on carrying `trap_kit` (`requires_item`, item-hint toast when unheld) -- the real hotbar disarm consumes it (`item_lost`) and banks `halls_cleared` directly; proves the vault's `encounter_when` gate flips open (`combat_started` fires) without a second full boss fight |
| `delve_talk` | 1 (fixture `delve_talk_start`) | 8d C4 (issue #14) TALK route: talk Ksmvr through the trapped_halls pressure plates (`ksmvr_plates.json`, gated on `horns_party_formed`) -- banks `guided_ksmvr_through_plates` (the real cost, `vault_boss_slot`'s `ally_hp_penalty` reads it) and `halls_cleared` directly; proves the vault's `encounter_when` gate flips open without a second full boss fight |
| `archer_earn_loop` | 9 (fixture `near_barracks`) | GH#70 [Archer] earn: bow -> archery_butt practice (`ranged_hit` x3) -> sleep -> `class_gained` -> a won ranged fight (`attack_resolved` at distance >1, the range+LoS seam live) |
| `settings_loop` | none | Issue #77: settings + accessibility surface -- bus-routing fix (`UI` sends to `SFX`), a real slider move asserted via `AudioServer.get_bus_volume_db`, fullscreen toggle, text-scale step + a copy-fit-sensitive TOAST re-render post-scale, `reduce_motion` surviving a full Save->Load UI rebuild, the Controls reference sub-page, Replay Hints re-arm, `settings.cfg` written -- opened from BOTH title and pause. Issue #107: the Help reference sub-page (appended before 'Back'), opened via a keyboard leg AND a click leg, each pinning `ui_help_rendered`'s section count + the 'Saving' section's exact line |
| `gate_visual_check` | 9 | GH#184: gatehouse + street arch visual check (fixture `gate_visual_start`); windowed screenshots both sides, headless asserts the gate transition |
| `credits_visual_check` | 9 | GH#147/About: Credits panel renders 4 sections + 2 numkey links; windowed screenshot proves the parchment fits (incl. Back row) |
| `mobile_tap_check` | 9 | GH#196/#197/#202: title-art tap, real-paging tap-advance (`qa_real_paging` opt-out; pages 1..4 pinned), last-page tap defers, interact-nothing excluded from Recent Messages (`recent_count` 1; fixture `mobile_tap_start`) |
| `rags_meeting_loop` | 9 (fixture `rags_gates_met_start`) | b1 #199: gated Rags meeting → SUPPLY close (draught consumed, pawn granted, quest completes, #211 grant banks) |
| `rags_gate_check` | 9 (fixture `rags_gate_unmet_start`) | b1 #199: `encounter_when` absent-arm refusal — gate toast, no dialogue (the arm's can-fail proof) |
| `floodplains_price_talk` | 9 (fixture `floodplains_price_talk_start`) | v0.16 F1 (#308): TALK route -- Krshia's 8g quiet run banks camp_trade_brokered, then the report to Rags closes the quest |
| `floodplains_price_help` | 9 (fixture `floodplains_price_help_start`) | v0.16 F1 (#308): HELP route -- corusdeer cull + camp carry job arm the drying rack, camp_larder_filled, then the report |
| `floodplains_price_fight` | 9 (fixture `floodplains_price_fight_start`) | v0.16 F1 (#308): FIGHT route -- the first goblin-ALLY encounter (rags_ally + goblin_spear_ally fielded), camp_ground_held, then the report |
| `floodplains_price_gate_proof` | 9 (fixture `floodplains_price_betrayal_start`) | v0.16 F1 (#308): betrayal branch -- drove_off_rags banks BOTH terminals, the F1 offer never appears, the camp mouth stays shut (the exclusion's can-fail proof) |
| `parley_talkdowns_loop` | 9 (fixture `parley_talkdowns_start`) | GH#217: four talk-downs close non-violently; staked formal bout fights by design |
| `parley_gates_check` | 9 (fixture `parley_gates_unmet_start`) | GH#217: tease legs — skill/gold arms LOCKED, permit arm HIDDEN (exact-list pins = can-fail) |
| `ratici_fence_loop` | 9 (fixture `ratici_fence_start`) | b2 #218: fence open, EXACT day-0 slate, buy leg, sleep rotation to day-1 |
| `ratici_fence_gate_check` | 9 (fixture `ratici_fence_untrusted_start`) | b2 #218: untrusted interact = eyed doorbell, no fence dialogue (gate can-fail) |
| `hedault_fragment_loop` | 7 (fixture `hedault_fragment_start`) | b6 #221: fragment trade by choice — yield preview, lore node, one-shot hide |
| `stationer_room_loop` | 9 (fixture `invrisil_stationer_start`) | v0.16 #306: the stationer's interior loop — new boulevard door both ways off (20,2), three non-quest observes, the clerk's pool, return landing |
| `adventurers_rest_loop` | 9 (fixture `invrisil_rest_start`) | v0.16 #306: the Adventurer's Rest interior loop — the (18,1) door both ways, three observes, two ambient pool NPCs, room read UNARMED |
| `invrisil_setting_talk` | 9 (fixture `invrisil_setting_start`) | v0.16 #306 I1 TALK terminal — commission to Hedault's bench to handover; OWNS the [Appraise Goods] VISIBLE-LOCKED pin (fixture lacks the skill) |
| `invrisil_setting_skill` | 9 (fixture `invrisil_setting_skill_start`) | v0.16 #306 I1 [Appraise Goods] arm — OWNS the SELECTABLE pin (fixture holds the skill), run through to the same terminal |
| `invrisil_setting_fight` | 9 (fixture `invrisil_setting_fight_start`) | v0.16 #306 I1 FIGHT route — the fence's back door, on_victory original_recovered, terminal on ruling 5's already-cleared save |
| `invrisil_hat_talk` | 9 (fixture `invrisil_hat_start`) | v0.16 #306 I2 TALK route — Wilovan's errand, three exchanges with the house factor, report for hat_job_done + 25 gold |
| `invrisil_hat_quiet` | 9 (fixture `invrisil_hat_quiet_start`) | v0.16 #306 I2 QUIET route AND its can-fail — table first banks handoff_mistimed, then peg rail, then table banks handoff_quiet off its variant |
| `invrisil_hat_loud` | 9 (fixture `invrisil_hat_loud_start`) | v0.16 #306 I2 LOUD route — the bravos fought, handoff_loud banked off on_victory, reported as a live third ending |
| `invrisil_v016_gate_check` | 9 (fixture `invrisil_stationer_start`) | v0.16 #306 gate proof — Hedault's hub pinned at exactly 5 rows (ruling 1 regression guard), fence + bravos gate toasts, both plates UNSPAWNED |
| `grimalkin_study_loop` | 6 (fixture `grimalkin_study_start`) | b4 #219: study accept-at-hub, single-slot mutual-exclusion option pins, turnin voice param (Grimalkin's not-done arm verbatim) |
| `save_port_loop` | 9 (fixture `near_pallass`) | a9 #246: export/import round trip — Export Save writes user://exports, Import trial-validates it back (GAME_LOADED reason:import, state identity, Continue slot persisted) |
| `horns_residence` | 1 (fixture `horns_residence_start`) | 8d D2 (issue #14/#15): the Horns of Hammerad's post-delve inn residence -- pool presences only v1 (Social II `talk_pool_stages` explicitly deferred). Walks to Ceria (common-room table)/Yvlon (by the hearth)/Ksmvr (beside the pantry door), pins one `talk_pool` line each via `ui_dialogue_rendered`, and proves entity presence via `ui_entities_rendered`'s inn sprite count (16 -> 19, `WIGame.entity_present`'s new `present_when` structural gate); `inn_walkthrough`'s own unmodified pre-quest run is this canonical's negative half |
| `overlay_loop` | 3 (fixture `inn_guests_start`) | GH#279 dev debug overlay — HIDDEN BY DEFAULT pinned at boot (D4: full-window captures make an overlay-on FEEL shot contaminated evidence), `toggle_overlay` renders with payload map cross-checked against `assert_state`'s snapshot, toggle off emits hidden. Debug builds only (`OS.is_debug_build`); inert in release exports (gate returns before any UI/connection/input) |
| `reload_loop` | 3 (fixture `inn_guests_start`) | GH#278 live data reload via the save round-trip — `reload_data` rebuilds sim+view coherently (map/cell/counters pinned before+after), the rebuilt world stays interactable (**Selys** dialogue via `inn_guests_loop`'s proven route; FoTI PR2b's 6-name roster over a met pool of 5 puts selys+krshia on shift at this fixture's `times_slept=10`, so Pisces's chair is the empty floor now), mid-dialogue reload REFUSES with the observable toast, and reload works again after close. Data-changed staleness is unit-pinned in `test_reload_caches` (temp-map trick); `res://data` cannot be mutated mid-QA-run |
| `inn_guests_loop` | 3 (fixture `inn_guests_start`) | d1 #247 Friends of the Inn -- RE-DERIVED for PR2b. The roster completed APPEND-ONLY at `[selys,krshia,olesm,pisces,relc,zevara]` over the same 2 seats, and the modulus is the met POOL, not the roster: this fixture banks FIVE (zevara alone unmet), so `10%5=0` and all three of the script's wakings re-seated -- t=10 **Selys (6,6) + Krshia (4,6)**, t=11 **Krshia + Olesm (3,5)**, t=12 **Olesm + Pisces (6,5)**. Proves DISPLACEMENT FIRST (before anyone is greeted the player walks clean over (3,5) and (6,5), which is only proof because THIS run seats Olesm on the first at t=11 and Pisces on the second at t=12 -- `present_when` absence is STRUCTURAL), servable (Selys's Serve banks the shared `served_customer` + consumes the `hot_meal`; every later Serve renders visible-locked "requires Hot Meal"), context awareness (`selys_inn`/`krshia_inn`/`olesm_inn`/`pisces_inn` all open, never the guild board, the market stall, `olesm_intro`'s chess desk or `pisces_magic`'s tutoring), and TWO SLIDES (t=11 Krshia STAYS while Olesm's chair goes blocked; t=12 Olesm holds while Krshia's goes walkable). Sprite pins 23 -> 24 across the first sleep: the extra row is `garden_door`, unlocked by the catalyst attunement inside the sleep beat, never a third guest. All four PR1/PR2a inn registers keep live coverage. `relc_inn_guest` (1,5) and `zevara_inn_guest` (3,6) exist on this map now but are never occupied across this run (zevara unmet here, relc's turn is t=13), so no route cell can silently block -- the PR2b pair is proven in `inn_guests_full_loop` |
| `inn_guests_full_loop` | 3 (fixture `inn_guests_full_start`) | d1 #247 Friends of the Inn **PR2b** -- the FULL-POOL twin, and the ONLY live coverage `relc_inn`/`zevara_inn` have (Zevara had ZERO met-state QA coverage anywhere in the suite before it). All six rostered met, so pool 6, `10%6=4`, seating **Relc (1,5) + Zevara (3,6)** -- the pair `inn_guests_loop`'s fixture can never reach. Relc's chair is (1,5), NOT the spec's (2,5): the windowed crowding check the spec demanded for this pair failed there -- his 124px rig sat two rows under `yvlon_inn` (2,3) and one row over the (2,6) boot cell and rendered as an unidentifiable green torso. Proves both new seats render and open within a step of the boot cell (2,6), REGISTER-PURITY (`relc_inn` never `relc_intro`'s spar/wager/spear or `relc_descent`; `zevara_inn` never `zevara_intro`'s cisterns sweep, summons or seal report, which is where the something-beneath arc lives), SERVE ON BOTH NODES (Relc served from his off-duty TOPIC node, Zevara from her GREET node -- the second bowl COOKED at the stew pot via `hotbar_3`, so `served_customer` banks twice in one waking), and THE WRAP (a sleep to `times_slept=11` moves the start to 5, seating [zevara, selys]: Relc's chair goes walkable, Zevara STAYS, and the window wraps the roster's tail onto its head -- the one edge no smaller pool can exercise) |
| `inn_guests_ext_loop` | 3 (fixture `inn_guests_ext_start`) | d1 #247 Friends of the Inn **FoTI extension** -- the ONLY live coverage `klbkch_inn`/`rags_inn`/`wilovan_inn`/`grimalkin_inn` have. Roster grew APPEND-ONLY to TEN (`[selys,krshia,olesm,pisces,relc,zevara,klbkch,rags,wilovan,grimalkin]`) over the same 2 seats; the fixture banks all ten met AND the three quest terminals, so pool 10, `16%10=6`, seating **Klbkch (11,5) + Rags (12,5)**. Two real sleeps walk the window through the whole new east block: 17 seats [rags, wilovan], 18 seats [wilovan, grimalkin], so all four registers open live and each new guest is proven to ARRIVE and to LEAVE. Grimalkin's 224px rig -- the biggest shipped, roughly two tiles wide and four tall on screen -- holds the far-east CORNER (14,5): his first cell (12,6, next to Wilovan at (11,6)) FAILED the windowed crowding check, burying Wilovan behind his arm and losing his own head to the player two rows north. ANY two roster members can co-render (a partial met-pool re-bases the window's neighbours), so no seat pair may lean on the full roster's ordering. REGISTER-PURITY is asserted as ABSENCE (`rags_meeting`, `invrisil_wilovan`, `pallass_grimalkin` never open across the run). SERVE ON BOTH NODES (Klbkch from GREET on the fixture's bowl, Rags from her TOPIC node on a bowl cooked at the stew pot via `hotbar_3`; Wilovan and Grimalkin pin the visible-locked "requires Hot Meal" render with an empty pack). Sprite pins 23 -> 24 across the first sleep: the extra row is `garden_door`, never a third guest |
| `inn_guests_gate_proof` | 3 (fixture `inn_guests_gate_start`) | d1 #247 FoTI extension -- the QUEST-GATE proof. `inn_guests_gate_start` is `inn_guests_ext_start` minus `rags_meeting_settled` and otherwise identical, INCLUDING `chatted_with_rags`: met is NOT enough for a quest-gated guest. `WIInnGuests.GUEST_POOL_GATES` holds her out of the met-POOL rather than merely out of her chair, so the modulus drops 10 -> 9 and the SAME `times_slept=16` seats a different pair (`16%9=7` -> **Wilovan (12,6) + Grimalkin (14,5)**) while her chair (12,5) is plain floor the player walks onto. A sleep to 17 (start 8 -> [grimalkin, selys]) proves the shorter cycle is a real cycle and she is still absent. The exhaustive never-once-across-a-full-rotation leg is unit-pinned in `test_inn_guests` (t=0..19) -- a live script cannot afford ten sleeps and does not need to |
| `journal_history` | 9 (fixture `journal_history_start`) | Issue #79 (journal & progression communication wave): the journal's Completed section (chosen-path history line), Postings section detail view (accepted posting/slip's own authored copy text), and per-class aspiration identity line, all asserted structurally off one `ui_journal_shown` open; a trailing cursor move + toggle proves the pre-existing skill-row mechanism still works with the new sections rendered above it (issue #209 re-pin: the drag-scroll + cursor legs now ride the Skills tab) |
| `journal_tabs_loop` | 9 (fixture `journal_history_start`) | Issue #209 (journal internal tabs): the three-tab switcher (Quests/Skills/History). Opens on Quests (`active_tab:"quests"`), TAPS each tab label via the new `click_journal_tab` driver action asserting `active_tab` flips while the FULL section payload stays intact on every emit (the tab-independent-payload contract that lets the ~20 payload-only journal canonicals survive the split), then proves keyboard `move_left`/`move_right` switch parity with wrap |
| `thread_lattice_loop` | 9 (fixture `thread_lattice_loop_start`) | Issue #148 Tier 5 (thread legibility): the rumor lattice armed->retire lifecycle -- the Raskghar handoff (`cleared_the_warren` banked, `raskghar_sealed` pending) arms Erin's relay-5 `talk_pool_stages` line, which plays on interact; installing the retired fixture (`raskghar_sealed` banked) and reloading via the pause-menu Load path retires it to her neutral base-pool line. Proves the array-order last-match-wins progression the whole lattice depends on |
| `missing_recruit_loop` | 9 (fixture `near_missing_recruit`) | Issue #81 (exploration & optional content): 'The Missing Recruit' end to end, POSTING-SEEDED -- a real `guild_board` rumor read banks `heard_about_missing_recruit`, Dresk's gated ask option starts the quest, `recruit_pell` (`present_when`-gated, structurally absent until the quest starts) is found in the sewers and completes beat 1, the journal's new Lore section picks up `note_sewer_surveyor`, and reporting back to Dresk completes beat 2 + pays 4 gold |
| `renns_warhammer_loop` | 9 (fixture `board_loop_start`) | Issue #81: 'Renn's Old Warhammer' end to end, POSTING-SEEDED, SAME-MAP shape (contrast `missing_recruit_loop`'s cross-map one) -- the same board rumor read banks `heard_about_lost_hammer`, Renn's gated ask option starts the quest, `renns_gear_stash` (an always-present container) completes beat 1, and the item-gated turn-in option completes beat 2 + pays 3 gold (the lore-note thread's third find, `note_old_dread`, was relocated to the barracks post-dynamism-check -- `barracks_walkthrough` carries it) |
| `crab_cull_loop` | 9 (fixture `crab_cull_loop_start`) | Issue #24: `bounty_crab_cull` end to end -- accept at Selys's desk (guild), the `crab_nest` confirm conversation (armor-telegraph node pinned verbatim, [Back away.] negative proves no combat, then [Strike first.]), win the `rock_crab_nest` fight (floodplains, `respawns:true`) TWICE with a real sleep between them (delta-since-accept >= 2), turn in for the exact 5 gold payout |
| `pallass_peek` | 9 | 8e Phase B+reconciliation (issue #16): teleport-based canonical for `pallass_market`/`pallass_forge`, the `dungeon_peek` precedent -- no live route at default boot (portals row gates on `pallass_attuned`); band-hardened bounds laps (up-bump y1 + down-bump y10, both maps), THE PERMIT OFFICE (forge_permit_clerk + the moved Grimalkin, C's graphs opened/closed at conversation-id level), the stallkeeper + checkpoint-clerk conversations, the elevator's locked read on both sides, the landmark props, the molten seam bump, whole-run `combat_started` absence |
| `pallass_walkthrough` | 9 (fixture `near_pallass`) | 8e E1 (issue #16, the milestone's exit-criteria canonical): the LIVE end-to-end 'Papers for Pallass' + 'Clearance for the Forge Tier' route -- Selys sponsors (10g) -> the #65 exclude/include portal-menu pin pair bracketing Krshia's attunement stone (18g) -> real travel to `pallass_market`'s reconciled arrival cell (4,8) -- the negative lift-locked read pre-permit -- the checkpoint clerk's 2g stamp + the human race-variant read, completing `papers_for_pallass` -- THE PERMIT OFFICE chain (5g file, Grimalkin's 8g exam voiced verbatim, 3g stamp), completing `forge_tier_permit` and producing `elevator_pass_stamped` -- THE LIFT open, a real walk across the forge tier to `grimalkin_station` and the round trip back down; gold ledger exact at every spend (60->50->32->30->25->17->14); zero combat whole run |
| `pallass_depth_gates_check` | 9 (fixture `pallass_depth_gates_start`) | v0.16 #307 THE GATE-PROOF LEG: a save holding NONE of the Pallass depth lane's 22 counters, walked over every surface the lane touched -- the smith's and the attendant's hubs still render exactly three rows (the lane's only two option-array pins), both clerk hubs unchanged, both `requires_skill` props (temper bench, consignee's file) render their `locked_toast` through the `skill_unknown` seam and bank nothing, `forge_temper_golem` stands at (8,6) PRESENT AND INERT (`encounter_when` is an interact/trigger gate, never structural absence) and the gate is proved AT THE INTERACT -- `player_blocked` on (8,6), the refusal's `gate_closed_toast`, and `combat_started` + the `forge_temper_golem` parley both asserted absent -- and the run closes with `assert_event_absent` over all sixteen quest-shaped counters plus `combat_started`. Cheapest failure detector in the lane; both tiers |
| `pallass_standards_talk` | 9 (fixture `pallass_standards_talk_start`) | v0.16 #307 P1 'Tempered Standards' TALK route: read the examination standard in `pallass_forge_hall` (it measures RECOVERY, not hardness), carry it to the smith, broker the six-year argument (`standards_brokered`), settle on that arm alone (`standards_tempered`) with `temper_run`/`golem_recalibrated` provably unbanked and zero combat -- then Grimalkin's NEW `standards_tempered` hub `text_variant`, the lane's only edit to his graph (ruling 4: variants only, three scripts pin his option array) |
| `pallass_standards_skill` | 9 (fixture `pallass_standards_skill_start`) | v0.16 #307 P1 SKILL route AND the no-ambush proof (D27): the temper bench's `requires_skill`/`on_skill_use` seam (`temper_run`), then the hall's y=7 lane walked end to end WITH the encounter present -- (7,7)/(8,7)/(9,7) is every cell a radius-1 trigger would fire on -- and `combat_started` + the parley conversation both asserted ABSENT, which is what proves the encounter is interact-only. Closes on the smith's billet arm |
| `pallass_standards_fight` | 9 (fixture `pallass_standards_fight_start`) | v0.16 #307 P1 FIGHT route: the script that closes the standing 'no QA script fights forge-hall content' debt -- fights the shipped `forge_hall` arena on the board for real, windowed shot included. spellsword:14 reference build; the parley node is asserted with `combat_started` pinned at ZERO at that step (the interact-only ordering proof), then `start_combat` -> autoplay -> victory -> `golem_recalibrated` with `forge_golems_culled` provably UNBANKED (ruling 2: a shared counter would have fed `bounty_forge_golem_cull`) |
| `pallass_ledger_offices` | 9 (fixture `pallass_ledger_offices_start`) | v0.16 #307 P2 'The Ledger Eats First' TALK route, the office loop that outranks itself: the Grand Lift down as a real `door_when` crossing, the market tier clerk ENDORSES, the forge-tier permit clerk COUNTERSIGNS, the den-shop consignee signs last (`loop_walked`) -- each office's arm is requires-gated on the previous office's product, so the cycle can only be walked in order -- then back up to the attendant (`ledger_unstuck`) |
| `pallass_ledger_carry` | 9 (fixture `pallass_ledger_offices_start`) | v0.16 #307 P2 HELP route and the ruling-7 proof: a staged lift carry composed ENTIRELY from shipped primitives (three `once_per_waking` props + `variants` thresholds + repeated `door_when` transitions). Pins each leg's EXACT toast, which is what makes a dead `variants` threshold fail loud -- both staged toasts key on (leg index - 1), dray rank at 1 and dock at 2, because variants resolve BEFORE the interact banks. Three legs in ONE waking -> the keeper's carry arm -> the attendant |
| `pallass_ledger_skill` | 9 (fixture `pallass_ledger_skill_start`) | v0.16 #307 P2 SKILL route: standing inside `pallass_den_shop` (the fixture that makes `MAP_REQUIRES['pallass_den_shop']` bite), the consignee's own file through the `requires_skill`/`on_skill_use` seam (`exemption_found`), out through the shopfront door, a real market walk, the lift up, and the attendant's report on that arm alone (`ledger_unstuck`) |
| `invrisil_round_trip` | 9 (fixture `spine_reach_start`) | THE INVRISIL RETURN PROOF, `pallass_round_trip`'s twin: the boulevard/alleys/parlor cluster's only way home is `invrisil_anchor_stone`, whose gate is `door_mounted` (NOT `door_awakened` -- the 18g-stone cohort never awakens the Door). Departs Liscor at `street_anchor_stone`, lands on the `invrisil` row's own arrival cell (4,8), walks the boulevard end to end and crosses into `mercantile_alleys` and back through the real door pair, then rides the anchor's picker home to the inn's authored (13,6); exact options pins both ends, zero combat |
| `wilovan_address_f` | 9 (fixture `wilovan_parlor_f_start`) | TWO blockers in the Brothers' parlor: the `{addr}`/`{Addr}` address token resolving at WIGame's own event sink -- the corpus's only `pc_gender` 'f' fixture pins the SHIPPED Wilovan talk_pool line and hub `text_variant` rendering 'miss' (the 'sir' spellings asserted absent whole-run) -- and the re-interact guard: `ratici` + the three parlor gentlemen + street `klbkch` each interacted TWICE on a spent `talk_pool` with no conversation/dialogue fallback (the old `Invalid access to property or key 'dialogue'`), re-serving the same line and banking nothing on the repeat |
| `pallass_round_trip` | 9 (fixture `spine_reach_start`) | THE PALLASS RETURN PROOF: `pallass_market`/`pallass_forge` share one lift and have no other map exit, so `pallass_market_arrival_anchor` (4,6) is a portal CARRIER now and this is the anti-softlock gate -- depart Liscor at `street_anchor_stone`, land on the `pallass` row's own arrival cell (4,8), walk BOTH tiers for real (market row 9 -> lift -> forge bench and back), open the plinth's picker (exact options pin, Pallass itself excluded) and ride it home to the inn's authored (13,6); zero combat |
| `pallass_race_peek` | 9 (fixture `near_pallass_drake`) | 8e E2 optional (debt-sweep item 5, Drake-line live coverage): teleport-based canonical pinning the DRAKE half of `tier_clerk_market`/`market_stallkeeper`'s `requires:{race:drake}` text_variants live (`pallass_walkthrough` already proves the human half) -- the unit test proves the `requires:{race}` mechanism, this proves the shipped graphs render it. Deliberately does not bank `pallass_entry_stamped` (out of scope, `pallass_walkthrough`'s own job); zero combat |
| `goblin_night_patrol_loop` | 9 (fixture `goblin_night_patrol_start`) | Issue #80 (world reactivity wave): floodplains' own night-only encounter slot, the `river_wolf_pack` precedent -- fixture starts past `night_at`, one real move into `trigger_radius` 1 fires `start_combat` live (goblin_raider/goblin_shaman, relc fielded), win banks `won_combat` |
| `dangersense_field_loop` | 9 (fixture `dangersense_holder_start`) | Issue #413: Rogue L4 [Dangersense] passive world overlay -- exact existing night trigger radii rendered, passive absent from hotbar, and a walked radius-skirt reaches (11,23) with no combat |
| `dangersense_nonholder_loop` | 9 (fixture `dangersense_nonholder_start`) | Issue #413 negative gate: identical night floodplains route at Rogue L3 emits no Dangersense overlay and starts no combat |
| `shaman_zone_loop` | 9 (fixture `goblin_night_patrol_start`) | GH#90: THE LIVE PROOF of combat_ai.gd's area_skill arm -- goblin_shaman casts [Ice Floor] as an ENEMY zone (`combat_set_cells` forces the >=2-no-ally geometry by construction), pinning `skill_resolved{actor:goblin_shaman}` + `terrain_added` + the exact 9-cell terrain snapshot + `ui_terrain_rendered` (the mid-AI-turn render); windowed captures the zoning shot; autoplay wins |
| `boulevard_night_footpads_loop` | 9 (fixture `boulevard_night_footpads_start`) | Issue #80 (world reactivity wave): Invrisil boulevard's own night-only encounter slot, the `river_wolf_pack` precedent's third application -- fixture starts past `night_at` (T3 warrior10 gear, R3 2026-07-12), one real move into `trigger_radius` 1 fires `start_combat` live (footpad_lookout/footpad_bruiser, solo), win banks `won_combat` |
| `trader_earn_loop` | 9 (fixture `near_trader`) | Class-foundation pass R5 (issue #98, 2026-07-12): [Trader]'s full earn-to-kit loop -- a real sale at the Pallass `market_stallkeeper` banks the 5th `deliberate_commerce` (a sell pinned, exact gold delta) -> sleep -> `class_gained{trader}` -> a [Bargain]-discounted buy at Eloise's (a NEW no-baked-price option, `dialogue.gd`'s price_mod renders/charges the discounted 6 gold, not the base 7, pinned exact) -> the Pallass `tier_clerk_market`'s own no_haggle refusal line (pinned). Zero combat |
| `trap_canonical` | 9 (fixture `near_sewers_trap`) | R6 (#95, class-foundation pass): `sewer_nest_snag` trap proof -- observe tell, the avoidable-path counterplay, the LIVE disarm leg banking shared `trap_disarmed` + 1g salvage via `press_field_skill disarm_trap` (the swap is APPLIED: `requires_skill disarm_trap` on all six placements, rogue:5 fixture with the skill on the bar; non-Rogues route around) |
| `floodplains_bestiary_loop` | 9 (fixture `floodplains_bestiary_loop_start`) | Issue #97 (bestiary expansion): `bounty_corusdeer_cull` end to end -- accept, the `corusdeer_range` confirm conversation (`[Back away.]` negative, then `[Strike first.]`), win the fight TWICE with a sleep between (delta-since-accept >= 2), turn in for 4 gold |
| `invrisil_mothbear_loop` | 9 (fixture `invrisil_mothbear_loop_start`) | Issue #97: `bounty_mothbear_cull` end to end -- accept, teleport to `invrisil_boulevard` at NIGHT, a real move into `trigger_radius` 1 springs `boulevard_mothbears` live, win ONCE (single-kill), turn in for 4 gold |
| `dungeon_kingslayer_loop` | 9 (fixture `dungeon_kingslayer_loop_start`) | Issue #97: `bounty_kingslayer_cull` end to end -- accept, the `kingslayer_den` confirm conversation, win the apex-predator fight ONCE (single-kill), turn in for 5 gold -- fixes the long-standing arachnid sprite gap |
| `pallass_watchgolem_loop` | 9 (fixture `pallass_watchgolem_loop_start`) | Issue #97: `bounty_watchgolem_cull` end to end -- accept, teleport to `pallass_market` (Pallass's 2nd combat encounter this task), the `market_watchgolems` confirm conversation, win TWICE with a sleep between (showcases the new `guard` AI profile), turn in for 12 gold |
| `thicket_cull_loop` | 9 (fixture `thicket_cull_loop_start`) | Issue #91 (hour-4 spine): `bounty_standing_thicket_watch` end to end -- accept at Selys's desk (post_game standing order), the `riverfarm_thicket_patch` confirm conversation ([Back away.] negative proves no combat, then [Strike first.]), win the fight (riverfarm_village, `thicket_remnant_a/b`, `respawns:true`) TWICE with a real sleep between them (delta-since-accept >= 2), turn in for the exact 8 gold payout -- THE repeatable-loop canonical proving accept->respawn-fight->sleep->re-fight->turn-in actually loops for the hour-4 spine's new content |
| `flood_ledger_talk` | 9 (fixture `flood_ledger_talk_start`) | v0.16 #305 R1 'The Flood Ledger' TALK route, the terminal path -- the headman's new `price_of_a_favor_reported`-gated start row, the `riverfarm_mill` ROUND TRIP (south approach (19,6) in, `mill_exit` back out to (19,6)), the tallyman's full tally walk through `tally_count` to `ledger_read_true`, all three mill observables with their toasts pinned verbatim, then the READ report completing beat 2. `combat_started` absent whole-run with `mill_granary_scavengers` armed and deliberately left standing |
| `flood_ledger_help` | 9 (fixture `flood_ledger_help_start`) | v0.16 #305 R1 HELP (Skill) route, and the owner of `mill_flood_stack`'s LOCKED read -- the mill door's SECOND legal approach (18,5) west (the exit always returns to (19,6), so entry-west/exit-south is a real one-cell asymmetry), a plain interact proving the `SKILL_UNKNOWN` + `locked_toast` arm on a `skill_uses` prop that carries no `requires_skill` (so `skill_hint_toast` can never fire there), then `press_field_skill` [Basic Cleaning] banking `flood_prep_done`, then the worked-report row. `ledger_read_true` asserted absent whole-run |
| `flood_ledger_fight` | 9 (fixture `flood_ledger_fight_start`) | v0.16 #305 R1 FIGHT route -- the ONLY live coverage `granary_scavenger_a/_b` have. Band fixture (warrior 10, `melee_hit` 57, no mage; `COMBAT_BAND_FIXTURES` pins the total at 10) matching `sim_combat_batch`'s `granary_scavengers_t3_warrior10_solo` cell. The win is pinned as `granary_cleared` (`on_victory`), NEVER `won_combat`, which a gray-band fight never banks whole. TRAP: the fixture's `rng_state` is derived at seed **2**, not 9 -- at ~0.81 measured win rate roughly one start state in five is a real defeat and seed 9's is one of them |
| `thicket_keeps_talk` | 9 (fixture `thicket_legacy_start`) | #396 THE RETIREMENT-CONTRACT canonical: `what_the_thicket_keeps`' offer row is deleted, so the run starts from a LEGACY SAVE MID-QUEST (`heard_thicket_keeps` pre-banked, quest already in `started_quests`) and enters through the R2 re-entry row instead of an accept -- `quest_started` is asserted ABSENT whole-run, because a legacy save can never re-accept a retired quest. Also the v0.16 #305 R2 'What the Thicket Keeps' TALK route, the terminal path -- and the owner of the `witch_hut` ROUND TRIP (`witch_hollow` (1,8) -> hut (4,6) -> back to (1,8), both arrival directions) plus `hut_ward_scrap`'s LOCKED read. The fixture holds NO `detect_magic`, which is what makes the refusal arm reachable and the whole-run `ward_scrap_read`/`detected_wardwork` absences sound. Zero combat with `thicket_line_den` armed and left standing |
| `thicket_keeps_skill` | 9 (fixture `thicket_skill_start`) | v0.16 #305 R2 SKILL route -- the ONLY live coverage `hut_ward_scrap`'s positive arm and all four `witch_hut` observables have. POSITIVE-ONLY by ruling: `assert_event_absent` has no window, so no locked-state negative is expressible in a run that casts the skill (`thicket_keeps_talk` owns it). The mandatory assertion is the whole-run ABSENCE of `detected_wardwork` -- the scrap must never become a fifth producer of that threshold. mage 7 is where `detect_magic` comes from (`spell_cast` raised to 24 for the level) |
| `thicket_keeps_fight` | 9 (fixture `thicket_fight_start`) | v0.16 #305 R2 FIGHT route -- the ONLY live coverage `line_stalker_a/_b` have. Band fixture (warrior 10, `melee_hit` 57, no mage) matching `sim_combat_batch`'s `thicket_line_den_t3_warrior10_solo` cell; the win is pinned as `thicket_cleared` (`on_victory`). Touches neither `riverfarm_thicket_patch` nor `bounty_standing_thicket_watch` -- different entity, different map, different roster ids. Same seed-2 `rng_state` derivation as `flood_ledger_fight` |
| `item_use_loop` | 9 (fixture `item_use_loop_start`) | Issue #92 R2: the combat-hotbar consumable toggle + real item-use canonical -- inventory-panel confirm toggles `mending_draught` onto the shared `hotbar_loadout` (`item:<id>`, the journal skill-toggle's own mirror), the 5th hotbar slot renders alongside the un-curated Warrior kit, confirm-gated use spends 1 AP flat via `WIGame.combat_use_item`, erases the item (consumption = `inventory.erase`), `item_used` + a toast fire |
| `chronicle_loop` | 9 (fixture `near_riverfarm`) | Issue #91 Chronicle: completed-run journal facts -> `settings.cfg` capture -> title card, then New Game before autosave proves the same results-only record survives with five selectable title rows |
| `second_wind_loop` | 9 (fixture `near_second_wind`) | Second Wind wave (#165): a [Swordsman] at L13 (sword_skill_used one short of L14) levels ORGANICALLY via a won fight's power_strike banks -> sleep grants [Crescent Cut] -> a second live fight proves it lands in `combat.combatants.pc.skills`; can-fail proven |
| `regional_work_loop` | 9 (fixture `near_pallass`) | v0.15 T4.4 (Lane B population): THE regional repeatable-work canonical -- the playtest found zero repeatable work outside Liscor. `riverfarm_field_board` (3,10), `parlor_errand_slate` (5,1) and `forge_fetch_slips` (18,4) each pay 2 gold once per waking on the `serving_tray` idiom (`once_per_waking` + `gold`, banked under interactions.gd's `serve:` key); exact ledger 60 -> 62 -> 64 -> 66, each prop's same-waking refusal pays and banks nothing, and a sleep re-arms the field board for a second real bank. `brothers_marker` unbanked at this fixture, so the slate's marker-rate variant is provably unmatched; zero combat |
| `witch_brew_loop` | 9 (fixture `witch_brew_start`) | v0.16.1 #6 ECONOMY: the ingredient-bounded brew arm -- the [Hedge Remedy] bench props (`eloise_cauldron`, `inn_witch_kettle`) had ZERO live coverage before this, which is how an unbounded brew->sell-to-Eloise loop shipped. One cast eats the held `dried_yarrow_bundle` and yields a `remedy_draught` (`item_gained` + `item_lost` on the same cast); the next cast is REFUSED at the `requires_item` gate with the authored hint and banks nothing. `assert_event_count` pins exactly one remedy per yarrow -- the bound. The consuming-recipe dup-output refusal (a held remedy must never bank a free `witch_craft_used`) is unit-proven in `test_sim_core`, which can stage the two-items-held state a save fixture cannot |
| `makings_loop` | 9 (fixture `makings_start`) | #396: `the_makings`, the quest wrapping the [Hedge Witch] grant -- the offer row gated to the PEACEFUL path (exact hub arrays both ways, the force-path save proving absence), `hollow_offering_pot` opened as a quest container, the pen's limping third lamb tended, then the kettle banking the FROZEN `witch_lessons` and a real sleep granting the class |
| `journal_categories` | 3 (fixture `inn_guests_start`) | <!-- v017-L1 --> GH#336 Skills-tab redesign: the tab body is CATEGORY-primary and deduped -- `skill_categories` (the rendered heading order, empty buckets dropped) and `skill_row_ids` (the flattened cursor order, 21 rows for 21 distinct ids) are pinned verbatim off a five-class kit, `skill_groups` still carries the class-primary headings the Classes strip draws, and the checkbox is HONEST end-to-end: it assigns on a Combat-Active row (the field bar HOLDING at 5 -- a combat-only kit must never blank the exploration bar) and on an Exploration-Active row (collapsing the AUTO field bar 5 -> 1), and REFUSES on a Combat-Passive row that reaches no bar at all |
| `journal_quest_hints` | 9 (fixture `door_awakening_start`) | <!-- v017-L1 --> GH#338 quest hints: `door_awakening_start`'s active beat is `attune`, the ONE code-banked beat in the game (no map produces `door_awakened`; sleep_beat.gd does), so its own copy is the only pointer a player gets — the exact shape the friend's report named. Pins the payload's new `quest_line_texts`/`quest_hint_lines` (the count-only gap closed), the indented sub-row rendering DEFAULT ON, the 'Quest Hints' settings row blanking it with the quest line itself byte-unchanged, and switching it back on. THE only script that pins the hint array |
| `line_display_ab` | 9 | <!-- v017-train --> GH#324 regression capture, promoted canonical at the v0.17 train: the same talk_pool bark interacted at world_ready+0 and +90 idle frames must BOTH render; asserts the derived capture-hold ceiling (TestDriver.capture_hold_ceiling_msec). The feel_peek_{day,dusk,night} triptych stays a windowed-only utility (title_peek class, not canonical). <!-- v018-W3 --> Leg B retargeted to the room's other pool NPC (stationer_client) when #318 gave stationer_clerk a `conversation` -- his second interact now opens a graph, and the A/B is bark-panel-vs-bark-panel, not NPC identity |
| `player_room_loop` | 9 (fixture `player_room_start`) | <!-- v017-R1 --> #350 the room you buy: hallway_door_a shut at 19 gold, the register's lease rendered LOCKED (exact 2-row option array), a 1-gold tray earn, the 20g purchase, the SAME door opening, the new room's own bed sleep beat, a pause Save/Load that walks out first so the load has to put you back in, and a NIGHT leg (second fixture player_room_night, swapped in and reloaded) so the room is read after dark |
| `invrisil_house_name_talk` | 9 (fixture `invrisil_house_name_talk_start`) | <!-- v018-W3 --> #318 'The Name and the Habit', TALK: the PRE-QUEST scribe (his counter pinned at TWO rows -- the three banking arms hide until the card is shown, the only pin on the #318 option-level quest gate), the Lady's hub with I1 already closed (its option array pinned whole -- proof five new rows moved nothing shipped), the boulevard carriage wake (Magnolia felt, never seen), the scribe's FREE ask refused then his [Charming Smile] chain, the broker's persuade banking `block_handed_over` on 25 gold unspent, the steward's report, the journal's Completed list pinned whole (the surface the fork-mislabel reached), the Lady's coda. Owns three negative pins its siblings cannot: [Appraise Foe] and [Stealth] LOCKED, and the 30-gold buy LOCKED at 25 gold |
| `invrisil_house_name_skill` | 9 (fixture `invrisil_house_name_skill_start`) | <!-- v018-W3 --> #318 SKILL, the mirror ledger: [Appraise Foe] reads the scribe's blotter, [Stealth] lifts the block off the broker's table (the game's first [Stealth]-gated dialogue option), both persuade arms VISIBLE-LOCKED. Also owns the shared-settle re-entry pin -- all four fork rows retire together off `seal_block_settled` |
| `invrisil_house_name_fight` | 9 (fixture `invrisil_house_name_fight_start`) | <!-- v018-W3 --> #318 FIGHT, the thread's only coin spend: neither Skill held, so beat 1 takes the 8-gold alternate (25 -> 17) and beat 2 takes `broker_hands`, which composes the SHIPPED `mercantile_alley` arena with the SHIPPED footpad pair (`alley_footpads_t3_warrior10_solo`, no new balance cell). Band fixture at 10, DAY for arena legibility, `on_victory` in its ARRAY form, then the broker's beaten register |
| `martial_field_loop` | 9 (fixtures `martial_field_start` -> `martial_field_armed`) | <!-- v019-L2 --> GH#380/#381/#382/#383 THE martial exploration canonical: phase 0 at warrior 5 walks every REFUSAL (both maps' own `unsteady_toast`, four `locked_toast`s, the carcass's `skill_unknown`), then the armed fixture swaps in through the pause-Load path and [Even Footing] crosses both unsteady placements, [Greater Strength] opens the north gallery, [Rope Work] rigs the drop and the run leaves through it, [Basic Repair] decks the riverfarm bank walk -- both `state_set` seams then survive a save round-trip AND a sleep (the PERMANENT persistence class, unlike a frozen channel). Also [Broader Shoulders]' waking cap, [Flame Jet]'s one-shot carcass, and [Ice Floor] cast out of combat. `burned_the_debris` absent whole-run proves the carcass's authored arm outranks the generic burns x burnable row; zero combat |
| `winter_teeth_talk` | 9 (fixture `winter_teeth_start`) | <!-- 396-lane-d --> #396 'A Winter of Teeth' TRACK route (terminal path) + the owner of the offer conversation (`winter_brief` <-> `winter_topic`, both directions) and the REPORT-BLOCKED proof: the post-accept hub's EXACT option list is pinned with all three report rows hidden. `wolf_sign_trail`'s presence, gating and once-semantics are proved by CELL OCCUPANCY -- an absent prop's cell is walkable, a present one blocks (`is_cell_blocked` blocks on any present entity), and `absent{pack_traced}` retires it, so the player ends up standing where the prop was. Day fixture: `river_wolf_pack` disarmed, `combat_started` absent whole-run |
| `winter_teeth_work` | 9 (fixture `winter_teeth_start`) | <!-- 396-lane-d --> #396 'A Winter of Teeth' WORK (fold) route IN ISOLATION -- the owner of the prop-gating contract: `winter_fold_hurdles` (15,12) is walked ONTO before the offer, BLOCKS after it, and is walked onto again once it pays out (approach is from (14,12), the only clear cardinal -- (15,11) is map-blocked). Toast pinned verbatim as both `toast` and `ui_toast_rendered`; `pack_traced`/`survived_wolf_night`/`hunter_will_come` all absent whole-run |
| `winter_teeth_fight` | 9 (fixture `winter_teeth_night_start`) | <!-- 396-lane-d --> #396 'A Winter of Teeth' FIGHT route -- the ONLY live coverage the watch ask and the shepherd-as-ally have after #396 stripped `allies` from both briar fights. `river_wolf_pack` fires off its `trigger_radius` with `combat.combatants.riverfarm_hunter.side` == player asserted INSIDE the open board, off a `hunter_will_come` earned live (the ask is gated on `heard_winter_teeth`, so the leg takes TWO conversations). Band fixture at the GEARED `t3_warrior10` reference build (`river_wolf_pack_t3_hunter`'s cell); NIGHT via `actions_since_sleep` 1000 rather than `riverfarm_fight`'s 940-interact burst. Also the `text_variants` precedence pin: three variants match, the LAST must win |
| `winter_teeth_prebank` | 9 (fixture `winter_teeth_prebank_start`) | <!-- 396-lane-d --> #396 'A Winter of Teeth' PRE-BANK COHORT -- a player who already survived the wolves quest-free, then meets the shepherd. The hub's `text_variant` acknowledges the history while NO row is unlocked by it (watch ask and watch report both need `heard_winter_teeth`), accepting completes the resolve beat SILENTLY -- measured: NO `quest_beat_completed` fires for beat 1, because nothing transitions -- and the report waits in the very next hub at a DIFFERENT index than the fight leg's. Two player actions, two toasts, one quest, beats in order |
| `briar_arch_fire` | 9 (fixture `near_briar_arch_fire`) | <!-- 398-pocket-p5 --> Burns the western briar with [Flame Jet], banks the shared dungeon trace, and opens the unique cache. Owns the BURN half of the private-counter contract: the arch banks its own `burned_the_briar_arch`, the burns row's shared `burned_the_debris` is absent whole-run, and the run walks the sewers `nook_strongbox` cell (0,3) to prove that two-regions-away chest stayed shut -- `collapsed_gallery_burn`'s cross-map pattern. Also the ARMED-ADJACENCY proof: it ends standing on (10,11), the exact cell `briar_arch_cut` starts the fight from, with the encounter's gate already MET, so `combat_started` absent means the wards need an interact rather than proximity |
| `briar_arch_cut` | 9 (fixture `near_briar_arch_cut`) | <!-- 398-pocket-p5 --> Sword-equipped [Power Strike] cuts the western briar, banks the same trace, and clears the armed +3-band ward encounter (the GATED `briar_arch_wards_warrior11_relc` cell's configuration -- the melee profile is the only one real autoplay reaches). Owns the CUT half of the private-counter contract: `cut_the_briar_arch` banks, and both shared row defaults (`cut_through_growth`, `burned_the_debris`) stay absent whole-run |
| `briar_arch_locked` | 9 (fixture `near_briar_arch_locked` -> `near_briar_arch_unarmed`) | <!-- 398-pocket-p5 --> TWO phases off two fixtures. Phase 0 (no fire, no blade): the one two-mode refusal every build gets, the impassable briar cell, and THE encounter-gate proof -- `briar_arch_wards` has no `present_when`, so the entity blocks (10,10) while its `encounter_when` is unmet, and interacting answers with `gate_closed_toast` instead of a fight (which is why `entity_present` can never carry this gate). Then the structurally absent cache from an inside probe. Phase 1 swaps in `near_briar_arch_unarmed` through the pause-Load path (martial_field_loop's idiom): a Warrior 11 who KNOWS [Power Strike] but holds no sword -- `assert_field_skill_absent` proves the weapon gate keeps it OFF the field bar, so the cut mode is unreachable rather than merely refused. Also the only tripwire against re-adding a single-skill `requires_skill` (asserts `skill_unknown{skill: kindle}` never fires) |
| `collapsed_gallery_pick` | 9 (fixture `collapsed_gallery_pick_start`) | <!-- 398-p2 --> Held-pick route through mortared rubble; trap kit remains held, nest is observed and cleared, strongbox yields `old_delvers_clasp` |
| `collapsed_gallery_strength` | 9 (fixture `collapsed_gallery_strength_start`) | <!-- 398-p2 --> Distinct Warrior fixture uses [Greater Strength] on the fallen beam, then clears the nest and claims the shared reward |
| `collapsed_gallery_burn` | 9 (fixture `collapsed_gallery_burn_start`) | <!-- 398-p2 --> [Flame Jet] burns the tarred shoring through the property table, then clears the nest and claims the shared reward; CROSS-MAP isolation leg (review HIGH-1) -- the shoring banks its OWN `burned_the_gallery_shoring` via the burns row's `counter_from: target` override, `burned_the_debris` asserted absent, and the run walks THROUGH the sewers nook cell (0,3) to prove that strongbox stayed shut |
| `collapsed_gallery_negative` | 9 (fixture `collapsed_gallery_unready_start`) | <!-- 398-p2 --> All three blocker hints, impassable cells, and structurally absent pre-clear reward |
| `warded_side_vault_rogue` | 1 (fixture `warded_side_vault_rogue_start`) | #398 pocket mode A: [Find Trap] -> [Disarm Trap], whose terminal banks BOTH `warded_vault_disarmed` and the shared `warded_side_vault_open` the guard's `encounter_when` reads (the String\|Array `accomplishment` contract), then the retuned construct, the dungeon-thread clue, and the unique counterweight |
| `warded_side_vault_warrior` | 9 (fixture `warded_side_vault_warrior_start`) | #398 pocket mode B: the noisy [Greater Strength] pry banking `warded_vault_forced_noisy` + the same shared `warded_side_vault_open`, then the same guard and the same unique reward from the other entrance |
| `warded_side_vault_gate_check` | 9 (fixture `warded_side_vault_gate_start`) | #398 pocket negative, and THE POCKET'S REACHABILITY AUTHORITY: a kitless Mage walks the whole seal perimeter and takes `player_blocked` on all ten y10 threshold cells plus (9,11)/(9,12)/(9,13) from x10 on the east face, refuses at both named modes in place, and DERIVES reward-absence -- no gate counter banks, no combat starts, nothing is gained, and `player_moved` never fires for any pocket cell including the guard's (4,12) and the cache's (4,13). NO teleport: the first version teleported in, which proved a `present_when` gate and missed an east face you could walk straight through |
| `counting_room_social` | 9 (fixture `counting_room_social_start`) | <!-- 398-lane-p4 --> #398 counting-room SOCIAL route: [Bargain] opens the factor gate, destination text is exact-pinned, the tuned guard fight resolves, and the factor-sealed trade bale is claimed |
| `counting_room_quiet` | 9 (fixture `near_invrisil_stealth`) | <!-- 398-lane-p4 --> #398 counting-room QUIET route: the has-skill interact HINT fires first, [Appraise Foe] times the watched side shutter, and the whole route is WALKED to the same bale the social leg claims -- neither sibling counter ever banks. The only public cell adjacent to that shutter sits inside shipped `alley_footpads_a`'s radius-2 box, and this fixture does NOT pre-remove it, so the leg resolves that ambush first: the counting-room fight is the SECOND `combat_started`, with `cleared_footpads_a` asserted between them |
| `counting_room_force` | 9 (fixture `invrisil_hat_start`) | <!-- 398-lane-p4 --> #398 counting-room FORCE route: hint toast, then [Greater Strength] banks `noisy_entry`, the factor's reaction line is destination-asserted (the SOLE pin on `rear_account`), and the leg walks on through the tuned guard to the bale. Cohort twin of the quiet leg: this fixture carries `alley_footpads_a/b` in `removed_entities`, so the same seam is proved with the ambush GONE |
| `counting_room_gate_check` | 9 (fixture `near_trader`) | <!-- 398-lane-p4 --> #398 counting-room NEGATIVE: visible-locked [Bargain] (destination node asserted absent), both arm refusals with `player_blocked` at each seam, and the pocket proved SEALED by walking (8,12) -> (7,12) -- the one cell whose blocked row closes it. Reward absence is derived, not teleported to: the cache's only spawn condition (`cleared_counting_room_guard`) is asserted never-recorded whole-run and both reward cells never entered. `teleport` is `Game.sim.transition()` and ignores blocking, so a teleport-then-`player_cell` probe proves nothing here |
| `pond_island_freeze` | 9 (fixture `pond_island_freeze_start`) | <!-- 398-p1 --> #398 P1 mode A on the pond's SOUTH-EAST lobe (the island was re-sited so the shipped `frozen_cache` at (10,18) keeps water on all four sides): the `pond_narrows` reeds carrier at (13,19) answers the bump the launch pad (13,20) actually makes, `freezes` x `freezable` opens the narrows (12,20), and the retuned +2-band guardian and its victory-gated unique cache pay out |
| `pond_island_blink` | 9 (fixture `pond_island_blink_start`) | <!-- 398-p1 --> #398 P1 mode B: the honest Runner 5 build (classes.json grants [Double Step] at 5) crosses exactly the one water cell (12,20) from the SAME launch pad the freeze leg casts from, and `frozen_cells` stays `{}` whole-run -- the two modes are proved independent, not just both-present |
| `pond_island_negative` | 9 (fixture `pond_island_negative_start`) | <!-- 398-p1 --> #398 P1 negative, and the K5 teaching pin: bumping the narrows (12,20) is answered with a block and NOTHING (no engine bump-toast exists for water -- `_hint_unsteady` is `unsteady`-only), then WITHOUT leaving that cell the reeds carrier is bumped and speaks, naming freeze AND [Double Step]. No traversal event, no fight, no reward |
| `riverfarm_islet_freeze` | 9 (fixture `riverfarm_islet_freeze_start`) | <!-- 403-riverfarm-islet --> #403 mode A: [Ice Floor] freezes the near river cell, the player walks onto the dry crown, and the one-shot ferry-tally cache pays out |
| `riverfarm_islet_blink` | 9 (fixture `pond_island_blink_start`) | <!-- 403-riverfarm-islet --> #403 mode B: the honest Runner 5 [Double Step] crosses from the lower bank to the same dry crown without freezing any cell, then claims the same one-shot cache |
| `riverfarm_islet_negative` | 9 (fixture `riverfarm_islet_negative_start`) | <!-- 403-riverfarm-islet --> #403 negative: a hotbar-bearing build with neither crossing mode pins the dry crown's rendered blocked-cell delta, walks to both river refusal cells, and earns no traversal, ice, or reward |
| `witch_briar_burn` | 9 (fixture `witch_briar_burn_start`) | <!-- 403-witch-briar --> #403 mode A: an honest Mage 2 walks to the new Witch Hollow briar gate, burns it with [Flame Jet], enters the sealed edge pocket, and claims the one-shot thorn-tally cache |
| `witch_briar_cut` | 9 (fixture `witch_briar_cut_start`) | <!-- 403-witch-briar --> #403 mode B: an honest Warrior 1 with a sword equipped walks to the same gate, cuts it with [Basic Swordwork] through `field_weapon:sword`, enters the pocket, and claims the same cache |
| `witch_briar_negative` | 9 (fixture `witch_briar_negative_start`) | <!-- 403-witch-briar --> #403 negative: an honest Helper walks to the newly-created gate, pins the pocket's five-cell rendered-blocking delta and the gate-cell refusal, and earns neither opening mode nor reward |
| `pond_frozen_cache` | 9 (fixture `pond_frozen_cache_start`) | <!-- 398-p1 --> #398 P1 review H2: the SHIPPED `frozen_cache` beat, and the FIRST coverage `bounty_pond_keepsakes`' `found_the_frozen_cache` objective has ever had. Proves the shipped strings literally against geometry this wave did not move -- (10,17) freezes, the player is STANDING on that ice when 'Kneeling on the ice' fires off (10,18), gold 20 -> 25 |

## Working conventions

- Add ANY player-visible feature → extend QA script (or add new one in `qa/scripts/`) that walks it, asserts both domain event AND `ui_*_rendered` confirmation; run before claiming feature works.
- Content = data + code: new entities/skills in `data/*.json`; new behavior in sim; presentation only renders.
- Tests = plain SceneTree scripts under `tests/` (no GUT/gdUnit). Pure classes only — anything needing autoloads = QA-script territory.
- Balance changes = `combatants.json`/`skills.json` edits validated by `sim_combat_batch.gd` (GATED cells win-rate 0.55-0.95, median rounds 3-12; `goblin_ambush/warrior2` measured-only by user directive — see "Combat depth (M3)" block in `docs/ARCHITECTURE-HISTORY.md`) — never tune by feel. Combat data/rule change can invalidate canonical QA seed: re-verify `combat_walkthrough`/`level_up_loop`, update seed in this file's canonical seed table (and `qa/ci_sweep.sh`'s `CANON` array) if needed.
- GDQuest GDScript style: tabs, static typing, `class_name` + `##` doc comments.
- **Comment style (user directives 2026-07-07 + 2026-07-12, binds all new code + _comment fields):** comments state FUNCTION, CONSTRAINTS, TRAPS — never provenance — and are CAVEMAN-COMPRESSED: every comment earns its tokens (drop articles/filler/narrative; keep exact terms/ids/numbers; one compressed sentence of function + one of trap, max, for most JSON entities; state a trap ONCE at the seam and cross-reference by symbol, never per call site). Targets: .gd <= 20% comment lines, data <= 15% _comment chars (GH#102 tracks the census). No task/wave/review-finding narratives, no issue citations (exception: forward-promise to OPEN issue may cite it until issue closes). History lives in git blame, ledger, docs/ARCHITECTURE-HISTORY.md. Trap/contract comments (drift seams, order constraints, payload shapes) = the valuable kind — keep writing those. Legacy corpus thinned by GH#44 at 8a→8b boundary.

## Gotchas

- `CanvasLayer` has no `modulate` — tint/fade child Controls instead (exact bug shipped dead quest chain in v2).
- **Godot 4.7 `ResourceLoader.load()` returns NON-NULL but uncompilable script resource when .gd has parse/compile error** — null-checking load() not enough; load gate also checks `Script.can_instantiate()`.
- `--script`-mode runs don't resolve autoloads as bare identifiers — why purity rule exists; no autoload refs from `src/core/wi_game.gd`, `event_log.gd`, `qa_paths.gd`, or `tests/`.
- Commit generated `*.uid` sidecars alongside new `.gd` files.
- **Sprite frames with transparent padding below figure need measured `anchor`** (data/sprites.json). Feet-anchoring uses FRAME bottom — Body_A/Citizen_F's 16px under-feet padding (figure y 18–47 of 64) drew every character exactly ONE CELL above logical cell; player visually aligned with door/prop was physically row off, interact hit nothing. Fixed with `anchor: [0.5, 0.75]`. New character/prop sheet: measure figure bbox (PIL alpha scan), set anchor from FEET PLANE, not frame edge; verify with windowed adjacency screenshot.
- Web QA seeding (M3 T8): `run_web_qa.sh <script> <seed>` threads seed through `run_web_qa.mjs` -> `window.__WI_QA__ = {script, seed}` -> `game.gd._build_sim` reads via `JavaScriptBridge.eval(...)` only when `OS.has_feature("web")` AND no `--seed=` user arg given natively. Only `game.gd` reads *seed* off bridge (`qa/test_driver.gd` already uses `JavaScriptBridge.eval` for script-name/screenshot/result plumbing) — sim core in `src/core/**` never touches bridge, sim purity unaffected.
  Combat QA scripts now web-runnable (parity with native); `load_gate` stays native-only (see above).
- `WICombatAI.take_turn`'s `_act_once` defaults empty `"ai"` field (PC's own combatant config) to **melee** profile, which never inspects ranged/spell skills. So `combat_autoplay` NEVER casts spell for player character, any seed, any classes — `mage_unlock_loop` verifies mage kit via `combat.combatants.pc.max_mp` (nonzero only when known skill carries `mp_cost`), not observed cast. Similarly `action_refused {reason: "no_los"}` unreachable under AI-only play: `_act_ranged` pre-filters targets with explicit `has_los` check; `_act_line` filters *indirectly* — `line_cells` truncates at first blocked cell, blocked cells can't hold combatants, so wall-obstructed direction can never satisfy ≥2-enemies-hit gate. Dead-code safety net under AI play, not reachable QA event. (Player path: single-target spells also pre-filtered — `combat_screen.gd` drops non-LoS targets before selection — refusal live only via player's line-skill direction picker, which has no filter.) `line_of_sight_denial` proves has_los gate positively: `flame_bolt` (wall-aware ranged cast) resolves successfully, `action_refused/no_los` never fires, uses `goblin_ambush` arena (only arena with ranged enemy), not walled-but-all-melee `chieftains_raid` roster. `level_up_loop` fight 2 swapped from `goblin_encounter_1` to `chieftains_raid` for same structural reason: in `goblin_encounter_1`, `relc` (ally) always lowest-HP/nearest target, lone melee enemy dies before reaching PC — `counter_strike` (riposte) can never trigger there, any seed; `chieftains_raid`'s 3 melee enemies give PC real chance of being hit (win rate 0.61 per balance harness — hence explicit seed search, not first-seed-tried pick).
- **PC death = immediate DEFEAT, even with living ally (WAVE A2 §7, post-D4 user-confirmed directive).** `_check_end` in `wi_combat.gd` checks `combatants["pc"]["alive"]` FIRST, before team-wipe scan — previously fight only ended in defeat when BOTH pc and every ally down; fight where pc dies mid-round with ally (`relc`) alive used to CONTINUE, could still resolve as win (ally mops up rest). Exactly what broke `level_up_loop` old seed 11: fight 2 (`chieftains_raid`) — pc down round 3, `relc` alive, `goblin_chieftain` at 4 HP, previously won when `relc`'s next turn finished chieftain — now instant defeat. Re-derived to seed 12 (wins fight 2 outright, still fires `reaction_triggered{counter_strike}`). **Any future combat-data or sim change: re-check this same failure mode** — canonical seed that won via "pc goes down, ally carries the win" no longer can; search replacement where pc actually survives, don't assume old win margin still applies. `sim_combat_batch.gd` structurally unaffected (`combat.outcome["victory"]` already reflects whichever branch `_finish` took), but gated-cell win rates can shift (losses no longer require every ally down too) — re-run harness after any defeat-condition change, not just data changes. Rule's canonical QA proof: `defeat_ally_alive` (seed 11, table above) — re-verifies "PC dies while Relc lives = instant defeat" through real input/AI/UI path every sweep.
- **Message panels budget WRAPPED LINES, not entries (M-FP F fix — keep it that way).** Combat feed (`combat_screen.gd`) + world `dialogue_line` panel (`message_layer.gd`) both fit text by measuring wrapped visual lines at panel width (`Font.get_multiline_string_size`; line pitch = font height + theme `line_spacing` — string-size call does NOT include line_spacing, real trap), then EVICT oldest feed entries / TRUNCATE with ellipsis until total fits panel art-safe capacity (parchment bottom fold bleeds above naive margin — feed effective text height 90px, not 106). Design rule D2-7 #6 binding: cut words, never widen UI. Dialogue panel truncates only ON-SCREEN string — `ui_dialogue_rendered` still carries full text (QA asserts exactly). Any future panel or `tutor_lines` copy change: re-verify with windowed `relc_tutorial` + `gate_district_walkthrough` runs, read by eyes.
  **M-LEGIBILITY L5 extended this to THIRD panel:** `combat_hud.gd` readout (`_readout_label`, a `RichTextLabel`, not `Label` — see `_rtl_wrapped_line_count`/`_rtl_line_capacity`/`_rtl_fit_to_lines`, distinct from feed's `Label`-typed helpers because RichTextLabel real theme properties = `normal_font`/`normal_font_size`/`line_separation`, not Label's `font`/`font_size`/`line_spacing`) had NO budget at all until this fix — L4 windowed shot (`.superpowers/sdd/fp-handoff/l4-shots/01_first_encounter_feed.png`) caught frost_bolt full 3-segment slot-info line wrapping to 2nd line (head + hint + 2 info lines = 4 total), 4th line rode parchment bottom fold exactly like pre-fix feed/dialogue panels. Fixed via `_compose_readout`: fits ONLY variable-length slot-info segment to capacity remaining after `head`+`hint` (capacity 3 lines total, calibrated to RichTextLabel real font pitch — see READOUT_TEXT_WIDTH/HEIGHT doc comment in `combat_hud.gd`). Same truncate-render-only contract: `ui_slot_info_rendered` still carries FULL escaped line (`combat_move_input.json` pins exactly for `[Power Strike]`) — only `_readout_label.text` gets fitted version. Windowed-reverified via `status_first_encounter` (frost_bolt line now truncates cleanly to 3 lines, no fold bleed).
- **Onboarding rev classless start (Task O1, 2026-07-05):** `player.classes` starts `{}` (no class, no combat skills); `warrior` now EARNED via `sparred_with_relc` (banked by shipped `relc_spar` tutorial fight), granted at next sleep. UI/hotbar/journal degrade gracefully with zero classes. Full narrative (incl. re-path window Task O5 later closed): `docs/ARCHITECTURE-HISTORY.md` "Onboarding rev classless-start" section.
- **A lambda Callable stored in a long-lived RefCounted module LEAKS ObjectDB instances at exit** (#194a seam 1, 2026-07-19): wiring WIInteractions' current_map getter as `func(): return current_map` produced `WARNING: 12 ObjectDB instances were leaked` on headless smoke (baseline clean, stash-verified); the identical wiring as a bound method (`_current_map_name`) is leak-free. All `WIGame._init` module wiring (WIEconomy/WISocial/WIFieldSkills/WIInteractions) uses bound-method Callables — keep it that way; transient lambdas (sort comparators etc.) are fine.

## GodotPrompter

Godot project, GodotPrompter skills available. Before implementing any game system, MUST check for matching `godot-prompter:*` skill and invoke it. Binds all agents, subagents, sessions in this repo.

Key skills: `gdscript-patterns`, `scene-organization`, `component-system`, `resource-pattern`, `event-bus`, `godot-ui`, `state-machine`, `godot-testing`.

Full skill list: invoke `godot-prompter:using-godot-prompter`.
